# Attention Guardian 当前工作交接

## 启动路径注意事项（2026-07-27）

- 数据库 schema 已升级到 v3。不要从
  `tests/AttentionGuardian.Desktop.Tests/bin/.../AttentionGuardian.Desktop.exe`
  启动应用；测试输出目录可能保留旧版 v2 程序，它会因为拒绝读取更新后的 v3
  数据库而退出。
- 当前 Release 程序位于
  `src/AttentionGuardian.Desktop/bin/Release/net10.0-windows10.0.19041.0/win-x64/AttentionGuardian.Desktop.exe`。
- 已使用现有用户数据库实际启动并确认该版本能够保持运行；本次未删除或重建数据库。
- Windows 通知依赖 Microsoft Windows App Runtime 2.3.1 x64。当前机器已经安装微软
  签名的官方运行时，并实机确认测试通知成功交给通知中心。GitHub 发布安装器必须部署
  同版本运行时，不能只复制应用 EXE。

更新日期：2026-07-27

## v0.1.0 用户验收结论（当前最高优先级）

2026-07-27，用户明确确认：

> 整个软件已经达到我的 v0.1.0 发布标准。

该结论是当前产品范围与交接状态的最高优先级事实，覆盖本文件下方历史章节中
“仍处于 UI 美化阶段”“当前不是正式版本完成状态”“某项历史延期功能必然阻塞
v0.1.0”等旧判断。

- 当前代码、交互、视觉、ambient 背景与启动动画共同构成用户批准的 v0.1.0 基线。
- 未获得用户明确授权前，不得继续以“顺手优化”“发布前再润色”为理由改变已获批
  UI、终稿 `final.svg`、启动笔顺、动效时序、页面职责或业务规则。
- “达到用户的发布标准”表示产品验收基线已经成立；它不等于已经完成版本号更新、
  CHANGELOG、打包、签名、Git 提交、Tag、发布页或安装包发布。不得声称这些尚未执行
  的发布操作已经完成。
- 历史记录中的 Windows 五分钟前系统通知、最终诊断界面与完整真实窗口自动化仍是
  已知未实现项，但根据本次用户验收，它们不再自动构成 v0.1.0 发布阻塞项。是否移入
  后续版本，应由下一位 Codex 在更新 Roadmap 与指向性文档时忠实记录，不得擅自重新
  扩大 v0.1.0 范围。
- 最近一次验证结果：Desktop 32 项、完整 Solution 155 项 Release 测试通过；
  Release 构建为 0 警告、0 错误；`final.svg` SHA-256 仍为
  `7140DE6A319D7AE59599E16E28B78AC1586BC4045190137692D0C0A9042ABE72`。

2026-07-27 后续实现已修复完成记录差异：当日数据库 schema 升级到 v3，已结束事项
标记 `completed` 并保留完成时刻，用户删除标记 `deleted`，活动查询只返回
`active`；整组排程更新不再物理清空完成历史。v0.1.0 只保留数据，不接入或调用 AI。

## 下一位 Codex 的首要任务

下一位 Codex 不应继续写功能或美化 UI。第一项工作是进行 v0.1.0 文档一致性盘点，
然后更新 `ROADMAP.md` 及其他会向贡献者、用户或后续 Agent 指示项目状态的文件。

至少检查：

1. `ROADMAP.md`：把用户已验收的 v0.1.0 能力标为当前基线；将非阻塞延期项放入正确
   后续阶段，删除或更正与实际软件冲突的旧路线描述。
2. `README.md`、`PRODUCT.md`、`CHANGELOG.md`：核对版本状态、已具备能力、明确不做
   的范围和面向用户的描述。
3. `PRODUCT_DECISIONS.md`、`PROJECT_DEVELOPMENT.md`、`ARCHITECTURE.md`：
   只在存在事实冲突时补充或更正；保留历史决策，不把历史过程改写成从未发生。
4. `SETUP.md`、`CONTRIBUTING.md`、`SUPPORT.md`、`SECURITY.md`：检查其中是否仍指向
   旧名称、旧命令、旧功能状态或错误的发布阶段。
5. `AGENTS.md`：其“当前优先级”和部分早期 UI 说明可能已经过时。应在不破坏长期
   产品边界的前提下更新为当前阶段，但必须先对照真实代码、测试与本交接结论。

文档同步完成后，再向用户报告哪些文件被更新、哪些历史未完成项被安排到哪个版本。
本交接没有授权提交、推送、创建 Tag、发布 Release 或制作安装包；需要这些操作时
必须等待用户明确要求。

## 下一对话开始前

必须按以下顺序完整阅读：

1. `AGENTS.md`
2. `HANDOFF.md`
3. `DESIGN.md`
4. `PRODUCT.md`
5. `ARCHITECTURE.md`
6. `PRODUCT_DECISIONS.md`
7. `PROJECT_DEVELOPMENT.md`

当前工作主线已经从 UI 美化切换为 v0.1.0 文档一致性与发布准备盘点。`DESIGN.md`
仍是不可破坏的已验收视觉基线；下一位 Agent 应完整阅读，但本轮重点是让 Roadmap、
README、CHANGELOG、产品决策和 Agent 指引与当前软件事实一致，不应继续修改 XAML。

`teaching.md` 不属于接手时必须完整通读的文件。它是只追加、不回写的历史教学
日志；下一位 Codex 只需阅读文件开头的用途与追加规则，搜索当前任务关键词，并
阅读最近相关章节。执行实际项目操作后，只能在文件末尾追加新章节，不得修改、
重写、整理、合并或删除已有教学内容；历史内容过时时，在末尾追加更正说明。

随后检查实际目录与 Git 状态，并重新运行最小相关测试。

旧名称 `HostedTime`、旧单会话 UI、首页时间展示和“不可移动冲突时拒绝保存”
均已过时，不得恢复。

## 用户给下一对话的直接任务

当前主要任务已经切换为 **UI 美化、设计系统收敛和真实落地核验**。不要先继续扩展
新的业务功能，也不要因为文档写着“已完成”就直接跳过现有功能。下一位 Agent 接手后
应先建立一份“声明—代码—自动化测试—真实窗口”核验清单，亲自检查下列能力是否真的
在软件中可见、可操作、可保存，并记录发现的偏差：

1. 添加类型选择页、当日待办表单、未来待办表单是否都真正采用低不透明度、克制高光
   和短阴影的 ambient frosted surface，而不是实心黑块或 Windows 合成后的银灰块。
2. 不可移动事件冲突提醒是否实际呈现红色 Apple 风磨砂材质，默认折叠，展开后显示
   冲突双方或冲突组，并且只保留一个“进入管理”入口。
3. `GlassNumberStepper` 是否与 ambient 背景同色调，减号、可直接编辑数值、加号处于
   一个连续胶囊中；按钮步进、键盘输入、回车和失焦提交都要真实操作。
4. 当日待办的开始时间小时、冒号、分钟是否形成紧凑连续组；冒号必须严格处于两侧
   控件中间并垂直居中。持续时间两项及未来日期控件也要检查基线和间距。
5. 管理页普通任务真实鼠标拖拽、队尾落位、拖回首位、休息模板复制是否仍然有效；
   不能只运行 ViewModel 测试后宣称拖拽可用。
6. 管理气泡双击展开/收起是否有克制的 ease-in-out，高度连续变化、下方气泡同步移动、
   间距不变，并在减少动态效果时正确降级。
7. 未来待办“一天后 / 两天后”、未来待办展开编辑、多个不可移动事件冲突提醒，以及
   `End <= now` 的已完成任务从管理页消失，是否都已实际接入当前 UI。

核验规则：

- 代码中存在命令、绑定或样式，不等于功能已经落地。
- 自动化测试通过，不等于真实鼠标拖拽、窗口合成材质、弹层位置和动画已经可用。
- 每项至少核对实际绑定路径；交互类和视觉类项目还必须使用隔离数据目录启动真实
  Windows 窗口操作，禁止向用户正式数据库注入验收样例。
- 若真实结果与本文“已完成”描述冲突，以真实软件为准，修复后同步更新本文、
  `PROJECT_DEVELOPMENT.md`，并只在 `teaching.md` 末尾追加更正记录。

UI 美化仍应保留已经确认的上位基线：

- 全页面连续的蓝青色 ambient 背景，以及当前明亮但安静的色彩逻辑；
- 无传统 Windows 标题栏、右上角三个圆形窗口按钮和顶部拖拽抽屉；
- 专注首页的圆环开始入口、低信息密度与完全隐藏时间的规则；
- 磨玻璃下拉、气泡式管理界面和现有组件形状；
- 当前逐元素 fluid reveal 页面转场：背景保持稳定，标题、面板和次级区域依次
  从下方上浮；不得恢复旧的整页光团闪烁、整体缩放或深浅背景切换；
- Windows“减少动态效果”下的静态降级。

可以继续调整磨砂材质、透明度、阴影、高光、字体、行高、控件内部对齐和局部留白，
但不得借精修之名重做 ambient 背景、整体布局语言或页面职责。任何功能修复仍必须遵守
Application/Core 合同、双数据库边界，并同步测试、`PROJECT_DEVELOPMENT.md` 与
`teaching.md`。

## 当前结论：首版发布前状态

- 添加、管理、排程、冲突/顺延应用内提醒、ambient/frosted glass UI 与启动动画
  已构成用户确认的 v0.1.0 产品基线。
- 到期未来待办“规划”闭环和进入表单时刷新默认开始时间已经实现；下方旧章节把它们
  列为缺口的内容仅是历史记录。
- 当日待办完成历史保留差异已经修复并通过迁移/持久化测试。
- 五分钟前 Windows 系统通知发送和五秒进程内调度已经接入；最小化与系统通知设置
  矩阵仍需发布前实机复查。完整逐步诊断仍属于后续扩展，但九类本地验收表单已实现。
- 正式打包、签名、版本 Tag、GitHub Release 和对外发布尚未执行。

## Git 与工作区

- 本地 Git 已初始化，分支为 `main`。
- 仓库尚无提交；当前文件原本即处于未跟踪状态。
- 不得擅自提交、推送、创建远程仓库或覆盖用户工作。
- Solution：`AttentionGuardian.slnx`。
- 技术栈：.NET 10、Avalonia 12、CommunityToolkit.Mvvm、SQLite。

## 当前验证基线

- Core：63 项通过。
- Application：44 项通过。
- Infrastructure：16 项通过。
- Desktop：24 项通过。
- 合计：147 项 Release 测试通过。
- Solution 格式检查通过。
- Release 构建：0 个警告、0 个错误。
- 最近一次依赖漏洞扫描未发现已知易受攻击包。

这些数字是历史验证基线，不是下一位 Agent 可以直接引用的当前验收结论。最近一次
玻璃材质与开始时间冒号精修后，Release Solution 构建实测为 0 个警告、0 个错误；
但最终真实窗口复查被用户按 Esc 中止。下一位 Agent 必须重新运行相关测试并从当前
构建开始做真实窗口 UI 核验，尤其不要沿用旧截图判断最新材质。

真实窗口已经检查过：

- 专注首页及开始按钮；
- 顶部拖拽抽屉；
- 添加类型选择页；
- 当日待办添加页；
- 未来待办添加页和事件管理页；
- 未来待办默认“一天后”，以及切换“两天后”时的选中态和具体日期联动；
- 开始时间小时与分钟独立下拉，点击小时不会同时展开分钟。
- 蓝青 ambient 背景在各页面间连续，不出现深色画布闪屏；
- 添加类型页、当日待办表单和管理页的逐元素上浮转场中间态与稳定终态。

2026-07-26 已使用隔离数据库完成普通任务前后重排、队尾落位和休息模板复制的真实
窗口验收。尚未使用隔离样例完成不可移动冲突组自动定位与组内拖拽验收。
不要为了截图向用户真实数据库注入测试事项。

2026-07-27 已使用隔离数据库验证不可移动冲突提醒与完成任务清理：冲突提醒可折叠，
展开后显示双方名称；进入管理页后 `End <= now` 的任务从活动排程中清除。

## 已完成的架构与业务主线

### Core

- `ScheduledTodo`、`UnscheduledTodo`、`TimeRange`。
- `[开始, 结束)` 区间；首尾相接不算重叠。
- 当前事件选择支持跨零点。
- `ScheduleTrial` 支持普通级联顺延、空档停止、越过一个或多个不可移动事件、
  保持持续时间和跨日标识。
- 不可移动事件冲突允许保存完整计划，并返回冲突集合等待用户管理。
- `ScheduleManagement` 支持：
  - 普通事件优先级重排；
  - 不可移动挡板与失败回落；
  - 连续不可移动组识别；
  - 组内重排；
  - 删除后补位；
  - 修改持续时间和不可移动属性后重排；
  - 固定名称“休息”的插入。

### Application

- `IScheduledTodoRepository` 与 `IUnscheduledTodoRepository` 分离。
- `TodoPlanningService` 支持添加两类待办、打开状态、未来待办规划、跨库失败安全重试
  和确认删除。
- 未来待办规划沿用源 `Guid`，重试不会重复创建或再次顺延。
- `ScheduleManagementService` 负责管理状态、完成任务清理、编辑、删除、重排、
  未来待办更新和休息插入；Desktop 不直接计算时间。

### Infrastructure

两个独立、带 `schema_version` 和 v1 migration 的正式 SQLite 文件：

- `attention-guardian-scheduled.db`
- `attention-guardian-future.db`

当日计划整张事务替换，失败回滚。未来待办生命周期为 `active`、`planned`、
`deleted`，活动查询只返回 `active`。高于程序支持版本的数据库会被拒绝。

## 已完成的 Desktop 功能

### 页面与专注首页

- 首页、添加类型页、当日待办页、未来待办页、事件管理页职责分离。
- 首页不显示开始、结束、安全截止、剩余时间、倒计时或进度。
- 有当前事件时，点击“开始”后才显示名称。
- 开始后显示克制的蓝青色背景流动。
- 无当前事件时显示平静空状态。
- 添加和管理入口只在顶部拖拽抽屉。
- 到期未来待办和不可移动冲突使用折叠提醒。
- 不可移动冲突提醒读取 Application 打开状态，展开后显示冲突双方名称。
- 顺延 toast 和跨日提示已有基础实现。

### 添加

- 先选择“当日待办”或“未来待办”。
- 两类表单相互独立。
- 当日待办开始时间拆成小时与分钟两个独立下拉。
- 持续时间推导结束时间，不让用户直接填写结束时间。

### 管理

- 使用独立 `ScheduledTodoBubbleViewModel` 和 `FutureTodoBubbleViewModel`。
- 双击展开，第二次双击收起时统一保存。
- 保存失败保持展开。
- 离开管理页存在未保存修改时可保存、放弃或取消。
- 当日和未来待办删除均显示包含名称的确认层。
- 进入管理页时，结束时刻等于或早于当前时刻的任务当前会从数据库物理移除。目标行为
  是标记完成、保留历史并从活动查询隐藏；当前实现不得作为最终验收行为沿用。
- 删除后由 Core 让后续事件从被删除事件原开始时刻补位。
- 普通事件鼠标拖拽已修复：独立把手在移动超过 6px 后启动拖放，气泡和队尾均可
  落位；隔离真实窗口已验证任务拖到队尾、拖回首位和休息模板复制。
- 支持 `Alt+↑`、`Alt+↓` 键盘等价重排。
- 非法位置回落并显示“这个事件没办法放在这里”。
- 重叠或首尾相接的不可移动事件同一行等分显示。
- 进入管理页后自动定位第一组连续不可移动事件。
- 孤立不可移动事件禁止移动；连续组内允许重排。
- 不可移动事件使用红色材质和文字双重标识。
- 未来待办主动展开后才查询，支持编辑名称、日期和删除。

### 休息模板

- 固定名称“休息”，默认 20 分钟。
- 模板小时和分钟可独立调整。
- 可拖到目标气泡或队尾区域。
- 按钮作为键盘等价操作加入队尾。
- 使用复制语义，可重复插入，模板不会消失。
- 每次生成新的普通事件，永远不是不可移动事件。
- 插入后可调整持续时间、重排和删除，但不可改名。

## UI 大改时必须保持的产品规则

### 专注首页

- 点击开始前不显示当前事件名称。
- 点击开始后名称才淡入。
- 完全隐藏所有时间信息。
- 到期提醒和冲突提醒默认折叠。
- 展开提醒后只提供一个“进入管理”入口。

### 顶部抽屉

- 添加与管理入口不得直接暴露在首页。
- 顶部保留低存在感把手。
- 用户向下拖动后出现抽屉。
- 点击入口或抽屉外部收回，不增加额外收起按钮。
- 必须保留键盘等价访问。

### 管理页

- 气泡顺序就是优先级和执行顺序。
- 普通、不可移动、休息三种状态必须同时通过颜色以外的方式区分。
- 不可移动事件只有在连续组内可重排。
- 气泡编辑没有单独保存按钮，收起时保存。
- 离开未保存修改必须询问。
- 所有删除必须确认并显示事件名称。
- 未来待办默认隐藏且不参与拖拽。

## 延后的 Windows 通知

用户明确表示当前不用着急实现。

2026-07-26 更新：Core/Application 的提醒资格算法已经完成，包括五分钟边界、无空档
相接、排除进入休息、休息结束后正常提醒和进程内单次去重。尚未完成的部分是 Windows
通知发送、轮询调度、最小化真实验证及平台降级。

2026-07-26 更新：不可移动事件重叠时“新输入事件优先成为当前事件”的算法与持久化
已经完成。当日待办保存 `CurrentSelectionPriority`，新输入取最大值加一；SQLite
当日库已从 v1 增量迁移到 v2，旧事件保留并使用优先序 0。该机制只决定专注页当前
候选，不会自动替用户解决不可移动冲突组的最终顺序。打开状态还会从持久化时间表
重新检测所有尚未结束的不可移动重叠事件对，因此重启后冲突提醒所需的数据不会
消失；Desktop 已把这一打开状态字段接入现有折叠提醒。

以后实现时必须：

- Application 提供可测试的当前/下一事件与提醒资格判断；
- 当前事件持续不少于五分钟；
- 下一事件与当前事件首尾相接；
- 下一事件不是“休息”；
- 通知同时显示当前与下一事件名称；
- 有空档、无下一事件、进入休息前均不提醒；
- 休息结束后紧接下一事件时正常提醒；
- 同一事件只提醒一次；
- 窗口最小化仍提醒；
- 只在进程存活时调度，应用退出后不后台常驻。

## UI 大改后的验证

至少运行：

```powershell
dotnet test tests/AttentionGuardian.Core.Tests/AttentionGuardian.Core.Tests.csproj --configuration Release --no-restore
dotnet test tests/AttentionGuardian.Application.Tests/AttentionGuardian.Application.Tests.csproj --configuration Release --no-restore
dotnet test tests/AttentionGuardian.Infrastructure.Tests/AttentionGuardian.Infrastructure.Tests.csproj --configuration Release --no-restore
dotnet test tests/AttentionGuardian.Desktop.Tests/AttentionGuardian.Desktop.Tests.csproj --configuration Release --no-restore
dotnet test AttentionGuardian.slnx --configuration Release --no-restore
dotnet build AttentionGuardian.slnx --configuration Release --no-restore
```

真实窗口至少检查：

- 首页无时间泄露；
- 有当前事件时开始前不显示名称；
- 顶部抽屉；
- 添加类型选择和两个表单；
- 小时与分钟分别展开；
- 折叠提醒；
- 气泡双击编辑、离开询问和具名删除确认；
- 普通拖拽与键盘重排；
- 孤立不可移动事件禁止移动；
- 连续组同排、组内重排和自动定位；
- 未来待办主动展开；
- 休息模板调时长与重复复制。

视觉检查后关闭测试应用，再重新构建，避免 Release 可执行文件被锁。

## 禁止误判

- 当前软件已经达到用户确认的 v0.1.0 发布标准，不得继续沿用“产品尚未达到 v0.1.0
  验收线”的旧结论。
- 当前尚未执行正式打包、签名、版本 Tag、Release 页面或对外发布；不得把“通过用户
  验收”误写成“已经发布”。
- 当前没有五分钟前 Windows 系统通知、最终验收诊断界面或完整真实窗口交互自动化；
  这些是后续 Roadmap 的候选项，不再自动阻塞 v0.1.0。
- 当日待办完成状态已由 schema v3 实现；不得恢复整表物理删除。
- 不得恢复时间泄露、首页直接入口或不可移动冲突拒绝保存。
- 不得让 Desktop 直接访问 SQL 或计算排程。

## 给下一对话的建议指令

> 请先完整阅读 AGENTS.md、HANDOFF.md，并重点完整阅读 DESIGN.md，再阅读 PRODUCT.md、
> ARCHITECTURE.md、PRODUCT_DECISIONS.md 和 PROJECT_DEVELOPMENT.md。teaching.md
> 是只追加的历史教学日志，不需要完整通读；只读开头规则、检索本任务关键词和最近
> 相关章节，之后只能在末尾添加内容，不能修改或删除历史。检查实际目录与 Git 状态。
> 用户已经确认当前产品与交互达到 v0.1.0 发布基线。不要继续修改 UI、`final.svg`
> 或启动动画。先完成文档盘点；随后必须修复已完成当日待办被物理移除的问题，使其
> 标记完成并保留历史。区分“已达到用户确认的产品基线”和“代码满足全部数据标准”及“已经
> 完成打包/Tag/对外发布”；后者尚未获授权。把历史延期项安排到合理后续版本，不得
> 擅自把它们重新变成 v0.1.0 阻塞项。完成后向用户汇报文档差异，不提交、不推送、
> 不创建 Tag 或 Release。

## 2026-07-27 UI 精修状态补充

- 本轮只修改 Desktop 视觉、布局和动效，没有改变任何排程或持久化规则。
- 首页冲突提醒已改为低层级窄幅 frosted 提醒；折叠与展开内容职责保持不变。
- 添加页与管理页的数字时长已统一改用连续胶囊式 `GlassNumberStepper`，中间数值
  可直接编辑；旧的上下箭头 `NumericUpDown` 不应恢复。
- 当日开始时间/持续时间与未来待办年/月/日已经完成基线及紧凑间距校正。
- `GlassPicker` 浮层和 `GlassNumberStepper` 已继续调整半透明填充、边缘高光和短
  阴影。Windows `ExperimentalAcrylicBorder` 在本机曾被系统补偿成银灰实心感，因此
  不得只检查 XAML 中是否写了 Acrylic；必须以当前真实窗口合成结果判断是否符合
  `DESIGN.md`。
- 管理页双击展开/收起使用 240ms smoothstep ease-in-out；编辑内容淡入淡出，下方
  气泡随高度连续移动，减少动态效果时直接切换。
- 历史真实 Windows 隔离验收覆盖过按钮步进与直接输入、时间/日期布局、选择浮层、
  冲突提醒、气泡展开中间态和收起保存；这些是历史证据，不替代对当前构建的复查。
- 最新一轮把全局黑色玻璃从近实心调整到与红色语义玻璃相近的不透明度层级，并再次
  收紧开始时间的小时—冒号—分钟对称列。最新 Release 构建成功，但最终真实窗口
  复查被用户按 Esc 中止，因此这两项必须作为下一位 Agent 的首要视觉验收项目。
- 文字输入光标已按 44px 控件高度垂直居中；全部 `GlassPicker` 的触发按钮铺满整块
  玻璃命中面，Picker 与 Stepper 的短横高光已替换为宽幅渐变镜面高光。
- ambient 背景已改为 Skia GPU runtime shader：未开始时较快且对比鲜明，开始时
  短促加速后进入低对比慢速专注态，任务切换/结束也触发一次脉冲；窗口隐藏或最小化
  时停止刷新，减少动态效果时冻结。
- 开始入口增加微发光实体边缘，外围圆环扩大、分散并降低透明度。隔离真实窗口已
  验收文字光标、Picker 全面命中、高光、开始入口和开始前/转换中/稳定专注三种背景。
- 启动开场现为约 5.6 秒：黑场光点先独立停留约 1 秒，随后中心流体扩散；`Guardian`
  直接读取不可修改的 `docs/design/final.svg`。光点沿终稿已有 contour 描绘空心
  Stroke Outline，全部轮廓完成后才转为实体，并始终保留克制的 ambient glow。
  末段交叉淡化到常驻 ambient shader；最小化时暂停，减少动态效果时跳过。
- 用户已确认除启动字标外的现有内容达到 v0.1.0 发布标准。字标已按最终参考改为倾斜
  大 `G`、下行回环长尾、紧凑小写、`d` 高杆和右侧长收笔；后续不得借字标调整顺带
  改动光点、流体、时间轴、交接或其他 UI。
- 2026-07-27 最新状态：旧 29 段中心线和 40 步一笔书写逻辑已经废弃。
  `LaunchMotionSurface` 只读取 `final.svg` 的单一填充 path，源文件 SHA-256 被测试
  固定，禁止调整路径。Desktop 29 项、完整 152 项 Debug 测试通过；隔离真实窗口
  确认开场能够正常结束并进入主界面。
## 2026-07-27 首页提醒统一与规划入口修复

- 到期未来待办提醒已改为逐项“规划”，直接打开 `AddScheduled` 并预填原未来待办名称。`MainViewModel` 保存时调用既有 `PlanUnscheduledTodoAsync`，成功写入当日排程后才标记源未来待办；未保存返回不改变源数据。
- 到期待办提醒和不可移动冲突提醒均移除箭头，整框双击展开/收起。四类首页提醒表面统一为 430px 居中窄幅，保留跨日提醒的关闭按钮与普通顺延提示的三秒自动收回。
- 当前任务标题的行高调整为 60px，并增加 7px 下方留白，修复拉丁字母下行部裁切。
- Desktop 测试现为 29 项，完整 Solution 为 152 项；Release 测试、格式检查和 Release 构建均通过，构建为 0 警告、0 错误。
- 真实窗口确认新版 XAML 和启动交接正常加载。当前正式数据中已无截图里的到期/当前测试事项，因此没有为视觉截图向正式数据库重新写入伪造数据。
## 2026-07-27 Guardian 平滑候选稿（尚未获批、禁止接入）

- 用户要求继续精修字标，但明确要求本轮不要应用到软件。
- 候选文件为 `docs/design/guardian-wordmark-smoothing-candidate.svg`，预览为 `guardian-wordmark-smoothing-candidate-preview.png`。
- 当前软件仍精确嵌入 `docs/design/guardian-wordmark.svg`；不要在用户确认前替换它，也不要修改 `LaunchMotionSurface` 的 40 步笔序。
- 候选稿重点修复 `G` 下方回笔短段重叠，并把可见直线改为 cubic Bézier；整体 viewBox、字宽、字高和字母位置保持现状。
## 2026-07-27 Guardian 严格连笔校准候选（仍未接入）

- 最新候选继续位于 `docs/design/guardian-wordmark-smoothing-candidate.svg`，预览文件名不变。
- 小写连接路径已按原参考中心线合并，候选主组现为 12 条 path；所有可见轨迹仍为 cubic Bézier，描边宽度不变。
- `r→d` 的最终候选连接为 `M 823 465 C 829 467, 836 472, 842 476`，它是对参考图短对角过渡的保守补线。

## 2026-07-27 final.svg 终稿接入（当前有效状态）

- 上述 `guardian-wordmark-smoothing-candidate.svg` 与旧 `guardian-wordmark.svg`
  均已被 `final.svg` 终稿取代，不得再接入启动动画。
- `final.svg` 是只读品牌资产。允许修改的只有 Skia 绘制颜色、描边宽度、发光、
  时间映射和画布变换；禁止修改、简化或重排 SVG 路径。
- 当前字标阶段为：G 的完整 C 形双轮廓 → G 连接与下方回环 → 后续字母 →
  完整空心轮廓 → 实体填充 → 克制但可感知的呼吸微闪
  与停留 → 交接主界面；总时长为 7.2 秒。
- 不得恢复“按离上一片段最近”排序。当前实现用固定 contour 参数区间保证 C 完成
  以前交叉结构绝不出现，并用测试约束首阶段最大衔接距离与 100% 轮廓覆盖。
- 字标按真实可见几何边界在窗口中双向居中；开场期间窗口三键隐藏且不可点击，
  进入专注界面后以 520ms 淡入。
- Desktop csproj 只嵌入 `final.svg`。终稿 SHA-256 固定为
  `7140DE6A319D7AE59599E16E28B78AC1586BC4045190137692D0C0A9042ABE72`，
  禁止修改路径、字形或文件内容。

## 2026-07-27：v0.1.0 本地正式打包结果

- Release 默认隐藏“验收检查”板块；开发诊断开关为
  `ATTENTION_GUARDIAN_ENABLE_ACCEPTANCE=1`。
- Windows x64 公开洁净安装器：
  `artifacts/v0.1.0-public-clean/AttentionGuardian-0.1.0-win-x64-setup.exe`
- 大小：185,138,855 字节（176.56 MiB）。
- SHA-256：
  `AB8E7D6BBD5CCD894C48B576C970276794D894CC1AF056EBD6C2C575DE0140F7`
- 当前机器安装成功；产品版本为 `0.1.0`，文件版本为 `0.1.0.0`，启动和卸载注册项
  检查通过。
- 158 项 Release 测试通过，格式检查通过，Release 构建 0 警告、0 错误。
- 安装器包含 Windows App Runtime 2.3.1 x64 官方安装器，但项目安装器自身未签名。
- 尚未完成干净 Windows 虚拟机矩阵、Git 提交、Tag、GitHub Release 或对外上传。
- Release 抽屉使用真正的两等分网格，不得把隐藏的验收第三列留在布局中；已在真实
  安装窗口确认两个入口均匀填满 360px 胶囊。
- 应用、安装器、快捷方式和卸载入口使用
  `src/AttentionGuardian.Desktop/Assets/Attention_Guardian_icon_high.ico`。
- 发布机器的两张业务表已清空；安装后复查均为 0 条。仓库和最终 publish 目录扫描
  不含数据库、日志、Windows 用户名或本机源码绝对路径。
