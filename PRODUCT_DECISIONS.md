# 产品决策记录

本文件记录已经确认的重要产品决策及理由。新决策采用追加方式记录，避免仅保留最终结论而丢失背景。

## PD-001：英文项目名

- 状态：已确认
- 日期：2026-07-25
- 决策：英文项目名使用 **Attention Guardian**，代码和仓库标识使用 `AttentionGuardian`。
- 理由：名称直接表达“替用户守护注意力”的产品价值。
- 影响：旧交接文档中的 `HostedTime` 仅作为历史信息，不再用于新工程。

## PD-002：第一目标平台

- 状态：已确认
- 决策：第一版优先支持 Windows。
- 理由：先完成一个真实、可靠的平台闭环，降低通知、打包和恢复验证复杂度。
- 影响：核心层仍保持平台无关，不承诺第一版同时交付其他平台。

## PD-003：技术路线

- 状态：已确认
- 决策：C#、.NET 10、Avalonia UI、MVVM、SQLite。
- 理由：适合本地桌面应用、分层测试和未来跨平台扩展。
- 影响：第一版不引入 Python、Node.js、Docker或云数据库。

## PD-004：主界面隐藏未来详情

- 状态：已确认
- 决策：交接阶段前不持续展示下一固定事项的完整内容。
- 理由：产品目标是减少未来事项对当前注意力的占用，而不是强化反复检查。
- 影响：紧急查看未来事项需要有意触发，例如长按或二次确认。

## PD-005：第一项可验证交付

- 状态：已确认
- 决策：先交付可独立测试的 `handoffTime` 和 `safeUntil`，而不是完整 UI。
- 理由：它是产品承诺的计算基础，也是最适合先验证的风险点。
- 影响：工程初始化后优先完成 Core 和测试项目。

## PD-006：开源许可证

- 状态：已确认
- 决策：使用 GPL-3.0。
- 理由：希望公开发布的衍生版本继续保持开源，同时保留作者收费和服务商业化的可能。
- 影响：公开仓库前必须加入标准完整许可证正文；未来如考虑双许可证，需要先处理外部贡献授权问题。

## PD-007：第一版 AI 范围

- 状态：已确认
- 决策：第一版不实现 AI，也不要求 Python 环境。
- 理由：AI 不是验证核心产品价值的必要条件。
- 影响：不创建空 AI 模块，不绑定供应商，不提交 API 密钥。

## PD-008：统一时间策略

- 状态：已确认
- 日期：2026-07-25
- 决策：Core 使用 `DateTimeOffset` 表达真实时刻、`TimeSpan` 表达持续时间；本地时间与时区解析留在应用边界，系统时钟通过 `TimeProvider` 提供。
- 理由：避免未指定时区的本地时间产生歧义，同时保持核心计算纯净且可测试。
- 影响：无效夏令时时间必须拒绝，重复时间必须明确选择偏移量；SQLite 后续统一存储 UTC ISO 8601 时刻和整数秒时长。

## PD-009：最小会话持久化

- 状态：已确认
- 日期：2026-07-25
- 决策：Application 定义会话仓库接口，Infrastructure 使用 SQLite 保存唯一当前会话；启动时重新计算状态而不是恢复旧倒计时。
- 理由：先验证关闭重开后的可靠恢复，同时保持业务用例不依赖具体数据库。
- 影响：当前只保存一个会话；数据库位于用户本地应用数据目录，未来增加任务和事件表前必须设计显式迁移。

## PD-010：桌面页面职责分离

- 状态：已确认
- 日期：2026-07-25
- 决策：应用打开后首先显示当前状态；添加事件和事件管理使用独立界面。事件管理只允许按时间查看和删除，不承担添加功能。
- 理由：用户打开 Attention Guardian 的首要目的，是确认此刻是否有事项被托管，而不是每次都重新录入事件。
- 影响：无当前事件时首页显示“添加事件”；有当前事件时显示托管状态并保留“添加事件”入口；只有主动点击后才显示添加表单。

## PD-011：待办时间类型与首页匹配

- 状态：历史决策，输入方式已由 PD-014 与 PD-015 细化
- 日期：2026-07-25
- 决策：不选日期但设置开始和截止时间的输入视为当日定时待办；选择日期但不设置时间段的输入视为指定日期未定时待办。首页当前事件由当前日期时间表中覆盖当前时间的定时待办决定。
- 理由：定时待办负责回答“现在应该执行什么”，未定时待办只说明当天仍有事项，不能被程序武断安排到具体时刻。
- 影响：当天存在未定时待办时，首页顶部显示带感叹号的小型红色提示“有当日未设置时间待办”；未定时待办不参与当前事件匹配。

## PD-012：自动顺延、强制事件与跨日

- 状态：历史决策，强制碰撞处理已由 PD-017 替代
- 日期：2026-07-25
- 决策：新增事件需要顺延时间表时，若当日没有强制事件可直接顺延；若存在强制事件，必须先完整试算，确认不会占用强制时间后才一次性写入。强制事件冲突不自动决定优先级。
- 理由：普通事件可以灵活移动，但强制事件代表不可由系统覆盖的用户约束；先试算再写入可以避免产生半完成时间表。
- 影响：顺延保持事件持续时间不变并允许跨日；跨日时托管页显示可关闭的柔和黄色提示“有事件被顺延至明日”。若试算会撞上强制事件，则不执行顺延并提示冲突。

## PD-013：待办区间边界与排程试算结果

- 状态：区间决策继续有效；冲突结果范围已由 PD-017 收窄
- 日期：2026-07-26
- 决策：定时待办使用左闭右开区间 `[开始, 结束)`；首尾相接不算重叠。自动顺延先生成不可变的完整内存结果，遇到强制事件碰撞时只返回冲突，不返回可供部分写入的计划。
- 理由：首页在事件开始时应立即匹配它，而在截止时刻应允许下一事件接续；把失败结果与可提交计划明确分开，可以从类型层面降低半完成写入风险。
- 影响：定时待办持续时间必须大于零；普通事件顺延保持持续时间不变；跨日由开始日期实际变化判定。SQLite 接入时仍必须用事务提交成功结果。

## PD-014：两类待办使用独立数据库

- 状态：已确认，持久化阶段 A 已实现
- 日期：2026-07-26
- 决策：“当日待办”是所有有明确时间安排、需要排程的事件分类，允许跨零点；“未来待办”（领域类型 `UnscheduledTodo`）只有日期，不参与排程。两类数据分别存入两个独立数据库。
- 理由：有时间事件需要排序、冲突检测和事务式批量更新；只有日期的事项只需保存和读取。分开存储可以让两类数据的职责和失败边界保持清楚。
- 影响：正式文件名为 `attention-guardian-scheduled.db` 与 `attention-guardian-future.db`，各自维护 schema 版本并从空库迁移到 v1。当日待办整组替换使用单事务；未来待办不执行时间冲突检测。现有单会话数据库仍是过渡实现，默认路径接线、备份与恢复留待后续阶段。

## PD-015：当日待办和待办事项的输入方式

- 状态：已确认并实现
- 日期：2026-07-26
- 决策：添加当日待办时只让用户选择开始时间和持续时间；未修改开始时间时默认从现在开始，结束时间由系统自动计算。添加未来待办时支持日历选日和“一天后、两天后、三天后”等相对日期选项。
- 理由：用户应表达“从什么时候开始、需要多久”，不必手工维护容易矛盾的开始与结束时间。相对日期选项让模糊但常见的表达可以落为确定日期。
- 影响：排程内部仍保存确定的开始与结束时刻，但普通规划结果和管理列表不直接暴露它们。只有打开软件时检查日期已到但未规划时间的未来待办。

## PD-016：管理页渐进展示与最终可交互验收

- 状态：管理页已实现；诊断界面移至 v0.1.0 后
- 日期：2026-07-26
- 决策：事件管理页默认只显示当日待办，待办事项隐藏在页面下方的主动展开入口后。全部主要功能完成后，再提供覆盖特殊排程情况的可操作样例和本地交互式诊断界面。
- 理由：默认隐藏未来日期事项可以降低认知负担；最终诊断界面让用户亲自验证加算和排序过程，而不是只能相信自动测试或开发者说明。
- 影响：诊断输出必须使用简短日常语言，支持重跑、逐步查看原计划与试算结果，并明确冲突时没有修改数据。该入口不得绕过校验或泄露真实用户数据。

## PD-017：普通事件自动越过强制事件

- 状态：已确认并实现于 Core
- 日期：2026-07-26
- 决策：普通事件的原时段或顺延结果撞上强制事件时，不提醒冲突，而是保持持续时间不变并整体移动到强制事件结束后，再继续顺延。连续强制事件依次越过。只有强制事件与强制事件重叠时才返回冲突提醒。
- 理由：强制事件只需要作为不可移动的时间挡板，不必阻止普通时间表自动整理。普通事件能够自动绕开它时，额外提醒会增加不必要的操作。
- 影响：新加入的普通事件直接撞上强制事件时也自动后移；越过强制事件造成跨日时仍设置跨日标识。最终诊断界面应解释“已移到强制事件之后”，不能把普通与强制碰撞显示成失败。

## PD-018：多待办 Application 边界

- 状态：已确认并实现于 Application
- 日期：2026-07-26
- 决策：有时间待办与只有日期的待办事项使用两个独立仓库接口。添加有时间待办时，Application 先执行完整 Core 试算，成功后把整张计划交给仓库替换；冲突时不调用保存。打开状态通过一个明确用例同时取得当前事件和当天未规划事项标识。
- 理由：先定义应用需要什么能力，再实现 SQLite，可以避免数据库结构反过来控制产品流程；整张计划提交合同也为未来事务写入提供清晰边界。
- 影响：Infrastructure 的当日待办数据库实现必须保证 `ReplaceAllAsync` 原子完成。未来待办数据库只按确定日期存取。打开状态用例应在软件打开时调用一次，不作为持续轮询方法。

## PD-019：添加类型选择直接决定数据库

- 状态：已确认并实现
- 日期：2026-07-26
- 决策：用户点击“添加事件”后，第一步必须选择“当日待办”或“未来待办”。选择结果直接决定后续表单、校验、用例和数据库；当日待办只管理当日待办数据库，未来待办只管理未来待办数据库。
- 理由：两个数据库来自两种不同的用户输入意图，而不是后台存储优化。让用户先明确类型，可以避免一个混合表单依靠空字段猜测数据归属。
- 影响：“未来待办”是用户可见名称，对应领域中的 `UnscheduledTodo` 和已有文档中的“待办事项”。添加分支不得同时访问两个数据库；管理页只有用户主动展开未来待办时才读取未来待办数据库。

## PD-020：到期未来待办的规划与删除闭环

- 状态：已确认并完整接入 Desktop/Application/Infrastructure
- 日期：2026-07-26
- 决策：软件打开时发现日期已到但未规划时间的未来待办，红色提示区域按事项显示名称，并提供“规划”和“删除”。规划会带着自动填写的名称进入当日待办页面；只有成功保存当日待办后，原未来待办才退出活动列表。未保存返回时继续提醒。删除必须显示包含事件名称的二次确认弹窗。
- 理由：提醒本身必须能直接通向解决动作，同时不能因为用户只是查看页面或误触返回就丢失原事项。删除显示名称可以降低删错事件的风险。
- 影响：未来待办采用 `active`、`planned`、`deleted` 状态而非物理删除，打开状态返回截止今天仍为 `active` 的具体事项集合。规划后的当日待办沿用源事项标识，使第二步失败后的重试只补做状态更新；删除只有明确确认后才写入，并只作用于所选事项。

## PD-021：专注界面隐藏时间并由用户主动开始

- 状态：已确认并实现
- 日期：2026-07-26
- 决策：专注界面完全隐藏开始、结束、安全截止、剩余时间、倒计时和进度。即使存在当前事件，用户点击“开始”前不显示名称；点击后名称淡入并出现克制背景色流。
- 理由：产品托管的是用户对时间的持续监控。显示结束时间会诱发边际收益递减式的反复检查。
- 影响：旧 Desktop 的 `SafeUntilText`、`RemainingText` 和事件开始时间展示必须退出新 UI。Core 仍可计算时间供通知和排程使用，但 ViewModel 不向专注界面暴露。

## PD-022：顶部下拉抽屉承载低频入口

- 状态：已确认并实现
- 日期：2026-07-26
- 决策：添加事件和事件管理不直接显示在首页，通过顶部细把手向下拖动后出现；点击入口或抽屉外部收回。
- 理由：首页只承担开始专注、当前任务和必要提醒，低频维护操作由用户主动召回。
- 影响：必须提供键盘等价入口和明确焦点，不得让触摸/鼠标手势成为唯一访问方式。

## PD-023：不可移动事件冲突是可保存、待管理状态

- 状态：已确认并实现
- 日期：2026-07-26
- 决策：两个或更多不可移动事件重叠时保存完整计划并保留重叠，首页显示可展开冲突提醒。进入管理页后冲突组同一行等分显示，用户拖拽决定先后，新时间从组内最早原始开始时刻连续计算并永久保存。
- 理由：阻断保存会打断输入，而且系统不应擅自判断哪个不可移动事件优先。让冲突可见并集中处理更符合用户意图。
- 影响：现有 Core 的“强制冲突即失败且不写入”规则必须重构。冲突状态必须显式返回和持久化，不得伪装成部分失败。

## PD-024：气泡排序、统一保存与休息模板

- 状态：已确认并实现
- 日期：2026-07-26
- 决策：管理页当日待办使用长条气泡，顺序即优先级。普通事件可拖拽，连续不可移动组内可拖拽；双击展开编辑，再次双击收起时统一保存。删除均需确认并触发向前补位。右侧提供固定名称、默认 20 分钟、可重复拖入的休息模板。
- 理由：用户通过直接操作顺序表达优先级，不需要编辑不可见的开始时间；批量保存一次编辑避免小时、分钟选择过程中反复重排。
- 影响：Application 需要事务式重排、编辑、删除、休息插入和冲突组查询能力；Desktop 必须提供键盘等价排序。

## PD-025：五分钟前只提醒无空档的下一非休息事件

- 状态：Core/Application 资格规则已实现；Windows 系统通知移至 v0.1.x
- 日期：2026-07-26
- 决策：当前事件持续不少于五分钟、下一事件与其首尾相接且下一事件不是休息时，在结束前五分钟显示当前与下一事件名称。窗口最小化仍提醒，应用完全退出不提醒；休息结束后若紧接任务则正常提醒。
- 理由：五分钟属于必要切换缓冲，不是持续暴露未来详情。空档和休息前无需制造准备压力。
- 影响：通知调度依赖当前事件、下一事件、连续性和应用生命周期，不能只按固定倒计时实现。

## PD-026：专注主页采用连续深空画布与鼠标牵引色流

- 状态：已被 PD-027 替代；第一版曾实现，现已移除
- 日期：2026-07-26
- 决策：专注主页使用从冷银灰过渡到石墨蓝的整幅沉浸式画布。用户开始专注后，
  冰蓝色流以带延迟的方式追踪鼠标；顶部抽屉展开为中央细长玻璃导航，而不是大型
  面板或卡片按钮。
- 理由：连续背景和低存在感导航能让当前任务成为唯一视觉中心，同时保留用户对
  鼠标操作的柔和感知，不把应用做成普通表单或卡片式 Todo List。
- 影响：鼠标动效只属于 Desktop 视觉层，不参与排程、不拦截输入；未开始专注时
  不显示活动流线。添加、管理入口仍必须通过顶部下拉手势或键盘等价入口召回。

## PD-027：采用平面工具型层级，取消持续动态背景

- 状态：已确认并完成第一版 Desktop 实现
- 日期：2026-07-26
- 决策：借鉴 Super Productivity 的平面表面、清晰分隔、当前项强调和拖拽时才
  抬升的设计原则，同时维持 Attention Guardian 更低的信息密度。专注主页使用
  近黑冷灰静态背景；不再使用视频、射线、鼠标追踪色流或持续装饰动画。
- 理由：持续背景即使技术上平滑，仍会与“替用户减少心理监控”的目标争夺注意力。
  工具型层级能让界面显得成熟，并让任务名称成为唯一视觉中心。
- 影响：开始后的状态变化由短淡入、轻位移和状态标签表达。业务规则、页面职责、
  顶部下拉入口、隐藏时间与数据库边界全部保持不变。

## PD-028：开始专注后显示低对比度水波色场

- 状态：已确认并完成第一版 Desktop 实现
- 日期：2026-07-26
- 决策：保留 PD-027 的平面工具型 UI，但修正“完全取消持续背景”的部分。只有
  用户点击开始后，背景才渐显冰蓝、薄荷绿、淡紫和青色的大面积柔化色雾；各层
  以不同长周期缓慢漂移、扩散和交融，不使用视频、射线、硬边或鼠标追踪。
- 理由：参考图的质感来自无边界的柔和彩色光场，而非可识别的水波线条。它能在
  不显示时间的前提下持续表达“专注已经开始”，又比视频更容易无缝循环和适配窗口。
- 影响：色场属于 Desktop 纯视觉层，不读取或修改排程数据，也不拦截指针。离开
  专注主页时停止渲染；减少动态模式最终应冻结为静态色场。

## PD-029：使用应用绘制顶栏并保留 Windows 原生窗口行为

- 状态：已被 PD-030 替代；第一版曾实现，现已移除
- 日期：2026-07-26
- 决策：将客户端内容延伸进 46px 标题栏区域，以近黑表面和细分隔线形成连续应用
  外壳；标题显示“托管时间”。右侧继续使用 Windows 原生最小化、最大化和关闭按钮。
- 理由：完全无边框并自行重造窗口按钮会额外承担八方向缩放、DPI、最大化安全边距、
  Snap Layout 和可访问性责任。扩展原生标题栏能获得参考图的视觉，同时保留成熟的
  Windows 行为。
- 影响：顶部下拉把手下移到标题栏下方，拖拽触发区域同步调整；添加、管理命令和
  页面内容不变。

## PD-030：取消贯穿式标题栏，使用右上角圆形窗口控件

- 状态：已确认并完成第一版 Desktop 实现
- 日期：2026-07-26
- 决策：窗口改用 `WindowDecorations=None`，内容连续延伸到最上方。右上角只显示
  三个 36px 圆形按钮，分别负责最小化、最大化/还原和关闭。
- 理由：用户需要的是参考图中的悬浮窗口控件，而不是把系统标题栏换一种颜色。
  无贯穿顶栏能保持首页是一整张专注画布。
- 影响：Desktop 自行补充顶部空白区域拖动、八方向边缘缩放、最大化圆角切换和
  可访问名称。顶部中央仍保留抽屉手势区域；业务导航、排程与数据库不变。

## PD-031：精修顶部抽屉与当日待办输入

- 状态：已确认并完成第一版 Desktop 实现
- 日期：2026-07-26
- 决策：顶部抽屉使用 280ms 位移加淡入/淡出，两个入口严格等分并居中。当日待办
  不再提供日期选择；保存时使用操作发生的本地当天，用户只选择开始小时、分钟和
  持续时长。时间类下拉统一为深色圆角浮层与纵向选项。
- 理由：“当日待办”已经表达当天，重复选择日期增加认知和视觉负担。紧凑时间组
  能避免冒号两侧出现无意义空白；抽屉动画则让顶部拖拽有真实因果反馈。
- 影响：未来待办仍保留日期输入。开始时间默认值在每次进入当日待办表单时刷新为
  当前本地时间；Core 排程与双数据库边界不变。

## PD-032：所有展开选择统一使用磨玻璃 GlassPicker

- 状态：已确认并完成第一版 Desktop 实现
- 日期：2026-07-26
- 决策：移除 Desktop 页面中的系统 ComboBox 与 DatePicker，统一使用自定义
  `GlassPicker`。浮层由 Acrylic 材质、圆角边缘、纵向 ListBox 和轻触关闭组成。
  未来待办日期拆为年、月、日三个独立选择器。
- 理由：修改系统 ComboBox 外层颜色无法替换其 Popup 结构和系统 DatePicker 的
  三列滚轮外观。独立控件才能让触发器、浮层、选项行、透明度和可访问名称完全一致。
- 影响：未来待办新增和管理编辑都以三个整数维护日期，并在保存前构造确定
  `DateOnly`；切换年月会自动收紧日期上限。数据模型和数据库仍保存一个确定日期。

## PD-033：五分钟前提醒资格先实现为纯规则

- 状态：Core/Application 算法已实现，Windows 通知尚未接入
- 日期：2026-07-26
- 决策：提醒窗口使用 `[当前事件结束前五分钟, 当前事件结束)`。当前事件必须持续
  不少于五分钟，下一事件必须与其首尾相接且名称不是“休息”。Application 在进程内
  按当前事件标识去重，同一事件只产生一次待发送提醒。
- 理由：先把“应不应该提醒”与“Windows 怎样显示通知”分开，可以用纯单元测试覆盖
  时间边界、空档和休息规则，也避免 Desktop 计时器复制业务判断。
- 影响：Infrastructure/Desktop 后续只负责轮询、发送和平台降级。进程退出后不后台
  常驻。

## PD-034：用持久化录入优先序解决当前不可移动冲突

- 状态：已确认并完成 Core/Application/Infrastructure 实现
- 日期：2026-07-26
- 决策：每个当日待办保存非负 `CurrentSelectionPriority`。新创建的当日待办取当前
  最大值加一；多个事件同时覆盖当前时刻时，优先选择数值最大的事件。相同数值只用于
  兼容旧数据，并继续按开始、结束和标识稳定排序。
- 理由：用户要求不可移动事件互相重叠时仍保存，且新输入事件成为当前事件。仅依赖
  列表顺序、开始时间或内存创建顺序无法跨重启恢复这一事实。
- 影响：当日 SQLite schema 升至 v2，v1 既有事件迁移后优先序为 0；以后新增、规划
  未来待办和插入休息都会分配新序号。移动、编辑和自动顺延保留序号。该字段不改变
  排程顺序，也不替用户解决冲突组的最终先后。打开状态会从完整时间表重新检测所有
  不可移动重叠，因此关闭软件再打开后冲突状态不会消失。

## PD-035：拖拽在移动超过阈值后启动，并提供明确队尾落点

- 状态：已完成 Desktop 实现与隔离真实窗口验收
- 日期：2026-07-26
- 决策：任务和休息模板使用独立非按钮拖拽把手。按下后捕获指针，移动至少 6px 才
  启动系统拖放；普通点击不触发拖拽。气泡和队尾区域都可接受任务移动或休息复制，
  Drop 根据 Application 是否成功明确返回 Move、Copy 或 None。
- 理由：按钮自身的点击与指针捕获会和按下即启动的系统拖放竞争，导致真实鼠标操作
  无效。移动阈值符合桌面拖拽习惯，明确队尾区域也让最后一个位置真正可达。
- 影响：键盘仍使用 `Alt+↑/↓` 重排；休息模板另保留“加入队尾”按钮。视觉基线、
  Core 重排规则和 SQLite 事务边界不变。

## PD-036：未来待办首批相对日期使用“一天后”和“两天后”快捷按钮

- 状态：已完成 Desktop 实现与隔离真实窗口验收
- 日期：2026-07-27
- 决策：未来待办添加页在具体年/月/日之前提供等宽的“一天后”和“两天后”按钮。
  默认选择一天后；点击按钮会同步具体日期，手动修改年、月或日会取消相对日期选中。
  保存请求只提交 `DaysFromToday` 或 `ScheduledDate` 其中一种。
- 理由：一天和两天是最常用、最直觉的近期表达；同时显示换算后的确定日期，让用户
  能看见系统最终会保存哪一天，也避免两套输入产生互相矛盾的值。
- 影响：Application 既有相对日期合同不变，Desktop 只负责表达用户选择。后续如需
  增加“三天后”等选项，可沿用同一命令和互斥状态模式，不必修改数据库 schema。

## PD-037：完成任务退出活动排程，冲突提醒只覆盖未结束事件

- 状态：活动查询边界继续有效；物理清理部分已被 PD-044 替代
- 日期：2026-07-27
- 决策：当日待办使用既有 `[开始, 结束)` 边界；任务到达结束时刻即视为完成。用户
  进入管理页时，结束时刻等于或早于当前时刻的任务退出活动排程。第一版不显示
  已完成历史列表。不可移动冲突提醒只检测仍未结束的任务。
- 理由：管理页用于安排现在和未来，已完成任务继续占位会干扰拖拽索引、冲突组和
  用户判断；历史能力尚未被产品确认，不应让活动数据库兼任隐藏历史库。
- 影响：`ScheduleManagementService` 注入 `TimeProvider`。打开状态返回的冲突由
  Desktop 接入折叠提醒，展开后显示冲突双方名称；已完成的旧冲突不会继续报警。
  旧实现通过整表替换物理清理完成项，现须按 PD-044 改为状态标记。

## PD-038：流体背景用状态转换表达专注，而不是持续强化刺激

- 状态：已完成 Desktop 实现与隔离真实窗口验收
- 日期：2026-07-27
- 决策：未开始专注时，蓝青流体背景使用较高对比和较快流速；点击开始时先短促加速，
  再以约 1–1.5 秒的减速尾段自然过渡到低对比、低流速的稳定专注态。任务切换或
  结束时重复一次短促流动脉冲。
  背景由 Skia GPU runtime shader 绘制，窗口隐藏或最小化时停止刷新。
- 理由：启动和切换需要可感知的环境反馈，但稳定专注期间不应持续争夺注意力。流体
  状态变化比静止高压焦点更尊重用户自主分配注意力，也能保持 ambient design 连续。
- 影响：本决策替代 PD-028 及旧 `DESIGN.md` 中“开始后持续更快、更丰富”的背景节奏；
  减少动态效果时保持冻结，shader 不可用时退化为静态渐变。排程、持久化和页面职责
  不变。

## PD-039：启动开场用手写品牌线条引导流体进入主界面

- 状态：已完成 Desktop 实现、shader 编译测试与真实 Windows 验收
- 日期：2026-07-27
- 决策：窗口第一次进入前台时播放约 5.6 秒开场。黑场中央光点先独立停留约 1 秒，
  流体随后扩散；`Guardian` 使用有序单线 SVG/Bezier 路径逐段绘制，不使用字体渐显
  或裁切模拟。末段与常驻 ambient shader 交叉淡化。
- 理由：开场需要建立“进入托管空间”的仪式感，但不能采用科技扫描或高压品牌广告。
  单次手写线条是品牌表达，流体扩散则与应用主体视觉语言连续。
- 影响：开场完全位于 Desktop，不参与 ViewModel、排程或数据加载。数据初始化与动画
  并行；最小化时暂停，减少动态效果时跳过。五段矢量笔画属于品牌资产，只在开场使用，
  不依赖系统手写字体，也不得扩张到普通 UI 字体。

## PD-040：Guardian 发布字标采用长尾书法结构

- 状态：已完成 Desktop 实现、路径测试与真实 Windows 慢速验收
- 日期：2026-07-27
- 决策：v0.1.0 启动字标固定采用大幅倾斜 `G`、向下回旋长尾、紧凑连写小写字母、
  明显 `d` 高杆和 `n` 后长收笔。继续使用单线 Bezier path drawing，不使用字体。
- 理由：该结构来自用户确认的发布参考图，比上一版平均高度的圆体连写更具明确品牌
  识别度，也保留了真实笔迹动画所需的单线拓扑。
- 影响：本次只替换品牌路径和线宽。光点、流体、5.6 秒时间轴、页面与其他 UI 被用户
  确认为 v0.1.0 发布标准，不随字标调整而改变。

## PD-041：启动字标改用不可变终稿轮廓描绘

- 状态：已完成 Desktop 实现与自动化验证
- 日期：2026-07-27
- 决策：废弃旧 `guardian-wordmark.svg` 单线书写资产，启动动画只读取
  `docs/design/final.svg`。该文件是不可修改的单一路径终稿；光点沿其既有 contour
  描绘空心 Stroke Outline，全部轮廓完成后再加入实体填充，ambient glow 始终保留。
- 理由：终稿已经用填充轮廓定义完整字形，不再需要人为推测中心线或模拟一笔书写。
  直接描绘终稿边界既能严格保持字形，又能让开场具有清楚、克制的光线引导。
- 影响：5.6 秒总时长、约 1 秒独立光点、流体扩散、交叉淡化、最小化暂停和减少动态
  效果降级保持不变。Desktop 测试固定终稿 SHA-256，任何路径或文件内容变化都会
  立即导致测试失败。

## PD-042：放缓终稿轮廓开场并延后窗口工具显现

- 状态：已完成 Desktop 实现与 Release 验证
- 日期：2026-07-27
- 决策：保持 `final.svg` 内容和路径完全不变，在运行时将原轮廓切分并映射为八个
  参考书写阶段；总时长由 5.6 秒延长至 7.2 秒。完整轮廓转为白色实体后增加一次
  不高于 ambient glow 的柔和微闪和停留，再交接主界面。开场期间隐藏窗口三键，
  交接完成后才淡入；字标按实际几何边界严格双向居中。
- 理由：轮廓顺序、完整字标的停留和窗口工具的出现都属于同一条注意力节奏。延长
  收尾并移除开场中的无关工具，可避免“刚完成就被赶走”的匆忙感，同时保留克制的
  品牌仪式感。
- 影响：仅改变 Desktop 的轮廓播放顺序、时间映射、几何定位与窗口工具可见性。
  数据初始化、流体配色、主界面、业务规则和 `final.svg` 均不改变；减少动态效果
  仍直接进入主界面。

## PD-043：G 的 C 形轮廓采用确定性连续笔序

- 状态：已完成 Desktop 实现、自动化与真实窗口关键帧验收
- 日期：2026-07-27
- 决策：移除轮廓碎片的最近邻排序。`G` 的第一阶段固定从右上方开始，连续完成完整
  C 形双侧轮廓；只有该阶段全部结束后，才显示交叉连接、下方回环和出口。其余字母
  按原 contour 参数与字母阶段稳定推进。终稿填充后的 breath glow 适度增强，但不
  超过清晰轮廓的主层级。
- 理由：最近邻只保证空间距离短，无法保证真实笔顺，容易在交叉结构附近跳到错误
  分支。固定轮廓参数区间能保护第一眼最受关注的 `G`，并消除“回头补线”的机械感。
- 影响：只改变 Desktop 的轮廓播放计划和填充后 glow 参数；`final.svg`、7.2 秒总
  时长、流体、布局、窗口工具交接与业务功能均不改变。

## PD-044：已完成当日待办保留为不可见历史

- 状态：已确认并实现
- 日期：2026-07-27
- 决策：当日待办到达结束时刻后退出活动排程和用户界面，但数据库记录不得物理删除；
  系统必须标记完成并保存完成时刻。自然完成与用户主动删除使用不同生命周期状态。
- 理由：用户不应被已完成事项增加认知负担，但历史记录是后续经用户授权的 API/AI
  分析和规划建议所需的数据基础。保留数据不等于 v0.1.0 已接入 AI。
- 影响：当日待办 schema 已升级到 v3；自然完成标记为 `completed`，用户删除标记为
  `deleted`，活动查询只返回 `active`。完成记录默认不在 v0.1.0 UI 中展示，也不
  参加排程和提醒。

## PD-045：首页提醒共享同一低层级玻璃规格

- 状态：已确认并实现
- 日期：2026-07-27
- 决策：到期未来待办、不可移动冲突、普通顺延和跨日顺延统一使用 430px
  `AlertGlass` 表面，共享不透明度、边缘、圆角、短阴影、字号与内边距。语义差异
  只通过小型色点、文字和必要的关闭操作表达。
- 理由：整块实心黄色跨日提醒会脱离 ambient 画布并抢夺注意力，也会让同一层级的
  四种提醒形成不同组件语言。
- 影响：跨日提醒不再使用 `#DDF5D47D` 实心背景；不得为新提醒复制独立材质参数。

## PD-046：五分钟前提醒接入 Windows App SDK

- 状态：实现与自动化规则验证完成；最小化实机矩阵待发布前复查
- 日期：2026-07-27
- 决策：Desktop 每五秒调用既有 `HandoffReminderService`，符合资格且进程内尚未
  提醒的事项通过 Windows App SDK 发送系统通知，同时显示当前与下一事项名称。
- 理由：只有资格算法而没有调度和发送端，用户实际不会看到任何 Windows 提醒。
- 影响：Desktop 目标框架限定为 Windows 10 19041 及以上；本地验收页提供
  “测试 Windows 提醒”，并明确反馈是否已交给 Windows 通知中心。

## PD-047：提供不读取真实数据的本地验收表单

- 状态：已确认并实现首批九类场景
- 日期：2026-07-27
- 决策：顶部抽屉增加“验收检查”，使用固定内存样例验证普通顺延、多个不可移动
  挡板、不可移动冲突、跨日、空档、首尾相接、跨零点、提醒资格和不提醒分支。
- 理由：自动测试适合防回归，但用户还需要在真实应用中看到每个场景的预期和实际结果。
- 影响：验收表单不读取或修改真实待办；后续可扩展逐步解释和原计划/结果对比。

## PD-048：Apple 平台采用 SwiftUI

- 状态：已确认
- 日期：2026-07-29
- 决策：Windows 客户端继续使用 Avalonia；macOS 与 iOS 客户端全部采用 SwiftUI。
- 理由：Apple 平台使用原生 UI 框架，能够更自然地接入系统交互、通知、辅助功能、
  签名和分发能力；Windows 已发布客户端不需要因扩展 Apple 平台而被重写。
- 影响：不再把当前 Avalonia 桌面项目作为 macOS 的长期交付路线。跨平台一致性由产品
  规则、数据契约和验收用例保障，而非共享 View/XAML。Apple 客户端的共享业务实现与
  数据同步/迁移方案必须另行作出可验证的架构决策，不能在 SwiftUI View 中复写排程规则。

## PD-049：Apple 平台重写 Swift Core，以跨平台规则规格保持一致

- 状态：已确认
- 日期：2026-07-29
- 决策：macOS 与 iOS 共同使用新的 `AttentionGuardianDomain` Swift Package，重写领域
  规则与应用用例；不让 SwiftUI 直接调用 C# Core，也不使用 MCP、常驻 .NET 服务或跨语言
  运行时桥接。Windows 保留 C# Core。
- 理由：iOS 需要可靠、离线、可签名发布的原生运行时；跨语言运行时会增加分发、崩溃恢复、
  调试、隐私和 SQLite 并发风险。规则一致性应由可执行合同保证，而不是绑定运行时。
- 影响：`docs/CROSS_PLATFORM_RULES.md` 成为两种 Core 的共同规格。Swift 实现开始前必须建立
  固定输入与期望输出的共享测试向量，并由 C# 与 Swift 双方读取验证。Apple Persistence、
  数据迁移与跨设备同步仍未授权实现。

## PD-050：Apple SwiftUI 采用 calm ambient Liquid Glass 设计语言

- 状态：已确认
- 日期：2026-07-29
- 决策：macOS/iOS 使用 Mesh Gradient、SwiftUI 原生 Material（初始为 `thinMaterial`）、
  四级 Depth、Capsule Button/Stepper、完整 Micro Interaction 与阻尼充分的 Spring
  Animation。原顶部抽屉改为底部隐藏抽屉，展开为 Floating Capsule Tab Bar；选择 Lens
  通过 `matchedGeometryEffect` 和液态形变在 Tab 间连续迁移。
- 理由：Apple 客户端应高度符合平台已有材质、运动和辅助功能体验，同时延续 Attention
  Guardian 克制、低信息密度、有实体感的 ambient 品牌，而不是复刻 Avalonia 控件。
- 影响：`DESIGN.md` 明确拆分 Apple SwiftUI 与 Windows Avalonia 两套语言。Apple 背景不用
  Windows GPU shader，改用同色调 Mesh Gradient；参考视频只确定选择 Lens 的运动结构，
  不照搬其中强烈绿蓝光斑和彩色像差。Windows v0.1.0 视觉基线不随此决策自动改变。

## PD-051：文档保留原路径，HANDOFF 只保存当前交接

- 状态：已确认并实施
- 日期：2026-07-29
- 决策：现有根文档保留原路径，不提前建立空的 `docs/apple`、`docs/windows` 或
  `docs/shared`。`HANDOFF.md` 可随阶段整体替换，只保存当前状态、唯一下一任务、禁止事项
  与验证入口，不再承担历史记录。
- 理由：稳定路径可以避免链接断裂和重复文档；短交接能让下一位 Agent 立即识别当前工作，
  而不被已经完成的 Windows 发布、旧 UI 迭代或过时任务误导。
- 影响：长期规则与当前优先级由 `AGENTS.md` 管理；决策历史只追加到
  `PRODUCT_DECISIONS.md`，实际操作历史只追加到 `PROJECT_DEVELOPMENT.md`，本地教学历史
  只追加到 `teaching.md`。当前 Apple 实施门槛是共享 JSON 测试向量及 C# 验证。

## PD-052：持久化后的休息允许切换为不可移动事件

- 状态：已确认
- 日期：2026-07-29
- 决策：休息模板每次拖入时仍以标题“休息”、`isMandatory = false` 创建；成为计划中的
  事件后，用户可以把它设为不可移动，也可以再改回普通事件。标题“休息”仍是保留名称，
  不允许改名。
- 理由：模板的默认属性与持久化事件的后续编辑能力是两件不同的事。默认普通可以保持快速
  插入体验，但用户仍需根据实际安排把某次休息固定下来。
- 影响：C# Core、Windows 管理页、用户指南、跨平台规格、共享向量与未来 Swift Domain 都
  必须允许该属性切换；此前“休息永远不是不可移动事件”的文字和测试结论由本决策取代。

## PD-053：管理页可选择显示并修改开始时间

- 状态：已确认
- 日期：2026-07-29
- 决策：管理页默认继续隐藏开始时间，但提供页面级按钮让用户选择是否在每个事件气泡末尾
  显示开始时间。双击展开气泡后可以修改开始时间。
- 开始时间冲突规则：若目标时刻的第一项是不可移动事件则拒绝；若是普通事件，确认弹窗让
  用户选择“被修改事件优先，原事件保持时长移到其后”或“只把原事件截短到新开始时刻”。
  第二种选择只截短第一项，后续事件继续整体重排；取消时零修改。
- 理由：默认隐藏维持低信息密度，但主动查看和编辑能力让管理页能处理真实计划偏差。冲突
  选择交给用户，避免系统擅自丢失普通事件时长。
- 影响：时间显示开关属于 presentation 状态；开始时间试算属于 Domain。C# 与 Swift 必须
  通过同一共享向量，UI 只负责收集选择、显示确认并提交完整操作。

## PD-054：Apple UI 以 iPhone 自适应为主并禁止绝对像素定位

- 状态：已确认并开始实施
- 日期：2026-07-30
- 决策：macOS 与 iOS 共享 `AttentionGuardianPresentation` SwiftUI 组件。macOS 开发用于
  验证同一套 iPhone 优先的自适应组件在宽窗口中的表现，不建立独立桌面 View。页面禁止
  `.position`、截图式 `.offset`、读取固定屏幕尺寸后摆放元素；使用安全区域、弹性容器、
  size class、Dynamic Type 与内容尺寸。可复用间距、圆角和最小命中区仍使用 SwiftUI point。
- 平台基线：macOS 14、iOS 17。Mesh Gradient 仅在 macOS 15/iOS 18 启用，其余使用同品牌
  静态渐变，信息层级不变。
- 理由：绝对位置会随 iPhone 尺寸、横竖屏、动态字体和 Mac 窗口缩放失效；共享的响应式布局
  才能让 macOS 原型真实服务于 iOS 交付。
- 影响：SwiftUI 页面必须通过紧凑宽度、辅助功能字号、Reduce Motion/Transparency 和键盘/
  触控路径审查；不得为了修一张 Mac 截图加入一次性坐标。

## PD-055：macOS 保留原生窗口壳与系统交通灯按钮

- 状态：已确认并实施
- 日期：2026-07-30
- 决策：macOS App 使用 SwiftUI `WindowGroup` 和系统标题栏，直接保留关闭、最小化、
  全屏/缩放按钮；不复制 Windows 无标题栏、自绘窗口控制和边缘缩放实现。
- 理由：Mac 版本的主要作用是承载并验证与 iOS 共享的 SwiftUI 产品界面。保留平台原生窗口
  行为能减少无关桌面外壳工作，也符合 macOS 用户预期和辅助功能。
- 影响：Apple Presentation 只负责窗口内容与共享导航；窗口交通灯、标题栏拖动、缩放和全屏
  由 AppKit/SwiftUI 系统 Scene 管理。

## PD-056：Mac 标题栏透明并让系统交通灯融入 ambient

- 状态：已确认并实施
- 日期：2026-07-30
- 决策：PD-055 所称“保留原生窗口壳”不代表保留白色标题栏材质。Mac 使用透明标题栏与
  full-size content，隐藏标题文字，让 ambient 背景延伸到顶边；红黄绿交通灯仍是系统控件。
- 理由：白色横栏会切断连续 ambient 空间并显得突兀；只透明化标题栏外观即可达到一体效果，
  无需复制系统窗口按钮。
- 影响：不得在共享 Presentation 中画交通灯；Mac App 壳只配置 `NSWindow` 外观和可拖动背景。

## PD-057：UI 优先完成真实功能并保持可局部替换

- 状态：已确认并实施
- 日期：2026-07-30
- 决策：后续 Apple UI 继续以 `DESIGN.md` 为默认设计原则，但页面、字段控件、表单状态、
  presentation draft 和 Application 调用必须分层。先完成可用功能闭环，不把当前视觉稿视为
  不可修改终稿。
- 理由：用户仍会持续调整颜色、层级、排列和交互。如果 View 同时计算业务规则或直接访问
  Repository，任何视觉修改都会牵动数据与时间逻辑。
- 影响：共享 View 通过 draft/closure 提交意图；App 组合层调用用例；Domain/Application
  继续保持唯一业务规则来源。后续视觉修改应优先替换 Token 或局部组件。
