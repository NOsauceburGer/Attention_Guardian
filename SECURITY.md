# 安全政策

## 支持范围

项目正在准备 v0.1.0，尚未对外发布受支持版本。首个 GitHub Release 完成后，本节
应列出实际支持范围；在此之前不得把本地发布候选描述为稳定版本。

## 报告漏洞

请不要通过公开 Issue 报告可能导致数据泄露、代码执行、权限绕过或其他安全影响的问题。

GitHub 仓库创建后，维护者应优先启用 GitHub Private Vulnerability Reporting。启用前，不应虚构安全邮箱；维护者必须在公开发布前补充真实的私密报告渠道。

报告建议包含：

- 受影响版本或提交。
- 复现条件和最小步骤。
- 实际影响与潜在影响。
- 可用的日志、截图或概念验证。
- 已尝试的缓解方式。

维护者收到报告后应确认接收、评估严重程度、协调修复和披露时间，并在可行时向报告者致谢。

## 敏感数据原则

- API 密钥和凭据不得进入源码、日志或 Git。
- 本地数据库可能包含用户任务和时间安排，应按敏感个人数据处理。
- 已完成事件将保留为本地历史记录；任何未来 API/AI 读取必须取得用户明确授权，并
  提供可理解的数据范围、撤销方式和删除/导出路径。
- 日志默认不得记录任务正文或用户隐私内容。
- 第三方依赖只按实际需要引入，并保持来源和许可证可审查。
