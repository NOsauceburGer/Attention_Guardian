# Attention Guardian Agent Guide

本文件约束所有在本仓库工作的 AI Agent。开始任务前，必须先完整阅读本文件、
`HANDOFF.md` 及与任务相关的当前项目文档。`teaching.md` 是用户与 Agent 之间的本地
教学日志，已由 `.gitignore` 排除，不属于开源仓库或发布内容；文件存在时只需阅读
开头的用途与追加规则，以及与当前任务直接相关的最近章节。

## 项目身份

- 中文工作名：托管时间
- 英文项目名：Attention Guardian
- 第一目标平台：Windows
- 技术栈：C#、.NET 10、Avalonia UI、MVVM、SQLite
- 许可证：GPL-3.0

不得继续使用旧的临时英文名 `HostedTime` 创建新文件、命名空间、项目或用户可见内容。

## 产品原则

Attention Guardian 的目标是替用户看守未来事项，减少用户对未来的持续心理监控。它不是普通 Todo List、完整日历或日程时间轴。

- 主界面在用户点击开始后只突出当前唯一任务和托管确认，不显示任何时间。
- 到达交接时间前，不持续暴露下一事项详情。
- 时间计算必须透明、确定、可解释。
- 第一版保持安静、低信息密度，不接入 AI、云同步或社交功能；但已完成事件必须保留为
  不可见的完成记录，为后续经用户授权的 API/AI 分析保留可靠历史数据。

## 桌面界面信息架构

桌面应用必须遵守以下页面职责，不得把添加事件表单当作默认首页。

### 已确认的专注界面与视觉交互

- 视觉方向采用冷银灰基底、冰蓝单一强调色的 Apple 风磨砂玻璃。玻璃用于抽屉、按钮、提醒和可交互气泡等有层级意义的表面，不把所有内容无差别装进玻璃卡片。
- 按钮按下必须具有轻微缩小或下沉反馈；气泡拖拽必须显示提起、位移和落位反馈。动效不得弹跳、闪烁或制造焦虑，并必须支持减少动态效果。
- 专注界面完全隐藏开始时间、结束时间、安全截止时间、剩余时间、倒计时和进度条。
- 专注界面初始只显示“开始”按钮。即使当前时间已有事件，在用户点击开始前也不显示事件名称。
- 点击开始后，当前事件名称从上至下淡入，背景出现克制、缓慢的彩色流动。该背景只表达“专注已经开始”，不能持续抢夺注意力。
- 当前事件结束前五分钟，只有下一事件与当前事件首尾相接且下一事件不是“休息”时，才在应用窗口打开或最小化状态下发送系统提醒。提醒同时显示当前事件和下一事件名称。当前事件持续不足五分钟、有时间空档、没有下一事件或应用完全退出时不提醒。
- 到期未来待办提醒默认折叠，只显示固定标题和右侧向下展开按钮；展开后才显示具体事项，整个区域只提供一个“进入管理”按钮。
- 不可移动事件冲突提醒采用相同折叠逻辑，展开后显示冲突名称并只提供一个“进入管理”按钮。存在多个当前冲突事件时，新输入事件优先成为当前事件，但提醒同时显示冲突双方或冲突组名称。
- “添加事件”和“事件管理”入口隐藏在顶部下拉抽屉。首页只保留一条低存在感拖拽把手；用户从窗口顶部向下拖动后看到两个入口。点击任一入口或抽屉外部即收回，不设置额外收起按钮。
- 每次保存造成任意顺延时，首页顶部缓慢下拉低存在感提示“有事件被顺延”，三秒后自动缩回。跨日顺延另显示可手动关闭的柔和黄色提示“有事件被顺延至明日”；两者可以同时出现。

### 待办输入类型、存储边界与当前事件判定

用户输入必须明确区分以下两类：

1. **当日待办**：指所有具有明确时间安排、需要参加加算、排序和冲突检测的事件。“当日”是产品分类名，不代表事件必须在当天结束；事件允许跨过零点并真实进入下一日期。
2. **未来待办**：用户可见名称，指只有日期、没有具体时间安排的事件。它只需要按日期保存，不参加加算、排序或时间冲突检测。代码和历史文档中的 `UnscheduledTodo`、“待办事项”均对应这一类。

两类数据必须使用两个独立数据库分别存储，不得放入同一待办表后仅依靠可空时间字段区分。当日待办数据库负责有时间事件及其排程结果；未来待办数据库只负责日期事项的保存和读取。当前单会话数据库属于过渡实现，正式迁移前必须设计 schema 版本、迁移和失败恢复。

两个数据库的边界直接来自用户的第一步选择，不是后台自行猜测：

- 用户点击“添加事件”后，必须先选择“当日待办”或“未来待办”。
- 选择“当日待办”后，只进入当日待办录入流程，只读取和写入当日待办数据库。
- 选择“未来待办”后，只进入未来待办录入流程，只读取和写入未来待办数据库。
- 进入某一分支后，不得在同一表单中混入另一类字段，也不得保存时根据字段是否为空临时判断数据库。
- 用户返回类型选择前，不得跨数据库创建、修改或删除数据。

“此时需要执行的事件”是有时间安排的事件中，当前时间落入其开始时间与截止时间范围内的事件。不得用“最后创建”“当前选中”或“最近未来事件”代替当前时间表匹配。跨零点事件在其完整时间区间内仍可成为当前事件。

只有打开软件时才检测当天是否存在日期已到但未规划时间的未来待办，不持续轮询。若存在一条或多条，打开界面最上方显示一个克制但清晰的小型红色提示区域。区域包含感叹号和固定文字：

> 有当日未设置时间待办

该提示区域只说明存在未定时待办，不应抢占当前定时事件的主要视觉位置，但必须让用户能够处理具体事项：

- 每条日期已到的未来待办显示事件名称，并在后方提供“规划”和“删除”操作。
- 点击“规划”后跳转到添加当日待办页面，事件名称由软件自动填入并保持可见；用户只需正常选择开始时间和持续时间。
- 只有用户成功保存该当日待办后，才把原未来待办从活动记录中去掉，或标记为已完成使其不再参与提醒。具体采用物理删除还是完成标记，必须在未来待办数据库 schema 设计时明确并保持一致。
- 如果用户没有完成任何输入或保存就返回主界面，原未来待办不得改变，红色提醒继续显示。
- 点击“删除”必须先显示确认弹窗。弹窗明确显示将被删除的事件名称，并询问用户是否继续。
- 用户取消删除时不修改数据；用户确认后，才从未来待办的活动记录中去掉该事项。
- 规划成功或确认删除必须只作用于用户点击的那一条未来待办，不得批量处理同日其他事项。

### 打开界面

- 打开应用后，先根据当前时间和已有事件判断此时是否存在需要执行或托管的事件。
- 没有此时需要执行的事件时，显示平静空状态；添加入口仍位于顶部下拉抽屉。
- 已有此时需要执行的事件时，先显示“开始”按钮；用户点击后才显示当前任务和托管确认。
- “添加事件”和“事件管理”始终只位于顶部下拉抽屉，不直接暴露在打开界面。
- 当天存在未定时待办时，无论当前是否存在定时事件，都在打开界面最上方显示“有当日未设置时间待办”提示。
- 打开界面负责呈现当前状态和进入其他流程，不直接承载添加事件表单。

### 添加事件界面

- 只有用户主动点击“添加事件”后，才进入添加事件界面。
- 添加流程的第一屏只负责选择“当日待办”或“未来待办”，不提前展示任一类型的完整表单。
- 添加当日待办时显示开始时间选择和持续时间选择，不让用户直接填写结束时间。
- 如果用户没有修改开始时间，开始时间默认为操作当下；如果用户修改了开始时间，则使用用户选择的开始时间。
- 结束时间必须由“开始时间 + 持续时间”自动计算，再进入当日事件的加算、排序和冲突检测。
- 除添加时必要的开始时间选择外，规划结果和普通列表不直接向用户暴露开始与结束时间。
- 添加未来待办时提供两种日期输入：直接在日历中选择日期；或通过“一天后、两天后、三天后”等明确选项表达相对日期。相对日期由系统换算为确定日期后保存。
- 添加成功后返回打开界面，由打开界面根据最新状态展示事件。

### 事件管理界面

- 当用户选择查看或删除已有事件时，进入独立的事件管理界面。
- 默认只展示当日待办，隐藏只有日期的未来待办，避免增加认知负担。
- 当日待办按排程顺序排列，但普通管理视图不直接展示开始与结束时间。
- 页面下方提供明确的“展开未来待办”入口；只有用户主动展开后，才读取未来待办数据库并展示其内容。
- 每条事件提供明确的删除操作。
- 删除必须作用于用户明确选择的事件。
- 事件管理界面只负责查看和删除，不承担添加事件功能，也不放置添加事件表单。

任何新增页面或导航改动都必须保持以上职责分离。如果需求仍有歧义，应先确认事件状态判定和导航入口，不得自行把多个页面重新合并为一个表单页。

### 自动顺延与强制事件

当新增当日待办需要占用已有时间段时，系统按以下顺序处理：

1. 检查当日时间表中是否存在强制事件。
2. 当日不存在强制事件时，可以直接顺延受影响的后续普通事件。
3. 当日存在强制事件时，不得立即修改时间表。必须先在内存中执行一次加减试算，模拟所有受影响事件顺延后的开始和截止时间。
4. 普通事件的原时段或顺延结果如果占用强制事件时间，不显示冲突提醒；把该普通事件整体移动到强制事件结束之后，再继续顺延后续普通事件。
5. 如果连续遇到多个强制事件，普通事件依次越过这些强制事件，始终保持原持续时间。
6. 两个或更多不可移动事件互相冲突时仍然保存，允许它们暂时重叠。返回专注界面后显示“有不可移动事件冲突”，由用户进入事件管理页决定顺序。

自动顺延必须保持每个事件原有持续时间，只移动开始和截止时刻。一次保存仍必须整体成功或整体失败；“允许不可移动事件暂时重叠”是完整计划的有效冲突状态，不是半完成写入。

顺延允许跨过午夜。跨日后必须真实更新事件所属日期，并在托管界面顶部显示柔和的黄色提示标识：

> 有事件被顺延至明日

该黄色提示允许用户手动关闭，因为跨日可能符合用户的正常作息。关闭只隐藏提示，不撤销顺延结果，也不删除跨日事件。

### 事件管理与手工排序

- 当日待办按执行顺序显示为纵向长条气泡；顺序本身就是优先级。不可移动事件使用语义红色背景，但仍以文字和交互状态辅助识别，不能只依赖颜色。
- 普通事件可拖拽改变顺序。重排保持持续时间不变，整条队列从原计划最早开始时间重新计算；后续事件以上一事件结束时刻为开始时刻。
- 普通事件拖到不可移动事件无法容纳的位置时，自动落到最后一个阻挡它的不可移动事件之后，并显示“这个事件没办法放在这里”。
- 冲突或首尾相接、没有空档的不可移动事件构成连续组。连续组内的不可移动事件允许拖拽排序；其他情况下不可移动事件不能拖动。
- 冲突组在同一行等分显示：两个各占一半，三个各占三分之一。进入冲突管理时自动滚动到第一组，其余组按时间顺序排列。
- 冲突组排序后从该组最早的原始开始时间连续重排，新时间正式保存，之后事件仍保持“不可移动事件”属性。
- 双击气泡展开编辑；再次双击收起时统一保存并重排。展开区只显示名称输入、持续时间的小时/分钟下拉选择、删除按钮和“不可移动事件”复选框，不显示开始时间，也不设置保存按钮。
- 用户带着未收起的修改离开管理页时，必须询问是否保存。
- 管理页所有删除都必须显示包含事件名称的确认弹窗。删除后后续事件向前补位；下一事件使用被删除事件原开始时刻。
- 右侧提供固定名称为“休息”的模板气泡，使用与不可移动事件不同的辅助底色。默认持续 20 分钟，可修改模板时长，可重复拖入主列表。拖入后按普通事件持久化，可排序、修改持续时间和删除，但不能改名，也永远不是不可移动事件。
- 未来待办保留在管理页的主动展开区域，按日期从近到远排列。未来待办气泡不参与拖拽，只显示名称和日期；双击展开后允许修改名称、日期和删除，再次双击收起时统一保存。

## 完整开发后的用户验收与诊断界面

以下内容属于全部主要软件功能完成后的最终验收阶段，不得抢在领域规则、持久化、页面、通知和恢复能力之前实现：

1. 以真实使用者视角创建一套可操作的 Todo List 验收样例，覆盖至少：
   - 没有强制事件时的普通顺延；
   - 普通顺延链撞上一个或多个强制事件后自动越过并继续；
   - 两个强制事件时间冲突且系统不自动决定优先级；
   - 一个或多个事件被顺延进入第二天；
   - 时间区间首尾相接、空档停止顺延、跨零点当前事件等边界情况。
2. 验收不能只依赖自动化测试或 Agent 自述。用户必须能在可交互界面中加载、操作和检查这些样例，并看到预期结果与实际结果。
3. 功能完整后提供一个本地、可交互的诊断入口。它可以让用户查看一次“加算与排序”过程，以及每一步采用了什么简单规则。
4. 诊断日志使用普通用户能理解的简短语言，例如“任务 A 与新任务重叠，因此向后移动 30 分钟”“前面是不可移动事件 B，所以任务 A 从它结束后继续”。不可移动事件互相冲突时显示“已保存，等待你在管理页决定顺序”。避免输出数据库术语、对象地址、堆栈或大量内部字段。
5. 诊断界面应允许选择验收样例、逐步查看或重新运行、对比原时间表与试算结果，并明确显示“已应用”或“因冲突未修改”。
6. 该入口是本地验证工具，不得绕过数据校验、安全边界或用户授权，不得成为远程访问后门。正式发布时应有明确入口或开发者开关，且不得默认泄露用户真实待办内容。
7. 实现该阶段时必须为验收样例和诊断输出增加测试；本地存在 `teaching.md` 时，在其中
   解释用户如何亲自验证，但不得将该文件上传 GitHub。

## 已完成事件生命周期

- 当日待办到达结束时刻后应退出活动排程，用户端默认不可见，也不再参与当前事件、
  排序、顺延、冲突检测或提醒。
- 退出活动排程不等于删除数据库记录。必须通过明确的完成状态和完成时刻保留历史；
  普通活动查询只返回未完成记录。
- 用户主动删除与自然完成是两个不同状态，不得混用。后续 API/AI 只能在用户明确
  授权后读取这些历史记录；v0.1.0 不接入 AI，也不上传数据。
- schema 变更必须提供版本迁移、失败回滚和旧数据库兼容测试。任何整表替换操作都
  不得物理擦除已完成历史。

## 架构边界

- `AttentionGuardian.Core` 不依赖 Avalonia、SQLite 或 Windows API。
- `AttentionGuardian.Application` 可以依赖 Core，但不依赖基础设施实现。
- `AttentionGuardian.Infrastructure` 实现 Application 定义的接口。
- `AttentionGuardian.Desktop` 负责组合各层与显示 UI。
- UI 不直接访问数据库。
- 时间计算不得放入 View 或 ViewModel。
- 不为尚未确认的未来功能创建复杂占位架构。

## 工作方式

1. 修改前检查实际目录、Git 状态和适用文档。
2. 较大修改前说明目标和验证方式。
3. 采用小步、可审查、可回退的实现。
4. 优先修复根因，不顺手修改无关问题。
5. 不覆盖用户工作，不擅自提交、推送、发布或改写历史。
6. 不声称未实际运行的构建、测试、数据库、通知或打包结果成功。
7. 实质性代码或架构变更必须同步更新 `PROJECT_DEVELOPMENT.md`。
8. 架构决策同步更新 `ARCHITECTURE.md` 或 `PRODUCT_DECISIONS.md`。

## 质量门槛

- 新业务规则应有独立单元测试。
- 核心时间逻辑测试不得依赖 UI 或数据库。
- 先运行最小相关测试，再运行更广泛的构建和测试。
- 不提交密钥、本地数据库、日志、个人配置或构建产物。
- 注释只解释重要约束和原因，不复述显而易见的代码。

## 本地教学要求

用户是完全没有项目开发基础、以产品型 Vibe Coder 方式学习的初学者。Agent 不仅要完成项目，还必须帮助用户理解每个开发步骤在完整工程流程中的意义。

`teaching.md` 只服务于本地教学，不得暂存、提交或上传到 GitHub。文件存在时采用
只追加的历史日志规则；后续 Agent 只能在文件末尾添加新章节，
不得修改、重写、整理、合并或删除已有内容；即使历史章节记录了已经被后续决策
替代的旧状态，也应保留原文，并在新章节中说明最新变化。

接手项目时不需要完整阅读 `teaching.md`。只需读取文件开头的用途与记录规则、
搜索当前任务涉及的关键词，并阅读最近相关章节，以避免重复解释或遗漏已有教学背景。

每次执行实际项目操作时，如果本地存在 `teaching.md`，必须在文件末尾追加教学记录；
如果用户主动移除该文件，不得为了 GitHub 发布擅自重建。新增内容应面向完全零基础的
学习者，至少解释：

1. 本次要解决什么问题；
2. 实际执行了哪些操作；
3. 为什么项目需要这些操作；
4. 这些操作位于完整开发流程的哪个阶段；
5. 如何验证操作真的成功；
6. 如果不做会有什么影响；
7. 常见替代方案；
8. 用户当前需要掌握到什么程度；
9. 遇到的错误、警告、限制和未完成事项。

教学记录不得只罗列命令，也不得把计划写成完成结果。首次出现重要概念时，应解释它是什么、位于哪一层以及与相邻概念的区别。重复操作可以引用已有章节，只补充本次差异。发现旧章节有错误或内容过时时，不得回头改写；应在文件末尾追加更正说明。

聊天中的说明不能替代已有的本地 `teaching.md`。实质性代码、架构、工具链、测试、
数据库、通知、打包或发布操作，在该本地文件存在时都必须留下相应教学记录；仅修正
错别字等无实质影响的修改可以不单独追加章节。任何情况下都不得将它加入 Git。

## 当前优先级

1. 以用户确认的 v0.1.0 验收标准审计代码与文档，不再把已完成的基础工程当作未来计划。
2. 修复“完成任务被整表替换物理移除”与完成历史保留标准之间的差异，并补充 schema
   迁移、失败恢复和测试。
3. 复跑 Release 构建、全量测试与必要的真实 Windows 验收，形成可追溯发布证据。
4. 完成版本号、变更日志、安装/已知限制、隐私与安全说明及干净环境打包检查。
5. 只有获得用户授权后才创建提交、Tag、GitHub Release 或对外发布。
6. v0.1.x 之后再按 `ROADMAP.md` 增加 Windows 系统通知、诊断界面和 AI/API 等能力。
