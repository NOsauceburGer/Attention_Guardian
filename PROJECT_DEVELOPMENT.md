# 项目开发记录

本文档是 Attention Guardian 的事实状态账本。只记录已经完成并能验证的事项，不把计划当作结果。

## 当前阶段

v0.1.0 GitHub 首版发布前准备。用户已确认现有产品、UI 与基础能力构成发布基线；
文档审计发现当日待办完成历史仍被物理移除，与最新验收标准冲突。下一步先修复完成
生命周期并验证，再进行版本号、打包、Tag 和 GitHub Release；后四项尚未获授权执行。

## 已完成

### 2026-07-25

- 阅读原始项目交接文档。
- 确认英文项目名为 Attention Guardian。
- 检查工作目录：仅存在交接文档。
- 检查 Git：当前目录不是 Git 仓库。
- 检查 .NET SDK：已安装 `10.0.302`。
- 确认当前没有 Solution、Project、业务代码、测试或可运行应用。
- 建立项目、架构、贡献、安全、支持和 GitHub 协作所需的基础 Markdown 文档。
- 初始化本地 Git 仓库，默认分支为 `main`。
- 创建适用于 .NET 的 `.gitignore` 和完整 GPL-3.0 `LICENSE`。
- 安装并验证 Avalonia CLI 模板 `12.1.0.2`。
- 创建 `AttentionGuardian.slnx` 及 Core、Application、Infrastructure、Desktop 和两个测试项目。
- 按架构边界配置项目引用，删除模板生成的无意义占位类和占位测试。
- 成功执行完整 restore 与 Release build：6 个项目全部生成，0 个警告、0 个错误。
- 执行测试基线：测试运行器可启动，但业务测试尚未实现，因此发现 0 个测试。
- 验证 Core 没有任何 NuGet 包依赖。
- 新建 `teaching.md`，以零基础视角补充阶段 0 和阶段 1 的开发教学记录。
- 更新 `AGENTS.md`，要求后续实际项目操作同步维护 `teaching.md`。
- 确认 Core 使用 `DateTimeOffset`、`TimeSpan` 和显式当前时间的统一时间策略。
- 实现最小 `FixedEvent`、`handoffTime`、`safeUntil` 和交接边界判断。
- 添加普通扣减、跨午夜、零时长、负时长和交接边界测试。
- Core 测试通过 10 项；完整 Release 构建通过，0 个警告、0 个错误。
- 建立 `PRODUCT.md` 与 `DESIGN.md`，确认安静、可信、克制的产品体验和柔和动效原则。
- 实现内存 `FocusSessionService`、本地时间解析、开始托管、定时刷新和清除用例。
- 新增 5 项 Application 用例测试，覆盖开始、边界交接、刷新、无任务和清除状态。
- 将 Avalonia 模板页替换为最小设置、专注和交接界面，并接入 ViewModel。
- 修复 Desktop 缺少 `System` 命名空间和 `Application` 重名导致的首次编译错误。
- 修复 Avalonia 12 中 `Watermark` 属性过时警告。
- 完整 Release 构建通过，0 个警告、0 个错误；Core 10 项和 Application 5 项测试全部通过。
- 实际启动 Windows 应用并验证设置、开始托管、专注显示和返回重新设置。
- 实现多个固定事件的下一事件选择：排除已开始事件，并选择最早的未开始事件。
- 在创建 `FixedEvent` 时拒绝超出支持日期范围的交接时间。
- 新增事件选择、时间越界和自定义夏令时时区测试。
- Core 测试增至 15 项，Application 测试增至 8 项；完整 Release 构建仍为 0 个警告、0 个错误。
- 将下一事件选择接入 `FocusSessionService`，支持从无序事件集合开始专注会话。
- 单事件入口复用多事件入口；没有未来事件时明确拒绝且不污染当前内存状态。
- Application 测试增至 11 项；完整 Release 构建保持 0 个警告、0 个错误。
- 在 Application 定义 `IFocusSessionRepository`，并由 `FocusSessionCoordinator` 组织保存、恢复和清除。
- 在 Infrastructure 实现内存仓库和 SQLite 仓库，新增独立 Infrastructure 集成测试项目。
- SQLite 保存唯一当前会话，使用 UTC 时刻、原始偏移量和整数秒时长；派生状态在恢复时重新计算。
- 接入 Desktop：开始托管后保存、启动时自动恢复、重新设置时清除本地记录。
- NuGet 首次恢复发现 SQLitePCLRaw 2.1.11 高危漏洞；显式升级到 2.1.12 后漏洞扫描无已知问题。
- 修复 SQLite 测试连接池持有临时文件和 Desktop 缺少异步命名空间的问题。
- 实际完成“保存 → 关闭窗口 → 重新启动 → 自动恢复”验证。
- Core 15 项、Application 15 项、Infrastructure 3 项测试全部通过；Release 构建 0 个警告、0 个错误。
- 确认桌面信息架构：打开界面展示当前状态，添加事件与事件管理使用独立界面。
- 当时确认事件管理界面按时间查看和删除、不承担添加事件功能；2026-07-26 进一步收紧为默认只显示当日待办，未来待办需主动展开。
- 明确当日定时待办与指定日期未定时待办两种输入类型。
- 确认首页当前事件由当前时间匹配当日时间表得出；当天有未定时待办时显示顶部红色提示。
- 确认自动顺延先检查强制事件；存在强制事件时先完整试算，安全后才一次性写入。
- 确认顺延允许跨日，并在托管页显示可关闭的黄色跨日提示。
- 确认强制事件互相冲突时不自动决定优先级。
- 新建 `HANDOFF.md`，记录当前验证基线、尚未实现的排程规则和新对话继续顺序。

### 2026-07-26

- 按交接要求重新检查目录和 Git 状态；仓库仍位于无提交的 `main`，现有文件均未跟踪。
- 重新运行 Core 基线测试，原有 15 项全部通过。
- 在 Core 新增 `TodoItem`、`ScheduledTodo` 与 `UnscheduledTodo`，明确两类待办共享稳定标识、标题和强制属性，未定时待办不伪造时间段。
- 新增 `TimeRange`，采用 `[开始, 结束)` 边界，并拒绝零时长和负时长区间。
- 新增纯内存 `ScheduleTrial` 与结果类型，支持普通事件级联顺延、空档停止、强制事件碰撞、失败不返回部分计划和跨日标识。
- 顺延通过创建新对象完成，保留原事件标识、标题、强制属性和持续时间，不修改输入时间表。
- 新增 17 项 Core 单元测试；Core 测试总数增至 32 项。
- 完整 Release 测试通过：Core 32 项、Application 15 项、Infrastructure 3 项。
- 完整 Release 构建成功：0 个警告、0 个错误。
- 本轮未修改 Desktop UI、Application 用例或 SQLite schema。
- 确认“当日待办”是所有有时间安排、需要排程的事件分类，允许跨零点；“待办事项”只有日期且不参加冲突检测。
- 确认未来使用两个独立数据库分别保存当日待办与待办事项；当前单会话 SQLite 仍是过渡实现。
- 确认当日待办输入为开始时间与持续时间，未改开始时间时默认现在开始，结束时间由系统自动计算。
- 确认待办事项支持日历日期和“一天后、两天后、三天后”等相对日期输入。
- 确认管理页默认只显示当日待办，用户主动展开后才显示未来待办。
- 确认全部主要功能完成后增加用户可操作的综合排程样例和本地交互式诊断界面；本轮只记录要求，尚未实现。
- 将强制碰撞规则调整为：普通事件自动整体移动到强制事件结束后继续顺延，不再返回冲突；只有强制事件互相重叠才返回冲突。
- 排程试算支持普通事件连续越过多个强制事件，并在越过强制事件导致跨日时设置跨日标识。
- 新增 2 项 Core 测试并改写 2 项旧冲突测试；Core 测试总数增至 34 项。
- 完整 Release 测试通过：Core 34 项、Application 15 项、Infrastructure 3 项。
- 完整 Release 构建成功：0 个警告、0 个错误；Core 与 Core.Tests 格式检查通过。
- 在 Core 新增 `ScheduledTodoSelector`，按 `[开始, 结束)` 匹配当前事件，并覆盖跨零点、结束边界和无序输入。
- 在 Application 定义 `IScheduledTodoRepository` 与 `IUnscheduledTodoRepository`，分别对应两个未来数据库。
- 实现 `TodoPlanningService`：未指定开始时间时使用现在、按持续时间计算结束、执行排程试算、成功才整组提交、冲突不写入。
- 实现日历日期和相对天数两种待办事项输入；相对表达在保存前转换成确定日期。
- 实现 `LoadOpeningStateAsync`，一次读取当前有时间事件和当天是否存在未规划事项。
- 新增 3 项 Core 测试与 10 项 Application 测试；Core 总数增至 37 项，Application 增至 25 项。
- 完整 Release 测试通过：Core 37 项、Application 25 项、Infrastructure 3 项。
- 完整 Release 构建成功：0 个警告、0 个错误；Core、Application 及两个对应测试项目格式检查通过。
- 本轮没有修改 Infrastructure、Desktop 或现有单会话 SQLite 文件。
- 确认两个数据库直接对应添加流程的两种用户选择：当日待办与未来待办。
- 确认添加事件第一步必须先选择类型；选择后只显示对应表单、调用对应 Application 用例并管理对应数据库。
- 明确“未来待办”是用户可见名称，当前 `UnscheduledTodo` 与“待办事项”历史术语保持为内部/历史对应关系。
- 本轮只同步需求与架构文档，未修改 Desktop 或数据库实现。
- 确认日期已到的未来待办提醒按事项提供“规划”和“删除”。
- 确认规划入口预填原事件名称；只有成功保存当日待办后，原未来待办才退出活动列表。
- 确认未输入或未保存返回时原未来待办保持不变并继续提醒。
- 确认删除必须先显示包含事件名称的二次确认弹窗，取消不修改，确认只删除所选事项。
- 记录双数据库规划转换需要补偿/幂等设计；物理删除或完成标记策略将在 schema 设计时确定。
- 新增 `SqliteScheduledTodoRepository` 与 `SqliteUnscheduledTodoRepository`，分别使用 `attention-guardian-scheduled.db` 和 `attention-guardian-future.db`。
- 两个数据库各自维护 `schema_version`，首次访问在事务内建立 v1 schema；高于支持版本的数据库会被拒绝。
- 当日待办以 UTC、原始偏移量和整数秒持续时间保存，`ReplaceAllAsync` 在单事务内替换整张计划。
- 未来待办采用 `active`、`planned`、`deleted` 生命周期状态；当前活动查询只返回 `active`。
- 新增 6 项真实临时 SQLite 文件集成测试，验证往返、排序、事务回滚、无损秒精度、按日期读取、双库隔离、版本表和未来版本拒绝。
- `OpeningTodoState` 改为返回截止今天仍活动的未来待办集合，包含逾期事项并排除未来日期。
- 未来待办仓库新增按标识读取和 `planned`/`deleted` 幂等状态转换；不兼容状态转换会被拒绝。
- 新增规划转换用例：转换后的当日待办沿用源 `Guid`，先写当日库再标记未来待办；第二步失败后重试不会重复创建或再次排程。
- 新增确认删除用例：取消不写数据库，确认后只标记所选事项。
- Application 新增 5 项流程测试，Infrastructure 新增 3 项生命周期与到期查询测试。
- 根据用户最终确认重写 UI 方向：专注界面隐藏全部时间，点击开始后才显示当前事件；添加和管理入口移入顶部下拉抽屉。
- 确认 Apple 风冷银磨砂玻璃、可触点击与拖拽、克制专注色流、折叠提醒和两类顺延反馈。
- 确认不可移动冲突改为可保存状态，管理页通过同排气泡和组内拖拽解决。
- 确认气泡编辑、删除补位、休息模板、未来待办气泡和五分钟前通知的完整交互规则。
- 本轮文档同步先于实现，旧 Core 的冲突拒绝和旧 Desktop 的时间展示仍待后续代码修改。
- Core 已把不可移动冲突改为“完整计划 + 冲突集合”，Application 会整体保存冲突状态；对应 Core/Application 测试已更新。
- Desktop 已切换到两个正式 SQLite 仓库，旧单会话协调器退出新主窗口组合。
- 第一版冷银玻璃应用壳已实现：首页时间完全隐藏、无任务时空状态、存在任务时点击开始后才展示名称、克制背景色流、顶部拖拽抽屉、折叠提醒、类型选择、两个添加表单、基础管理气泡和休息入口。
- 使用真实 Windows 窗口检查了首页、顶部拖拽抽屉和类型选择页；抽屉手势与导航实际可用。
- 当前尚未完成气泡双击编辑、拖拽重排、删除确认、未来待办编辑、五分钟前系统通知和冲突组同排布局。
- 本轮只记录规划，未实现 UI、跨仓库用例或数据库状态变更。

## 尚未完成

- 创建 GitHub 远程仓库。
- 把已结束当日待办改为标记完成并保留历史，补充 schema 迁移和测试。
- 完成 v0.1.0 版本号、发布说明、干净 Windows 打包与安装验证。
- 创建首次提交、远程仓库、Tag 和 GitHub Release（均需用户明确授权）。
- 后续版本实现五分钟前 Windows 系统通知和本地交互式诊断界面。

## 历史验证结果（2026-07-26 快照）

| 检查项 | 结果 |
| --- | --- |
| `.NET SDK` | `10.0.302` |
| Git 仓库 | 已初始化，默认分支 `main` |
| Solution | `AttentionGuardian.slnx` |
| Project | 4 个源项目、2 个测试项目 |
| Avalonia CLI 模板 | `12.1.0.2` |
| Release 构建 | 成功，0 个警告、0 个错误 |
| Core 测试 | 37 项通过，0 项失败 |
| Application 测试 | 30 项通过，0 项失败 |
| Infrastructure 测试 | 12 项通过，0 项失败 |
| Release 构建 | 成功，0 个警告、0 个错误 |
| 依赖漏洞扫描 | 无已知易受攻击包 |
| 可运行应用 | 当时为支持 SQLite 恢复的最小体验版；当前状态见文末最新记录 |

## 下一检查点

1. 重构 Desktop 打开界面，使其只呈现当前状态、到期提醒和导航入口。
2. 添加事件先进入类型选择，再进入两个独立表单。
3. 接入逐条规划、确认删除和管理页渐进展开。
4. Desktop 只调用 Application，并使用两个新数据库的默认本地路径。
## 2026-07-26：补齐管理排程的 Core/Application 合同

- 新增纯 Core `ScheduleManagement`，覆盖普通事件重排、不可移动挡板回落、
  连续不可移动组识别与组内重排、删除补位、编辑后重排和休息插入。
- 重排结果显式返回实际位置和是否回落，供 Desktop 显示已确认的失败提示；
  Desktop 无需也不得重新计算时间。
- 新增 Application `ScheduleManagementService`，统一编排加载、Core 计算和
  整张计划原子替换，并保留所有删除操作的显式确认参数。
- 未来待办仓库新增“读取全部 active”和“只更新 active”合同，SQLite 实现
  不允许编辑操作复活已规划或已删除记录。
- 新增 9 项 Core、6 项 Application 和 2 项 Infrastructure 测试。
- 最小验证实测：Core 46 项、Application 36 项、Infrastructure 14 项通过。
  首次并行运行 Application 与 Infrastructure 时因两者同时写同一编译输出，
  被 Windows Defender 短暂锁定；改为顺序运行后通过。
- 全套 Release 测试、格式检查和构建通过，构建为 0 个警告、0 个错误；
  依赖漏洞扫描未发现已知易受攻击包。
- 本阶段只完成管理能力的领域与用例基础，Desktop 气泡 ViewModel、双击编辑、
  离开询问、确认弹窗、真实拖拽和冲突组布局仍未实现。
## 2026-07-26：校正 Desktop 对称与按钮居中

- 将主内容区四边留白统一为同一数值，建立稳定的窗口中心线。
- 为所有按钮统一设置水平和垂直内容居中，避免复合内容沿用左对齐。
- 添加类型选择卡片中的两行文字、专注空状态、两个添加表单标题和底部操作组
  均显式居中。
- 未重新设计颜色、材质或动效；管理页继续保留主列表与休息侧栏的功能性双栏结构。
- Desktop 格式检查与完整 Release 构建通过，0 个警告、0 个错误。
## 2026-07-26：接入管理气泡编辑与确认边界

- Desktop 组合根接入 `ScheduleManagementService`，管理页不再直接用仓库修改排程。
- 新增 `ScheduledTodoBubbleViewModel` 和 `FutureTodoBubbleViewModel`，把展开期间的
  输入与已保存领域对象分开。
- 当日和未来待办均支持双击展开、再次双击统一保存；保存失败时编辑区保持展开并
  显示错误。
- 当日气泡编辑名称、小时/分钟持续时间和不可移动属性；休息名称和不可移动属性
  在界面与 Core 两层受保护。
- 未来待办只有用户主动展开区域后才读取，支持编辑名称和日期。
- 当日与未来待办删除均显示包含事件名称的确认层，确认前不写数据。
- 带未保存修改离开管理页时，允许“保存后离开”“放弃并离开”或取消。
- 新建 Desktop 测试项目并加入 Solution，9 项 ViewModel 测试通过。
- 全套验证通过：Core 46、Application 36、Infrastructure 14、Desktop 9；
  Release 构建 0 个警告、0 个错误。
- 尚未实现真实拖拽/键盘重排、冲突组同排与自动定位、休息模板复制拖入和通知。
## 2026-07-26：拆分添加页开始时间选择器

- 将会同时展开小时与分钟列的复合 `TimePicker` 替换为两个独立 `ComboBox`。
- 小时下拉只包含 0–23，分钟下拉只包含 0–59；保存时再组合为一个确定的本地时间。
- 日期选择器保持独立，不与小时或分钟下拉共享展开状态。
- 新增 Desktop 测试验证小时与分钟在保存时正确组合；Desktop 10 项通过，
  Release 构建 0 个警告、0 个错误。
- 已真实启动窗口进入当日待办表单，确认点击小时后只展开小时列表，分钟控件保持关闭。
## 2026-07-26：实现管理页拖拽与键盘重排

- 当日气泡增加独立拖动把手，拖动时提供提起缩放与透明度反馈。
- 可落位气泡在拖拽经过时显示冰蓝边缘，落下后由
  `ScheduleManagementService.ReorderAsync` 保存完整排程。
- 气泡支持 `Alt+↑` 和 `Alt+↓` 键盘等价重排，并提供可访问帮助文本。
- 普通事件遇到不可移动挡板时使用 Core 返回的实际位置；回落或非法移动均在管理页
  显示“这个事件没办法放在这里”。
- 不可移动事件仍由 Core 限制为只能在连续不可移动组内重排，Desktop 不复制规则。
- 新增 Desktop 测试验证优先级顺序保存和不可移动事件非法移动提示。
- 全套测试通过：Core 46、Application 36、Infrastructure 14、Desktop 12；
  Release 构建 0 个警告、0 个错误。
## 2026-07-26：实现不可移动连续组布局与自动定位

- 新增 `ScheduledTodoRowViewModel`，把单个事件行与连续不可移动组行明确分开。
- 重叠或首尾相接的不可移动事件在同一行使用 `UniformGrid` 等分；两个各占一半，
  三个各占三分之一，更多成员继续等分。
- 不可移动气泡使用语义红材质并保留“不可移动”文字，休息气泡使用青绿色材质，
  不只依赖颜色表达状态。
- 管理页打开并完成布局后，若存在连续组，自动把第一组带入可视区域。
- 组内拖拽继续使用统一重排服务；孤立不可移动事件仍被 Core 拒绝移动。
- Desktop 刷新气泡时显式按开始、结束和标识排序，不依赖仓库碰巧返回的顺序。
- 新增测试覆盖首尾相接成组和有空档时拆组；Desktop 共 14 项通过。
- 全套测试和格式检查通过；Release 构建 0 个警告、0 个错误。
## 2026-07-26：实现可重复复制的休息模板

- 管理页右侧休息模板增加独立小时与分钟下拉，默认保持 20 分钟。
- 模板可拖到任一目标气泡位置，也可拖到队尾区域；按钮激活作为键盘等价操作，
  直接加入队尾。
- 拖放使用 Copy 语义，模板不从侧栏移除，每次生成新的稳定标识。
- 每个持久化休息事件固定名称“休息”、始终为普通事件，之后仍可调整持续时间、
  排序和删除。
- 目标位置被不可移动事件阻挡时按 Core 结果回落，并显示既定提示。
- 零时长在 Desktop 写入前被拒绝；重复插入、固定属性、挡板回落和零时长均有测试。
- Desktop 测试增至 17 项。
- 全套测试和格式检查通过；Release 构建 0 个警告、0 个错误。
## 2026-07-26：为下一对话的 UI 大改整理准确交接

- 审计当前功能完成度，确认项目尚不能标记为正式版本完成。
- 除用户延后的 Windows 通知外，仍缺相对日期输入、到期未来待办规划的 Desktop
  完整流程，以及未修改开始时间时使用操作当下。
- 整体重写 `HANDOFF.md`，授权下一对话进行 UI 大改，并记录必须保留的业务能力、
  页面职责、数据边界、验证基线和大改后的真实窗口检查清单。
- 本次只更新文档，没有修改代码或产生新的测试结果。

## 2026-07-26：落地沉浸式专注主页与鼠标牵引色流

- 将 Desktop 全局视觉调整为冷银石墨深色画布，保留冰蓝单一强调色和既有语义色。
- 新增 `FocusFlowSurface` 纯视觉控件，绘制连续背景、柔化银雾、鼠标光晕和多条
  贝塞尔流线；鼠标只更新目标坐标，动画帧通过平滑插值追踪。
- 流线仅在专注已经开始且当前页面为主页时逐渐出现；鼠标离开窗口后回到安静停靠点。
- 顶部抽屉改为中央细长玻璃胶囊，内部只保留“添加事件”和“事件管理”以及细分隔线。
- 当前任务字号和托管确认文案按概念图调整；首页仍不显示任何时间。
- 添加表单改用更稳定的不透明深色表面，避免在复杂背景上降低输入可读性。
- 真实 Windows 窗口检查了空状态主页、下拉导航和添加类型页；未向用户数据库写入
  测试事项，因此本轮没有在真实窗口制造专注任务来检查流线。
- 首轮视觉检查发现背景过亮和椭圆边界明显，随后压暗石墨层并改用径向渐变柔化。
- Solution 格式检查通过；Core 46、Application 36、Infrastructure 14、Desktop
  17 项测试全部通过；Solution Release 构建为 0 个警告、0 个错误。

## 2026-07-26：以成熟工具型层级替代持续动态背景

- 研究 Super Productivity 官方仓库、官方截图及其焦点模式、任务列表和深色主题
  样式，提炼出平面表面、清晰分隔、当前项强调、拖拽时才抬升和短促状态过渡。
- 保留 Attention Guardian 的低信息密度、顶部抽屉、隐藏时间和点击开始后才显示
  当前事件等产品边界，不复制对方的高密度任务管理布局。
- 删除 `FocusFlowSurface` 与鼠标追踪计时器，主页改为近黑冷灰静态渐变；开始后
  使用状态标签、任务名称淡入和轻微纵向位移表达专注已经开始。
- 统一深色表面、输入框、按钮和分隔线令牌；添加类型页改为两条平面选择行，
  使用前置状态标记、说明文字和右侧箭头建立可扫描层级。
- Desktop Release 构建通过。真实窗口检查确认主页、顶部抽屉和添加类型页可用；
  完整测试结果记录在本轮教学章节。

## 2026-07-26：加入专注启动后的柔化水波色场

- 新增 `AmbientFocusSurface` Desktop 绘图控件，用四层大范围径向渐变模拟冰蓝、
  薄荷绿、淡紫与青色色雾。
- 色层使用不同的正弦周期缓慢移动并自然交叠，避免视频循环接缝、额外媒体资源和
  持续解码；色场没有硬边、射线或鼠标跟踪。
- 控件绑定 `IsFocusStarted`：开始前完全透明，开始后约 1.4 秒渐显；离开专注页面
  时停止计时器，返回后继续，且始终不参与命中测试。
- 业务 ViewModel、排程、持久化和页面导航命令均未修改。

## 2026-07-26：把系统标题栏融合为应用外壳

- 开启 Avalonia 客户区延伸标题栏，新增 46px 近黑顶栏和底部分隔线。
- 保留 Windows 原生标题栏按钮、圆角、阴影、缩放边缘和贴靠布局，不自行复刻系统
  窗口管理能力。
- 将顶部抽屉把手、展开面板和顺延提示整体下移，并同步修改下拉手势的纵向触发范围。
- 真实 Windows 窗口检查发现首版自绘产品名与系统标题文字重叠，随后删除重复文字，
  由窗口标题统一显示“托管时间”。
- 再次检查确认标题只出现一次，原生窗口按钮清晰，顶部下拉抽屉仍能正常展开。

## 2026-07-26：移除贯穿标题栏并加入圆形窗口控件

- 根据用户进一步确认，移除上一版 46px 深色标题栏和系统窗口按钮。
- 使用 Avalonia 12 的 `WindowDecorations=None` 创建无系统装饰窗口，透明顶层内
  放置带 14px 圆角和细边缘的连续应用画布。
- 右上角新增最小化、最大化/还原和关闭三个 36px 圆形按钮，并提供可访问名称。
- 在代码隐藏中补充顶部空白区域拖动、窗口八方向 6px 边缘缩放，以及最大化时
  圆角归零、还原时恢复圆角的行为。
- 真实 Windows 窗口操作确认三个圆形按钮可被辅助功能识别，最大化、还原、关闭
  正常，顶部中央抽屉手势仍可展开。

## 2026-07-26：完成七项 UI 精修

- 顶部抽屉加入 280ms 位移与透明度过渡，关闭动画结束后真正隐藏；两个入口使用
  固定等分列、铺满各自区域并居中。
- 管理页返回按钮下移 22px，避开右上角圆形窗口控件。
- 将三个窗口按钮的字符替换为具有真实填充面积的矢量几何，修复线段被填充渲染后
  消失以及图标视觉不居中的问题。
- 当日待办表单删除日期控件；保存时由 `TimeProvider` 取得本地当天。每次进入表单
  都重新使用当前小时和分钟作为默认开始时间。
- 开始时间改成 92px 小时、20px 冒号、92px 分钟的紧凑组；持续时间改成 118px
  整数小时与分钟输入。
- ComboBox 项目增加深色圆角浮层、统一间距、悬停和选中状态。
- 专注色场相位步进从每帧 `0.0065` 提高到 `0.0105`，完整游荡周期约缩短 38%。
- 真实窗口逐页检查了抽屉动画中间态与终态、图标、当日表单、时间下拉和管理页。

## 2026-07-26：用统一磨玻璃选择器替换所有旧展开栏

- 新增 `Controls/GlassPicker`，由自定义触发按钮、Overlay Popup、
  `ExperimentalAcrylicBorder`、纵向 ListBox 和轻触关闭行为组成。
- Acrylic 材质使用 Digger 背景源、低不透明冷灰 tint、14px 圆角和半透明边缘，
  允许后方颜色模糊透出；选中态覆盖 Fluent 内部 ContentPresenter，取消亮蓝底色。
- 当日待办小时/分钟、未来待办年/月/日、管理页未来待办日期、休息模板小时/分钟
  全部迁移到 `GlassPicker`；Desktop 页面不再包含 ComboBox 或 DatePicker。
- `MainViewModel` 将未来日期改成独立的 `FutureYear/FutureMonth/FutureDay`，年份范围
  从本地当前年开始，月份变化时按真实月份天数校正日期。
- `FutureTodoBubbleViewModel` 同步改为年/月/日缓冲编辑，保存时仍向 Application
  提交确定的 `DateOnly`。
- 新增测试验证年/月/日组合后保存的未来日期，并更新未来待办缓冲编辑测试。
- 真实 Release 窗口检查了三个日期触发器和年份 Acrylic 浮层；第二轮又修正了
  Fluent 主题覆盖导致的蓝色选中项。

## 2026-07-26：重做专注主页的 ambient 色场与圆环入口

- 将专注主页从近黑底上的低透明色团改为蓝青到深钴的全画幅 ambient mesh；
  未开始时保持缓慢流动，开始后提高相位速度并增强冰蓝、青绿与淡紫层次。
- 用八个低对比圆环和中央 164px 圆形按钮替换原来的“大圆 + 胶囊”开始控件，
  保留“开始”命令、键盘焦点、可访问名称和按下反馈。
- 专注开始后的主内容删除“正在专注”标签，只保留任务名称和托管确认两句话。
- 通过 Windows `SPI_GETCLIENTAREAANIMATION` 读取减少动态效果设置；启用时冻结
  ambient 色场，只保留静态颜色状态。
- 未改变 `MainViewModel`、排程服务、数据库、导航命令或任何时间显示规则。
- Desktop Release 构建通过，0 个警告、0 个错误；Desktop 18 项测试通过。
- 真实窗口检查确认空状态主页、持续色场、无时间泄露、右上角窗口按钮和顶部
  下拉抽屉均正常。用户数据库当前没有正在执行的事件，因此未写入测试任务来制造
  “开始”状态截图。

## 2026-07-26：加入液态气泡融合式页面转场

- 逐段检查 Neural Flow State 参考视频，提炼出“失焦遮合、光团融合、内容凝结”
  三段节奏，没有采用视频里的高饱和红色、科技网格或文字炫技。
- 新增 `AmbientPageTransitionLayer`，在页面改变时绘制五个蓝青与淡紫径向光团，
  以 560ms 扩散、重叠并自然消失。
- 所有由 `MainViewModel.CurrentPage` 驱动的主页面切换统一使用同一转场：
  新页面从 24% 透明度、12px 模糊和 0.965 缩放开始，以 smoothstep 曲线在原位
  恢复到完全清晰；没有横向或纵向滑动。
- 新增 `MotionPreferences` 集中读取 Windows 减少动画设置，专注色场和页面转场
  共用同一偏好；减少动态效果时禁用气泡层并只保留 140ms 淡入。
- `MainViewModel`、导航命令、数据库和领域逻辑未修改。
- 真实窗口检查了“主页 → 添加类型页 → 主页”的起始态、中间态和结束态；
  转场期间窗口按钮与可访问树保持可用。
- 全套测试通过：Core 46、Application 36、Infrastructure 14、Desktop 18；
  Solution Release 构建 0 个警告、0 个错误。

## 2026-07-26：修复转场残留透明度并统一页面背景

- 定位开始按钮和专注文字变淡的根因：页面转场直接修改整个 `PageContent` 的
  透明度、模糊和缩放，连续操作或开始专注可能发生在容器尚未复位的窗口内。
- 将转场起点从 24% 透明度、12px 模糊和 0.965 缩放调整为 72% 透明度、
  8px 模糊和 0.982 缩放，保留 ambient 凝结感但不再牺牲文字可读性。
- 新增独立 640ms 完成计时器；正常动画完成、计时器兜底以及开始专注三条路径
  都调用同一个复位方法，强制清除 opacity、blur 和 scale。
- `AmbientPageTransitionLayer` 新增显式停止能力，转场被新状态打断时不会残留光团。
- 将 `AmbientFocusSurface` 改为所有主页面常驻背景。添加类型、两个表单和管理页
  现在与主 Dashboard 共用同一蓝青 ambient 画布，不再切换到近黑页面造成闪屏。
- 真实窗口检查确认开始按钮、专注标题和托管文案恢复清晰；添加页与管理页背景连续，
  页面转场完成后内容保持完全不透明。
- Release 完整测试通过 114 项（Core 46、Application 36、Infrastructure 14、
  Desktop 18）；完整 Solution 构建为 0 警告、0 错误。

## 2026-07-26：废弃整页光团转场，改为逐元素上浮

- 使用用户已安装的 FFmpeg 对 13.79 秒 Nyacloud 参考视频以 2fps 全片拆帧，并对
  两段页面切换以 10fps 复查。确认参考节奏是背景稳定、旧内容干净退场，新页面的
  标题、主面板和次级区域依次从下方出现，而不是整页闪光或整体缩放。
- 删除 `AmbientPageTransitionLayer` 及其 XAML 覆盖层，移除整页 opacity、blur、
  scale 动画与完成兜底计时器。
- 新转场只处理当前页面内可见的顶层元素：单项 360ms、间隔 58ms、从下方 18px
  上浮，同时从 0 透明度和 4px 模糊恢复，曲线使用 ease-out-quart。
- 表单的深色承载面板作为第一项参与动画，然后标题、字段和操作依次跟进，避免面板
  突然整块出现产生新的闪光感。
- 连续导航时会先恢复上一组控件原有的透明度、变换和效果；减少动态效果时直接显示，
  不播放上浮。
- 开始专注后的任务文案改为从下方 14px 淡入，与新的上浮方向保持一致。
- `MainViewModel`、页面职责、命令、数据库和领域规则均未修改。
- 真实窗口检查了添加类型页 120ms/250ms 中间态、当日待办表单 120ms/260ms
  中间态及管理页 120ms 中间态；共享 ambient 背景始终连续，没有旧光团闪烁。
- Release 完整测试通过 114 项（Core 46、Application 36、Infrastructure 14、
  Desktop 18）；完整 Solution 构建为 0 警告、0 错误。

## 2026-07-26：统一 Desktop 中文排版层级

- 在 `App.axaml` 建立页面标题、表单标题、区块标题、正文、次要文字、说明文字、
  状态标签和字段标签的语义排版样式。
- 继续使用 Segoe UI Variable/Segoe UI 单字体家族，不引入额外字体文件。
- 将添加类型页、两个添加表单、管理气泡、休息模板、确认层和专注辅助文案接入
  统一字号、字重和行高；当前任务名称仍保持 44px 的专注页唯一主层级。
- 输入框与 `GlassPicker` 统一为 16px，深色背景正文使用更宽松行高；没有改变
  ambient 背景、组件形状、页面职责或逐元素上浮转场。
- 首次构建被仓库 Release 窗口 PID 62048 锁住；确认可执行路径属于本仓库后关闭，
  重跑 Desktop Release 构建为 0 警告、0 错误，Desktop 18 项测试全部通过。
- 真实 Windows 窗口检查了首页空状态、添加类型页和当日待办表单，标题层级、
  辅助文案、字段标签与输入文字均清晰，没有出现裁切或时间泄露。

## 2026-07-26：完成五分钟前提醒的 Core/Application 算法

- 用户要求暂时冻结 UI，先完成剩余算法；本轮未修改任何 Desktop XAML、ViewModel
  或 Windows 通知代码。
- 新增 Core `HandoffReminderPolicy`，返回当前事件、首尾相接的下一事件、提醒时刻、
  当前是否应该提醒和不符合条件的明确原因。
- 规则覆盖：当前事件持续至少五分钟、下一项必须无空档相接、下一项不能是“休息”、
  休息结束后可正常提醒下一任务、提醒窗口结束边界不包含当前事件结束时刻。
- 新增 Application `HandoffReminderService`，从仓库读取计划、使用 `TimeProvider`
  取得当前时刻，并在进程内保证同一当前事件只返回一次待发送提醒。
- 新增 8 项 Core 测试和 2 项 Application 测试；最小验证实测为 Core 54 项、
  Application 38 项全部通过。
- 用户真实使用确认管理页鼠标拖拽完全无效。虽然 Core/Application 重排算法、
  Desktop 事件接线和自动化 ViewModel 测试存在，该功能必须重新标记为“未完成，
  待算法阶段结束后修复并真实验收”，不得继续按已完成功能报告。
- Windows 通知发送、最小化状态、权限/平台降级仍未实现；不可移动冲突中“新输入
  优先成为当前事件”也缺少可跨重启恢复的优先级数据，属于下一项算法设计。

## 2026-07-26：持久化不可移动冲突的当前事件优先序

- 本轮继续冻结 UI，没有修改 Desktop XAML、ViewModel 或拖放事件。
- `ScheduledTodo` 新增非负 `CurrentSelectionPriority`；移动、编辑和自动顺延均保留
  该值。`ScheduledTodoSelector` 在多个事件同时覆盖当前时刻时选择优先序最大的事件，
  从而落实“新输入事件优先成为当前事件”。
- Application 在新增当日待办、规划未来待办和插入休息前读取现有最大值并分配下一个
  序号；序号耗尽时明确拒绝创建，避免整数回绕后静默选错。
- 新增纯 Core `ScheduleConflictDetector`，从持久化时间表重建所有不可移动重叠事件
  对；Application 打开状态现在携带这些冲突，重启后不依赖上一次保存结果的内存状态。
- 通用 SQLite 迁移器由单个 v1 脚本改为按顺序执行迁移列表。当日待办库升级到 v2，
  新增 `current_selection_priority`；旧 v1 事件以 0 安全迁移。未来待办库仍为 v1，
  仍会拒绝它不支持的 v2 数据库。
- 新增 Core 9 项、Application 4 项、Infrastructure 2 项测试。最小验证实测通过：
  Core 63、Application 42、Infrastructure 16。
- Windows 通知、管理页真实鼠标拖拽、未来待办展开 UI 仍不在本轮范围；完整 Release
  验证通过 139 项（Core 63、Application 42、Infrastructure 16、Desktop 18）；
  Solution 格式检查无差异，Release 构建为 0 个警告、0 个错误。

## 2026-07-26：修复管理页真实鼠标拖拽

- 根因是任务和休息拖拽源使用 `Button`，同时在 `PointerPressed` 当下启动系统拖放；
  按钮点击、指针捕获与拖放捕获互相竞争，自动化 ViewModel 测试无法覆盖该问题。
- 任务把手和休息模板改为独立拖拽表面：按下时捕获指针，移动超过 6px 后释放捕获并
  启动 Avalonia 系统拖放。拖动时提升整个任务气泡，而不是只缩放把手。
- 所有 DragOver 和 Drop 明确设置处理状态与 Move/Copy/None；Application 拒绝非法
  位置时不再向系统伪报落位成功。
- 原队尾区域现在同时接受任务移动和休息复制；休息模板保留独立“加入队尾”按钮作为
  清晰的键盘等价路径。
- Desktop 支持 `ATTENTION_GUARDIAN_DATA_DIRECTORY` 本地目录覆盖，真实验收可使用
  隔离数据库而不向用户正式数据库写入样例。
- 新增“首项移动到队尾”回归测试；全套 Release 测试 140 项通过（Core 63、
  Application 42、Infrastructure 16、Desktop 19），格式检查无差异，Solution
  构建为 0 个警告、0 个错误。
- 使用隔离真实 Windows 窗口验证：创建“拖拽验证 A”，加入休息，任务拖到队尾后顺序
  变为“休息、拖拽验证 A”，再拖回首位恢复；休息模板拖到首项后成功复制到指定位置。
- 隔离测试窗口已关闭。临时数据库目录的自动清理被执行策略拦截，目录仍位于系统
  Temp，不包含用户正式待办；其绝对路径记录在本轮教学章节。

## 2026-07-27：接入未来待办“一天后 / 两天后”快捷日期

- 未来待办添加页新增等宽“一天后”和“两天后”按钮，并使用现有冰蓝强调色表达
  当前选中项，不改变已经确认的页面背景、窗口外壳或表单布局语言。
- ViewModel 新增互斥的相对日期状态与两个命令。快捷选择会把年/月/日同步到对应的
  确定日期；用户手动调整具体日期后会退出快捷选择。
- 保存时明确只向 Application 提交 `DaysFromToday` 或 `ScheduledDate`，避免相对
  日期与具体日期同时生效。
- 新增 3 项 Desktop 测试，覆盖一天后、两天后以及手动日期覆盖快捷选择。Desktop
  最小 Release 测试 22 项通过。
- 使用隔离数据目录启动真实 Windows 窗口：默认“一天后”显示 2026-07-28；点击
  “两天后”后选中态切换，具体日期同步为 2026-07-29。验收窗口已关闭。
- 最终格式检查无差异；全套 Release 测试 143 项通过（Core 63、Application 42、
  Infrastructure 16、Desktop 22）；完整 Release 构建为 0 个警告、0 个错误。

## 2026-07-27：接通不可移动冲突提醒并清理已完成任务

- 修复首页状态接线遗漏：`ReloadOpeningAsync` 现在消费 Application 返回的
  `MandatoryConflicts`，刷新可观察冲突集合并通知 `HasConflicts`。
- 冲突提醒展开区增加冲突双方名称，仍只保留一个“进入管理”按钮。
- `ScheduleManagementService` 注入 `TimeProvider`；进入管理页时将
  `End <= now` 的任务判为完成，以一次整表事务保存剩余活动排程。
- 打开状态的冲突检测只包含 `End > now` 的任务，避免历史冲突持续提醒。
- 新增 Application 2 项、Desktop 2 项回归测试，覆盖完成边界、持久化清理、
  已完成冲突排除以及 Desktop 冲突集合接线。
- 隔离真实窗口创建一条已完成任务及“强制冲突 A / B”：首页正确显示折叠冲突提醒，
  展开后显示双方名称；进入管理页后只保留两个活动冲突事件，已完成任务消失。
- 最终格式检查无差异；全套 Release 测试 147 项通过（Core 63、Application 44、
  Infrastructure 16、Desktop 24）；完整 Release 构建为 0 个警告、0 个错误。

## 2026-07-27：完成 ambient 组件、表单对齐与管理气泡动效精修

- 首页不可移动冲突提醒改为居中的窄幅折叠组件：只保留小型语义红点、短标题和展开
  入口，展开后才显示冲突名称与唯一“进入管理”按钮；提醒与中央专注区域保留独立
  呼吸空间。
- `GlassPicker` 将磨砂材质层与文字内容层拆分，降低材质层透明度并保留细边缘、短阴影、
  清楚选中态和键盘可访问性；时间与日期触发器的数值改为真正居中。
- 新增复用 `GlassNumberStepper`，使用“减号—可直接编辑数值—加号”的连续胶囊结构，
  支持按钮步进、键盘直接输入、回车或失焦提交以及最小/最大值约束。添加页和管理页
  的旧 `NumericUpDown` 均已替换。
- 当日待办的开始时间与持续时间统一使用相同行高、字段标签行和 44px 控件行；小时、
  冒号、分钟改为紧凑连续组。未来待办快捷日期及年/月/日选择器改为固定、均衡的紧凑
  网格，管理页未来待办日期编辑同步采用相同间距。
- 管理页定时与未来气泡的编辑区改为 240ms smoothstep ease-in-out 高度与透明度动画。
  下方内容随气泡实时重排并保持既有间距；保存失败时编辑区恢复展开；Windows
  “减少动态效果”开启时直接切换，不播放位移动画。
- 未修改排程、保存、冲突判断、数据库、导航和 ambient 背景。真实隔离窗口检查了
  胶囊步进及直接编辑、时间与日期对齐、选择浮层、折叠/展开冲突提醒、管理气泡展开
  中间态与收起状态。
- 格式检查无差异；全套 Release 测试 147 项通过，完整 Release 构建为 0 个警告、
  0 个错误。

## 2026-07-27：移除文字编辑时的黑色强制聚焦背景

- 所有 Desktop `TextBox` 的 Fluent 内层背景统一改为透明；名称输入、管理页事件名称
  和未来待办名称在静止、悬停、聚焦与选中文字时均不再出现纯黑填充。
- 文本焦点改用 180ms 轻微上浮与缩放、冰蓝边缘、短距离冷色阴影和冰蓝光标表达，
  文字选区使用低透明冰蓝，不再使用黑色选区。
- `GlassNumberStepper` 的中间可编辑数值保持完全透明；获得焦点时由整个连续胶囊
  轻微抬升并增强边缘和阴影，不再在数字后生成独立黑块。
- Desktop 24 项及完整 147 项 Release 测试通过；格式检查无差异，完整 Solution
  Release 构建为 0 个警告、0 个错误。隔离真实窗口已验证当日待办名称输入和持续
  小时直接编辑的静止与聚焦状态。

## 2026-07-27：统一输入对齐、玻璃实体感与 GPU 流体专注背景

- 全局文字输入改为垂直居中内容，修复 44px 名称框内光标偏上的问题。
- `GlassPicker` 的触发按钮铺满整个玻璃表面，所有开始时间和未来日期选择器共享相同
  命中区域；Picker 与 `GlassNumberStepper` 的短横高光替换为宽幅、纵向衰减的镜面
  渐变高光。
- `AmbientFocusSurface` 改用 Skia runtime shader，通过分形噪声与域扭曲生成连续
  蓝青流体。未开始时对比更高、流动更快；开始时产生一次速度脉冲，再平滑进入低
  对比慢速专注态；当前任务标识变化时在专注态产生同类脉冲。
- shader 刷新随 Windows 窗口可见状态启停；隐藏、最小化、关闭或脱离视觉树时停止
  计时器，减少动态效果时冻结。绘制能力不可用时保留下层静态渐变。
- 开始按钮增加柔和光晕与更清晰的实体边缘，外围八个圆环扩大、分散并降低透明度。
- 使用隔离数据目录启动真实 Windows 窗口，验证光标居中、Picker 整面命中、宽幅
  高光、开始入口，以及开始前、100ms 转换中和 2.2s 稳定专注态三种背景状态。
- Desktop 24 项及完整 147 项 Release 测试通过；格式检查无差异，完整 Solution
  Release 构建为 0 个警告、0 个错误。

## 2026-07-27：将玻璃高光收回表面并延长流体减速尾段

- 移除 `GlassPicker` 与 `GlassNumberStepper` 内部独立的 14px 亮白覆盖层，修复子层
  没有按父级圆角裁切、两端越出胶囊边界的问题。
- 高光改为玻璃外壳自身的低对比蓝灰纵向渐变：顶部只有轻微环境提亮，中部回到既有
  冷灰玻璃，底部略微加深。光感现在属于材质本身，不再像附着在背景外的亮条。
- 保持流体脉冲初始速度不变，将衰减速率从每秒 `1.15` 降至 `0.74`，使完整减速尾段
  从约 0.87 秒延长到约 1.35 秒。
- 隔离真实 Windows 窗口确认 Picker 与 Stepper 的环境光完全收在圆角边界内；在
  点击开始后的 100ms、700ms 和 1400ms 分别采样，背景位移连续并平滑回落。
- 完整 147 项 Release 测试通过，格式检查无差异，完整 Solution Release 构建为
  0 个警告、0 个错误。

## 2026-07-27：实现 Guardian 手写流体启动开场

- 新增 `LaunchMotionSurface` Desktop 绘图控件，使用独立 Skia runtime shader 从
  纯黑中央光点生成蓝青、深钴和少量紫色的有机扩散流体。
- 使用 Windows 标准 `Segoe Script` 连笔字形，从左向右揭示 `Guardian`，同时绘制
  低强度冰蓝 soft glow 与移动笔尖光点；艺术字没有进入应用普通排版。
- 正式时间轴为 4.6 秒：光点与流体先出现，文字随后写入、短暂停留并淡出，最后约
  12% 时段将整个开场层交叉淡化到现有 `AmbientFocusSurface`。
- 动画从窗口第一次 `Activated` 开始，避免窗口创建过程提前消耗可见时间；数据仍在
  `Opened` 中并行初始化。窗口最小化时暂停，恢复后继续；减少动态效果时直接结束。
- 首次真实验收发现 shader 中双线性噪声的外层 `mix` 缺少第三个参数，导致运行时
  静默只显示黑场与文字。修正后新增 `LaunchShader_Compiles` 测试，直接检查 SkSL
  编译结果，防止同类回归。
- 使用临时 4 倍慢速构建目测字迹、流体和退场阶段，确认后恢复正式 4.6 秒并重新构建；
  正式版本真实启动后正常进入首页，没有残留覆盖层或交互阻塞。
- Desktop 25 项及完整 148 项 Release 测试通过；格式检查无差异，完整 Solution
  Release 构建为 0 个警告、0 个错误。

## 2026-07-27：延长启动光点并改为真实 SVG 路径书写

- 正式启动时间轴从 4.6 秒调整为 5.6 秒。中央光点先在纯黑背景独立存在约 1 秒，
  到第 1 秒后彩色流体才开始从中心扩散；后续流体、字迹、退场和首页交接保持原方向。
- 删除 `Segoe Script` 字体绘制、横向裁切和透明度模拟书写的实现。`Guardian` 现在由
  五段手工定义的单线 SVG/Bezier 路径组成，通过 `SKPathMeasure.GetSegment` 按累计
  路径长度逐段生长。
- 线条使用圆形笔端、圆形连接、4.2 单位主线和低透明模糊光层；移动笔尖直接取当前
  路径切点，不再根据文字宽度猜测位置。
- 慢速真实窗口验收确认路径会从 `G` 的第一笔连续生长。首轮发现 `d` 的高杆位于字腹
  之后，容易读成额外的 `l`；随后把 `d` 改为先上行、再回落闭合字腹并连接 `i`。
- 新增矢量路径解析测试，与既有 shader 编译测试共同保护开场资产。Desktop 26 项及
  完整 149 项 Release 测试通过；格式检查无差异，完整 Solution Release 构建为
  0 个警告、0 个错误。

## 2026-07-27：按发布参考图重绘 Guardian 品牌字标

- 只替换启动动画的五段 Bezier 品牌路径与绘制尺寸，没有修改光点、流体、5.6 秒
  时间轴、交叉淡化、页面、业务逻辑或其他 UI。
- 新字标把视图框扩展为 1000×300：`G` 使用大幅倾斜上弧和向下回旋长尾，小写部分
  压低并紧凑连写，`d` 保留明显高杆，最终 `n` 向右拉出长收笔。
- 主线从 4.2 调细为 3.6 逻辑单位，环境光层同步从 8 调为 7，使成品更接近参考图的
  纤细书法质感，同时继续使用圆形笔端、真实路径长度和移动笔尖。
- 五段路径解析与 shader 编译相关测试通过；使用临时四倍慢速真实窗口检查了 `G`
  起笔、下行回环、连续 `Gua…` 和 `d` 高杆，随后恢复正式 5.6 秒。

## 2026-07-27：建立待确认的单线 Guardian SVG 字标稿

- 根据更严格的品牌字形说明，在 `docs/design/guardian-wordmark.svg` 新建独立静态
  SVG 稿；本阶段没有替换应用内已有字标，也没有修改启动动画。
- `Guardian` 的主体由一条自定义 Bezier 路径连续构成，只有 `i` 的点采用第二条
  独立路径；没有使用字体、SVG `text`、遮罩或横向揭示。
- 主路径与点分别声明稳定的 `pathLength`，为用户确认字形后使用
  `stroke-dasharray` / `stroke-dashoffset` 接入真实书写动画做好资产边界。
- 使用本地 SVG 渲染器输出 `docs/design/guardian-wordmark-preview.png`，检查大写
  `G` 的开口回环、连续小写、`d` 高杆、最终 `n` 收笔以及画布边界。
- 当前状态是“静态几何待用户确认”；在确认之前不得把该稿接入 v0.1.0 启动画面。

## 2026-07-27：按参考中心线重建 Guardian SVG

- 废弃上一稿依靠字形特征推测的路径，改为直接测量用户参考图中实际发光笔迹的
  中心线；参考图现在被视为严格几何基准，而不是风格提示。
- 将交叉、回笔和字母连接拆成 28 段平滑 Bezier 路径，并单独保留 `i` 的点。所有
  可见内容仍然是固定线宽矢量路径，没有字体、SVG `text`、遮罩或透明度揭示。
- 重点恢复参考图中 `a` 的闭合字腹与右侧回笔、`r` 的短竖和肩部、`d` 的独立字腹
  与高杆，避免为了强行使用单一 DOM path 而产生额外交叉。
- 输出 `guardian-wordmark-comparison.png`，同时展示参考、SVG 与像素叠加结果。
  2px 容差下参考主笔迹覆盖约 91.7%，SVG 约 99.8% 位于参考笔迹附近；3px 容差下
  SVG 路径全部位于参考范围内。剩余差异主要来自原图发光、抗锯齿和阈值宽度。
- 本阶段仍未修改 `LaunchMotionSurface` 或启动时间轴；等待用户确认静态几何后再
  设计每段路径的真实书写顺序。

## 2026-07-27：将获批 Guardian SVG 接入正式启动开场

- `AttentionGuardian.Desktop` 现在把 `docs/design/guardian-wordmark.svg` 作为
  程序集嵌入资源读取；软件与设计文档共用同一份路径数据，不再维护容易漂移的代码内
  手抄版本。
- `LaunchMotionSurface` 从 SVG 加载 29 段路径，并按参考分镜组合为 40 个实际
  书写步骤：`G` 上弧与大弧、下方回环、回到连接点、小写连续书写、`i` 落点以及
  `n` 向右收笔。
- 新增反向路径生长支持。闭环或竖笔需要回笔时，动画从路径末端向起点增加真实
  `SKPathMeasure` 片段；没有引入遮罩、横向揭示、字体或整词透明度模拟。
- 字标使用 SVG 的 940×410 viewBox 居中缩放，保留既有线宽、soft glow、流体、
  5.6 秒时间轴、1 秒光点等待、交叉淡化和减少动态效果降级。
- Desktop 测试新增 SVG 几何数量、`G` 起笔、下方回环、`i` 落点位置及最终收笔
  顺序断言。Desktop 27 项、完整 150 项 Release 测试通过；格式检查无差异，
  Solution Release 构建 0 警告、0 错误。
- 使用临时四倍慢速真实窗口检查 `Gua` 中间态和 `Guardian` 末段，随后恢复正式
  5.6 秒并再次实际启动；正式中间态显示路径正在书写，结束后正常进入主界面。
## 2026-07-27：统一首页提醒并修复未来待办规划入口

- 到期未来待办不再跳转事件管理页。用户展开提醒后可对具体事项点击“规划”，直接进入当日待办表单，名称自动预填；只有当日待办保存成功后，原未来待办才标记为已规划。
- 到期未来待办、不可移动事件冲突、普通顺延提示和跨日顺延提示统一为 430px 居中窄幅提醒表面，以不可移动事件冲突提醒的低层级玻璃材质为基准。
- 到期待办与冲突提醒移除向下箭头，改为双击整个提醒表面展开；具体操作按钮仍保持单击，避免单击浏览时强迫展开。
- 当前任务标题增加真实行盒高度和下方内部留白，修复 `g`、`y`、`p`、`q`、`j` 等下行字母被裁切。
- Desktop 新增 2 项规划流程测试，共 29 项；完整 Solution 共 152 项 Release 测试通过，格式检查无差异，Release 构建 0 警告、0 错误。
## 2026-07-27：建立未接入软件的 Guardian 平滑候选稿

- 新增独立候选资产 `docs/design/guardian-wordmark-smoothing-candidate.svg` 及其 PNG 预览；正式嵌入资源 `guardian-wordmark.svg` 与 `LaunchMotionSurface` 均未修改。
- 候选稿移除 `G` 下方回笔中起点、末端重复经过同一短段的结构，改为一条开放且连续的 cubic Bézier 回笔。
- 小写主线保留既有比例与字母位置，将短直线全部替换为曲线，并合并 `u` 的主要连接段，降低圆形端帽在路径交界处叠加形成的折点感。
- 候选预览采用正式启动字标的 3.6 主线宽度和 7 单位环境光层，等待用户视觉确认；未经确认不得替换正式 SVG 或修改动画笔序。
## 2026-07-27：Guardian 候选稿进入严格连笔校准

- 本轮继续只修改 `guardian-wordmark-smoothing-candidate.svg`，正式 `guardian-wordmark.svg`、Desktop 嵌入资源和动画代码保持不变。
- 保持 940×410 viewBox、G 外轮廓、小写字高、字宽、3.6 主线宽度、7 单位环境光层及尾线终点不变。
- 将 `a→r`、`d→i` 和第二组 `a→r→n` 的原独立路径按既有中心线合并；候选主组由 26 段收敛为 12 段，减少字母交界处的圆端帽叠加。
- `r→d` 经多轮参考图放大校准，最终采用 `(823,465)` 到 `(842,476)` 的短 Bézier 对角过渡，控制点为 `(829,467)`、`(836,472)`，没有扩大 `d` 或增加装饰拱线。
- 候选 SVG XML 可解析、无 `L` 直线命令，并已重新渲染 PNG 等待用户确认。

## 2026-07-27：用 final.svg 重做启动字标轮廓动画

- `AttentionGuardian.Desktop` 不再嵌入旧 `guardian-wordmark.svg`，改为直接嵌入
  `docs/design/final.svg`；源 SVG 没有经过格式化、复制、简化或路径修改。
- `LaunchMotionSurface` 读取终稿中的唯一填充 path，并通过 `SKPathMeasure` 遍历其
  全部 contour。动画先由一个冰蓝光点沿 contour 移动并留下双层 Stroke Outline：
  细亮主线负责清楚轮廓，低透明模糊层负责 ambient glow。
- Outline 在时间轴 28%–72% 之间完成；只有完成后，实体填充才在 72%–83% 之间
  平滑加入。填充完成后仍保留轮廓光效，随后沿用原有交叉淡化进入主界面。
- 1 秒黑场光点、GPU 流体从中心扩散、5.6 秒总时长、窗口最小化暂停、恢复继续和
  Windows 减少动态效果时跳过等机制没有改变。
- 新增终稿不可变测试：嵌入资源必须只有一个 path，并且 SHA-256 必须保持为
  `7140DE6A319D7AE59599E16E28B78AC1586BC4045190137692D0C0A9042ABE72`。
- Desktop 29 项及完整 Solution 152 项 Debug 测试通过。第一次 Release 相关测试
  被用户原有验收窗口锁住输出 DLL，因此改用未被占用的 Debug 输出完成验证；没有
  关闭用户窗口。隔离真实窗口确认动画结束后能正常交接主界面。

## 2026-07-27：校准轮廓顺序并放缓启动交接

- 保持 `docs/design/final.svg` 字节和路径完全不变；`LaunchMotionSurface` 在内存中
  将原 contour 切为短轮廓片段，并按参考图映射为八个空间阶段：`G` 主体、`G`
  下方回环，再依次推进小写字母。
- 总时长由 5.6 秒延长至 7.2 秒。实体填充完成后增加一次幅度低于既有 ambient glow
  的微闪，并推迟退场区间，为完整 Guardian 留出稳定阅读时间。
- 通过终稿的实际几何 bounds 计算缩放和平移，避免 1536×1024 viewBox 的透明留白
  影响水平或垂直居中；新增八阶段和双向居中的回归测试。
- 开场期间右上角最小化、最大化、关闭按钮为透明且不可命中；开场完成后以 520ms
  淡入。减少动态效果时仍直接进入已初始化主界面。
- 找到用户实际运行的是
  `tests/AttentionGuardian.Desktop.Tests/bin/Release/net10.0/AttentionGuardian.Desktop.exe`。
  关闭锁定该输出的旧进程后重新执行 Release 全量测试，使该可执行文件同步到最新
  动画。Desktop 31 项、完整 Solution 154 项测试通过。

## 2026-07-27：重建 G 的真实书写顺序

- 诊断确认上一版以“距离上一碎片最近”决定次序，导致 G 的 C 形尚未完成时先跳到
  交叉结构，随后又回头补 C 的开头；视觉卡顿来自排序模型，而不是 GPU 性能。
- 保持 `final.svg` 完全不变，按其实际 contour 长度确定 G 的参数区间。第一阶段
  连续完成 C 形的两侧轮廓，第二阶段才依次显示连接段、下方外回环、内回环和出口。
- 后续字母不再使用最近邻搜索，改为在每个字母阶段内沿原 contour 参数稳定推进，
  避免路径在相邻交叉点之间随机跳转。
- 增加首阶段最大衔接距离与全轮廓覆盖测试：C 阶段的内部衔接必须保持在 12 个 SVG
  单位以内，所有 contour 的累计覆盖率必须为 100%。
- 填充后的 breath glow 从几乎不可察觉调整为低幅、约 0.94 秒的柔和呼吸；主轮廓
  层级和 7.2 秒总时长不变。
- 四倍慢速真实 Windows 关键帧确认 C 完成前没有交叉段，C 完成后下方回环才开始。
  验收后已恢复正式时长并删除临时路径探针。

## 2026-07-27：统一 v0.1.0 文档基线并发现完成历史差异

- 完整阅读除 `teaching.md` 外的仓库 Markdown，按用户确认的 v0.1.0 验收标准统一
  README、产品、设计、架构、路线图、交接、发布与社区指引。
- 明确区分“产品与交互已获用户确认”“代码满足全部数据验收标准”和“已经对外发布”：
  当前只成立第一项，正式打包、Tag 和 GitHub Release 尚未执行。
- 代码核对发现 `ScheduleManagementService.LoadAsync` 会过滤已结束任务，
  `SqliteScheduledTodoRepository.ReplaceAllAsync` 又先执行
  `DELETE FROM scheduled_todo`；当前 schema 没有完成状态或完成时刻。
- 因此“已完成事件保留记录并标记完成”尚未实现，被列为 v0.1.0 发布前阻塞差异。
  本轮只更新文档，没有修改业务代码、数据库或测试，也没有提交、推送或发布。

## 2026-07-27：修复提醒一致性、完成生命周期并接入 Windows 提醒

- 四类首页提醒统一使用 `AlertGlass`：430px 宽、相同低透明冷灰材质、边缘、圆角、
  短阴影、内边距和文字层级。跨日提醒移除实心黄色底，仅保留小型黄色语义点。
- 当日 SQLite schema 从 v2 迁移到 v3，新增 `active`、`completed`、`deleted`
  生命周期和 `completed_utc`。已结束事项标记完成并从活动查询隐藏，不再物理删除。
- `ReplaceAllAsync` 不再清空整张表；缺失活动项标记删除，新计划使用 upsert，
  完成历史保持不变。未来待办规划继续使用既有 `planned` 状态。
- Desktop 接入 Windows App SDK 2.3.1；每五秒调用既有提醒资格服务，符合五分钟、
  无空档、非休息和单次去重规则时发送系统通知。
- 当前任务刷新间隔缩短为五秒；相邻任务切换时保持专注状态，标题执行克制的
  14px 淡入位移，减少动态效果时即时切换。
- 顶部抽屉新增本地验收表单。九类固定内存场景可逐项运行，不读取或修改真实数据库；
  页面另提供 Windows 提醒测试按钮和发送状态反馈。
- 自动化新增完成记录持久化、验收场景和任务切换测试。Release 实测通过：
  Core 63、Application 45、Infrastructure 17、Desktop 33，共 158 项；完整 Release
  构建 0 警告、0 错误。
- 隔离真实窗口确认验收页正常打开，九项均显示“符合预期”。Windows API 发送端已
  接入，但系统弹窗是否显示还受 Windows 通知设置和勿扰模式影响；最小化状态与通知
  设置矩阵仍需在发布前复查。

## 2026-07-27：生成 v0.1.0 Windows x64 正式安装包

- Release 正常界面隐藏内部“验收检查”入口；Debug 或设置
  `ATTENTION_GUARDIAN_ENABLE_ACCEPTANCE=1` 时仍可用于开发诊断。
- 桌面项目版本固定为 `0.1.0`，文件版本为 `0.1.0.0`，补齐产品与版权元数据。
- 158 项 Release 测试、格式检查和 Release 构建通过，构建为 0 警告、0 错误。
- 使用 .NET 10 self-contained `win-x64` 发布，并用 Inno Setup 6.7.3 生成每用户安装器。
  安装器同时部署微软签名的 Windows App Runtime 2.3.1 x64。
- 安装器在当前 Windows 用户环境静默安装成功；已安装程序持续运行超过五秒，版本信息、
  安装目录和 Windows 卸载注册项均正确。
- 产物为 `artifacts/v0.1.0/AttentionGuardian-0.1.0-win-x64-setup.exe`，大小
  185,170,960 字节，SHA-256 为
  `04FAAF9B73C89C35E280F2B88164D8D3073C1688CA757A8755641C6F72C73646`。
- 当前安装包没有项目方 Authenticode 签名，且尚未在全新 Windows 虚拟机中完成安装
  矩阵验证。没有创建 Git 提交、Tag、GitHub Release，也没有对外发布。

## 2026-07-27：修复 Release 顶部抽屉残留空列并重打包

- 根因不是外框的 360px 宽度，而是验收入口隐藏后，三栏网格的第三个星号列仍参与
  布局，导致正式版右侧保留三分之一空白。
- Release 改用独立的两等分网格；开发诊断模式继续使用独立三等分网格。颜色、材质、
  高度、外框宽度和 280ms 动效均未改变。
- 158 项 Release 测试通过，Release 构建 0 警告、0 错误，安装包重新生成并安装。
- 真实已安装窗口确认“添加事件”和“事件管理”各占胶囊一半，右侧不再留空。
- 新安装包大小为 185,178,945 字节，SHA-256 为
  `265ED73904A7955EB218AADFA2CC033D6A20415368CF687C2F5E22F538E1E6D2`。

## 2026-07-28：清除测试数据、接入正式图标并生成隐私洁净包

- 查明安装后出现旧事件的原因是应用继续读取 `%LOCALAPPDATA%\AttentionGuardian`
  中的持久化数据库，不是安装器包含数据库。清理前当日表 4 条、未来表 1 条；两张
  业务表清空并压缩后均为 0 条。
- 仓库源文件没有数据库或日志，但教学历史中四条临时验证路径包含本机用户名；按本次
  明确隐私要求将用户名部分匿名化为 `%USERPROFILE%`。
- 新增 `Directory.Build.props`，Release 使用确定性构建和 `PathMap`，避免自研 DLL
  的 CodeView 元数据暴露本机源码绝对路径。
- 将 `design/Attention_Guardian_icon_high.ico` 复制为 Desktop 正式资源，并同时用于
  应用 EXE、Inno Setup 安装器和卸载入口；源文件与应用资源 SHA-256 一致。
- 旧安装包、手工验收数据库以及 `bin/obj` 移出 Guardian 到系统临时隔离目录。最终
  Guardian 扫描不含数据库、日志、邮箱格式、本机用户名或用户目录绝对路径。
- 158 项 Release 测试通过，格式检查通过，构建 0 警告、0 错误。新安装器安装启动后，
  当日表和未来表仍各为 0 条。
- 公开洁净产物：
  `artifacts/v0.1.0-public-clean/AttentionGuardian-0.1.0-win-x64-setup.exe`
- 大小 185,138,855 字节（176.56 MiB），SHA-256：
  `AB8E7D6BBD5CCD894C48B576C970276794D894CC1AF056EBD6C2C575DE0140F7`。
- 尚未创建首次 Git 提交、GitHub remote、Tag 或 Release。

## 2026-07-28：首次 GitHub Release 正式公开

- 使用 GitHub 隐私邮箱完成首次提交，提交为
  `58e582e63be980f65d3d8da08b45ab9e63051d00`。
- 连接公开仓库 `NOsauceburGer/Attention_Guardian`，推送 `main`，远程提交与本地一致。
- 创建并推送注释 Tag `v0.1.0`，Tag 指向首次发布提交。
- 先创建 Draft Release 并上传公开洁净安装器；GitHub 返回附件大小 185,138,855
  字节、digest
  `sha256:ab8e7d6bbd5ccd894c48b576c970276794d894cc1af056ebd6c2c575de0140f7`，
  与本地校验一致后才正式公开。
- Release 地址：
  `https://github.com/NOsauceburGer/Attention_Guardian/releases/tag/v0.1.0`
- 发布后发现 README、Roadmap 等仍有发布前状态措辞；不移动已公开 Tag，而是用后续
  文档提交同步 `main`，保持已发布版本不可变。

## 2026-07-29：macOS 首轮可运行迁移

- 将 Avalonia Desktop 与 Desktop 测试项目从 Windows 专属目标框架改为 `net10.0`，移除
  `Microsoft.WindowsAppSDK`、Windows manifest、`win-x64` 运行时标识和 Windows EXE
  输出类型；Core、Application 和 SQLite Infrastructure 保持不变。
- Windows 通知发送端替换为显式的不可用适配器：保留提醒资格、五秒调度和应用内状态，
  但暂不发送 macOS 原生通知。macOS 原生 `UNUserNotificationCenter` 适配、权限请求、
  `.icns` 图标、`.app` 签名和 DMG/PKG 打包均留待下一阶段。
- 移除了 `user32.dll` 的减少动态效果 P/Invoke；在接入 macOS 辅助功能偏好前，动效保持
  启用。系统通知测试入口改用平台中性文案。
- 在 Apple Silicon macOS、.NET SDK 10.0.302 环境完成依赖还原与 Debug 构建：0 警告、
  0 错误。自动化测试通过 Core 63、Application 45、Infrastructure 17、Desktop 33，
  共 158 项。

## 2026-07-29：确认 Apple 客户端改用 SwiftUI

- 用户确认 Windows 已发布客户端继续使用 Avalonia；macOS 与 iOS 的客户端界面全部改用
  SwiftUI。此前让 Avalonia 在 macOS 启动成功的工作保留为历史验证记录，不再作为 Apple
  平台的长期实现或发布方向。
- 本次只更新架构、产品决策、路线图、交接和项目说明；没有创建 SwiftUI 工程、没有迁移
  功能、没有修改现有 C# 业务逻辑、数据库 schema、测试或 Windows 客户端。
- 尚未决定 SwiftUI 与现有 C# 领域/持久化代码的共享边界。开始 Apple 平台开发前，必须
  先明确跨语言规则实现、SQLite 数据所有权与迁移、并发、备份和隐私方案，并据此建立
  可验证的 macOS/iOS 验收计划。

## 2026-07-29：确认 Swift Core 与跨平台规则合同

- 用户确认 Apple 平台不通过 MCP、常驻 .NET 进程或跨语言桥接调用现有 C# Core，而是为
  macOS/iOS 共同重写 Swift `AttentionGuardianDomain`。Windows C# Core 保持不变。
- 新增 `docs/CROSS_PLATFORM_RULES.md`，把时间边界、当日/未来待办、生命周期、当前事项、
  自动顺延、强制冲突、管理重排、休息模板、规划幂等性与五分钟前提醒资格收敛为平台无关
  合同，并列出 C# 与 Swift 必须共用的测试向量范围。
- 本次只确认架构和规格；没有创建 Swift 工程、没有改写 C# Core、没有设计 Apple SQLite
  schema，也没有运行构建或测试。

## 2026-07-29：建立 Apple SwiftUI 设计语言并与 Windows 基线分离

- 完整保留 Windows Avalonia v0.1.0 的既有设计说明，并在 `DESIGN.md` 前部新增独立 Apple
  SwiftUI 规范，明确两套客户端共享品牌与信息架构、不共享 View 和平台交互实现。
- Apple 背景由 Windows GPU shader 改为同色调 Mesh Gradient；界面采用 SwiftUI 原生
  Material（第一版 `thinMaterial`）、四级 Depth、Capsule Button/Stepper、Soft Shadow、
  Specular Highlight 和全交互 Micro Interaction。
- Apple 顶部抽屉改为底部隐藏抽屉与 Floating Capsule Tab Bar。根据用户提供的 3.73 秒、
  30fps 参考视频，规范了独立 Liquid Selection Lens 的前缘拉伸、后缘跟进、有限折射、
  一次轻微 overshoot 与 `matchedGeometryEffect` 连续迁移；强烈彩色像差不纳入 calm 设计。
- Apple Motion 改用阻尼充分的 SwiftUI Spring；Reduce Motion、Reduce Transparency、
  Increase Contrast、Dynamic Type、VoiceOver、键盘、指针和触控降级均进入设计门槛。
- 本轮只更新设计与决策文档，没有创建 SwiftUI View、没有修改 Windows Avalonia 代码，
  也没有运行产品构建或测试。

## 2026-07-29：分层项目文档并重建当前交接

- 保留所有既有文档路径，没有提前建立空的 Apple/Windows/Shared 文档目录。新增
  `docs/superpowers/specs/2026-07-29-document-layering-design.md` 与
  `docs/superpowers/plans/2026-07-29-document-layering.md`，分别保存获批分层设计和执行计划。
- 按用户明确要求整体替换旧 `HANDOFF.md`：从 520 行历史堆叠收敛为 129 行当前快照，只保留
  已确认事实、尚未开始、唯一下一任务、后续顺序、禁止事项、必读入口、验证和工作区说明。
  Windows UI 迭代、候选字标和发布过程继续由本文件、产品决策、CHANGELOG 和 Release 保存。
- `AGENTS.md` 新增文档职责，修正 Apple Swift 分层边界，并把已完成的 Windows 发布准备优先级
  替换为唯一当前主线：版本化共享 JSON 测试向量 → C# 读取验证 → Swift Domain。
- `ARCHITECTURE.md` 增加 Windows 已发布架构、Apple 尚未创建的目标架构和共同合同入口；
  `ROADMAP.md` 把 Apple 工作拆为六个有序门槛；`README.md` 明确 Apple 尚无可运行客户端；
  `SETUP.md` 限定为真实存在的 Windows/C# 命令，并记录当前 macOS 目录没有 `.git`。
- `PRODUCT_DECISIONS.md` 追加 PD-051，明确 `HANDOFF.md` 是可替换快照，历史只进入追加式记录。
- 文档验证实测：`HANDOFF.md` 为 129 行，旧交接章节标签不存在；根入口都指向共享 JSON
  测试向量；Apple/Windows 平台归属明确；更新入口没有 TBD/TODO；所有链接目标存在；
  深度三层内没有 `.swift`、`Package.swift` 或 `.json` 文件。
- 当前目录未检测到 `.git`，因此没有创建 worktree、提交、分支或推送。本轮没有修改 C#、
  Avalonia、SQLite 或发布产物，也没有创建 Swift 工程或测试向量；因此未运行产品构建或测试。
## 2026-07-29：补充文档分层后的完整测试证据

- 文档分层完成后的首次 `dotnet test AttentionGuardian.slnx --configuration Release --no-restore`
  尝试受到当前 macOS 沙箱限制：MSBuild 创建本地命名管道时返回
  `System.Net.Sockets.SocketException (13): Permission denied`。这属于测试进程未能启动，
  不是产品测试失败。
- 获得沙箱外执行许可后，以相同命令重新运行 Release 全量测试并成功完成：
  Core 63、Application 45、Infrastructure 17、Desktop 33，共 158 项通过，0 项失败，
  0 项跳过。
- 本条是对上一章节“本轮没有运行产品构建或测试”的时序更正：该描述在章节写入时属实，
  但随后为了完成交接验证补跑了完整测试。保留旧文字以遵守本文件追加式历史规则。

## 2026-07-29：建立第一批共享 JSON 测试向量并接入 C# 验证

- 新增 `test-vectors/README.md` 与 `test-vectors/v1`，定义 `schemaVersion = 1` 的公共
  envelope、固定小写 UUID、带显式偏移的 ISO 8601 时刻、确定日期、整数秒持续时间和
  `start → end → UUID` 稳定排序。
- 第一批向量按六类操作拆分：当前事项选择、插入与自动顺延、管理重排/删除/休息、五分钟前
  提醒、本地日期时间解析、Application 规划与删除生命周期。数据全部使用固定 UUID 和固定
  时间，不读取系统当前时间或数据库顺序。
- 新增 `AttentionGuardian.SharedVectors.Tests` 并接入解决方案。测试从仓库外部 JSON 文件
  读取输入，实际调用现有 C# Core/Application，验证 `[start, end)`、跨零点、冲突最新优先、
  空档停止、连续越过不可移动事项、跨日标志、不可移动冲突保存、管理回落、删除补位、
  休息保留名、提醒全部原因、夏令时拒绝、规划重试幂等和确认删除。
- 测试还验证未知 schema 版本、JSON envelope 格式错误、错误 UUID、缺少偏移的时刻以及
  输入倒序时仍产生稳定计划输出。没有修改 C# 产品业务行为，也没有创建 Swift 文件。
- TDD 红灯阶段先只增加合同测试：沙箱内首次运行被 MSBuild 命名管道权限阻止；获准在沙箱外
  运行后，测试因 `test-vectors/v1` 不存在而按预期 2 项失败。创建合同与向量后，最小测试
  最终 13 项全部通过。
- Release 全量实测通过：Core 63、Application 45、Infrastructure 17、Desktop 33、共享向量
  13，共 171 项通过，0 项失败、0 项跳过。NuGet 漏洞数据源因受限网络产生 `NU1900` 警告，
  不影响现有缓存依赖下的编译和测试结果。
- 当前仍属于合同阶段的第一批落地。跨平台规格第 9 节的编辑后重排、不可移动连续组、自然完成
  历史保留和到期未来待办等剩余案例，以及更严格的逐操作 JSON Schema，仍需补齐后才能进入
  Swift Domain。

## 2026-07-29：限制 HANDOFF 只能由用户明确要求更新

- 用户补充长期工作规则：`HANDOFF.md` 是供下一位 Agent 快速理解某一时点项目进度的交接
  快照，不是每次项目操作后都要持续维护的状态文档。
- `AGENTS.md` 已明确规定，只有用户直接要求更新 `HANDOFF.md` 时 Agent 才能修改它。任务完成、
  阶段变化、测试结果变化或 Agent 判断内容过时，都不能自动扩大为修改授权。
- 未获明确要求时，实际操作仍按原有职责追加到 `PROJECT_DEVELOPMENT.md`；本地存在
  `teaching.md` 时继续追加教学记录。此次规则更新没有修改 `HANDOFF.md`。

## 2026-07-29：补齐共享向量的管理与生命周期边界

- 在既有 `manageSchedule` 向量中增加编辑时长后整队重排，以及重叠不可移动连续组从组内
  最早时刻重新排序；C# 执行器继续直接调用 `ScheduleManagement.Edit/Reorder`。
- 在 `applicationLifecycle` 向量中增加自然完成保留历史、管理页只返回活动事项、打开应用时
  同时返回逾期与今日未来待办、确认删除当日待办后记录为 `deleted` 且不混用 `completed`。
  测试仓库只模拟 Application 接口所要求的状态变化，不进入产品运行时。
- 扩展 v1 JSON Schema，为六种 operation 分别声明输入/输出 case schema，并加入自动检查：
  schema 枚举、实际操作文件与 operation 专属规则必须一一对应。`jq` 已确认 schema 和全部
  新向量均为有效 JSON。
- TDD 红灯阶段，新案例先使共享测试分别报 `Unsupported management action edit` 和缺少旧
  `futureTodo` 字段；加入最小执行支持后转绿。最小共享向量测试 14 项通过。
- 最终 Release 全量测试通过：Core 63、Application 45、Infrastructure 17、Desktop 33、
  SharedVectors 14，共 172 项通过，0 项失败、0 项跳过。仍有受限网络导致的 `NU1900`
  漏洞数据查询警告，不影响缓存依赖下的实际测试结果。
- 本轮没有修改任何产品业务代码、没有创建 Swift 文件，也按照用户新增规则没有修改
  `HANDOFF.md`。

## 2026-07-29：完成共享向量门槛并启动 Swift AttentionGuardianDomain

- 逐项审计 `docs/CROSS_PLATFORM_RULES.md` 第 9 节十二类门槛与既有九个验收场景，新增
  `test-vectors/COVERAGE.md`，把每条规则映射到实际 JSON case id。
- 补齐首尾相接切换、两项普通级联、越过单个不可移动事项、无挡板普通重排、休息改名与
  不可移动拒绝、完成历史在整组替换后保留、相对日期换算等向量。
- 规划恢复向量改为真实注入第一次 `MarkPlanned` 失败；第二次调用只补做 planned 标记，
  当日计划 `ReplaceAll` 仍严格为一次，实际验证幂等恢复合同。
- 共享向量发现 C# `ScheduleManagement.Edit` 允许把标题为“休息”的事项设为不可移动，
  与跨平台规格冲突。新增独立 Core 单元测试并在 Core 根因处拒绝该状态；不影响普通事项编辑。
- C# Release 全量验证通过：Core 64、Application 45、Infrastructure 17、Desktop 33、
  SharedVectors 14，共 173 项通过，0 项失败、0 项跳过。`NU1900` 仍来自受限网络的 NuGet
  漏洞数据查询。
- 在 `apple/AttentionGuardianDomain` 创建 Swift 6.2 Package，仅包含 Domain library 与
  test target；没有创建 SwiftUI、Apple Application、SQLite 或通知代码。
- 首个 Swift Testing 测试直接读取仓库同一份 `select-current.json`。沙箱内首次运行因
  SwiftPM 用户缓存权限失败；沙箱外运行后先得到缺少 `ScheduledTodo`/selector 的预期编译
  红灯，再以最小实现加入领域模型、输入校验、半开区间包含和稳定当前事项选择。
- 最终 `swift test` 实测 1 项共享向量测试通过。架构状态与路线图门槛已同步；遵守用户规则，
  本轮没有修改 `HANDOFF.md`。

## 2026-07-29：更正休息属性规则并实现 Swift 自动顺延

- 用户明确更正：休息模板插入时默认是普通事件，但持久化后的休息允许切换为不可移动事件；
  只有名称“休息”不可修改。此前追加历史中“休息不能设为不可移动”的结论已过时，本条和
  `PD-052` 是最新规则，不回写删除旧历史。
- 更新 `AGENTS.md`、跨平台规则、Windows 用户指南、共享覆盖矩阵、JSON Schema 与
  `manage-schedule.json`。原拒绝案例改为成功编辑并输出 `isMandatory = true`。
- 撤销错误加入的 C# Core 拒绝分支，单元测试改为验证休息保留名称同时可成为不可移动；
  Windows 管理页不再禁用休息的不可移动复选框，保存时也不再强制写回 `false`。
- Core、Desktop、SharedVectors 最小测试分别通过 64、33、14 项；随后 Release 全量测试
  Core 64、Application 45、Infrastructure 17、Desktop 33、SharedVectors 14，共 173 项
  通过，0 项失败、0 项跳过。
- Swift Domain 新增 `insert-schedule.json` 测试。有效红灯为缺少 `ScheduleTrial` 和带偏移
  模型字段；最小实现加入稳定排序、半开区间重叠、普通级联、空档停止、越过不可移动事项、
  不可移动冲突集合、原始偏移保存和跨日本地日期判断。
- 首次实现后 Swift 编译指出 `TimeZone.gmt` 仅在较新 macOS 可用，改用
  `TimeZone(secondsFromGMT: 0)` 平台中性回退。最终 `swift test` 两个 suite、两项测试通过，
  分别遍历当前事项和插入排程的全部共享案例。
- 本轮没有创建 SwiftUI、Apple Application、数据库或通知代码，也没有修改 `HANDOFF.md`。

## 2026-07-29：定义开始时间编辑合同并完成 Swift 管理 Domain

- 用户确认管理页默认继续隐藏时间，但未来增加页面级显示切换按钮；开启后只在气泡末尾显示
  开始时间。双击展开气泡后增加可修改的开始时间字段。
- 用户确认开始时间冲突规则：目标时刻第一项不可移动则拒绝；普通事项由确认弹窗二选一：
  保持原时长移到被修改事项之后，或只截短该第一项到新开始时刻。后续事项正常整体重排，
  取消确认时零修改。
- 更新 `AGENTS.md`、`DESIGN.md`、跨平台规格与 `PD-053`。展示开关明确属于 UI 状态，
  冲突试算与整体重排属于 Domain；本轮尚未实现 Windows 或 SwiftUI 的按钮、时间字段和弹窗。
- `manage-schedule.json` 新增三项开始时间向量并扩展 schema：移动原事件、截短第一项、
  不可移动占用时拒绝。C# `ScheduleManagement.EditStart` 返回完整试算结果、拒绝原因与冲突
  事项 ID；新增三项独立 Core 测试。
- C# Core 最小测试 67 项通过，共享向量测试 14 项通过；最终 Release 全量 Core 67、
  Application 45、Infrastructure 17、Desktop 33、SharedVectors 14，共 176 项通过，
  0 项失败、0 项跳过。
- Swift 先新增整份 `manage-schedule.json` 测试并得到缺少管理类型的编译红灯，随后实现普通
  重排、不可移动连续组内重排、挡板回落、删除补位、编辑、休息插入/属性、开始时间两种模式
  与拒绝结果。
- Swift 首次管理测试发现空计划插入休息时丢失输入 `+08:00` 偏移；接口增加明确
  `utcOffsetSeconds` 后修复。最终 `swift test` 三个 suite、三项测试通过，分别遍历当前事项、
  自动顺延和管理操作全部共享案例。
- 本轮没有实现 Apple Application、Persistence、SwiftUI 或通知，也没有修改 `HANDOFF.md`。

## 2026-07-29：Swift Domain 实现五分钟前提醒资格

- 新增 Swift Testing `ReminderVectorTests`，直接读取 `evaluate-reminder.json`；有效红灯为
  缺少 `HandoffReminderPolicy`。
- Swift Domain 新增纯提醒评估器，使用当前事项选择与稳定排序，返回当前事项、首尾相接的
  下一事项、提醒时刻、是否立即提醒和明确不符合原因。
- 共享案例覆盖五分钟边界、任务恰好五分钟、任务过短、没有当前事项、没有相接下一项、
  下一项为“休息”和窗口外；Domain 不请求通知权限，也不发送系统通知。
- 最终 `swift test` 四个 suite、四项测试全部通过，分别遍历当前事项、插入排程、管理操作和
  提醒共享向量。C# Release 全量仍为 176 项通过，0 项失败、0 项跳过。
- Swift Domain 后续还需补充领域生命周期模型；规划幂等恢复、Repository/Clock 协议和夏令时
  解析属于 Apple Application 边界，不应放进纯 Domain。本轮没有修改 `HANDOFF.md`。

## 2026-07-29：把 Windows 管理页同步任务写入 Agent 长期规则

- 用户明确要求把 `PD-053` 的管理页更新交给后续另一位 Codex，同步内容已完整写入
  `AGENTS.md`，本轮不实现 Windows 页面。
- 展示规则进一步明确：每次进入管理页默认关闭；按钮文案在“显示开始时间 / 隐藏开始时间”
  间切换；开启后只在当日待办气泡末尾显示开始时间。开关只属于当前页面展示状态，不写事件
  数据、不触发重排；展开编辑始终显示开始时间字段。
- `AGENTS.md` 当前优先级已从已完成的共享向量准备阶段更新为实际状态：Swift Domain 生命周期
  → Apple Application → Persistence/Infrastructure → SwiftUI。共享合同和 Swift Package
  不得被后续 Agent 重复初始化。
- 单独列出 Windows Codex 的允许范围：显示开关、气泡时间、展开编辑、调用既有
  `ScheduleManagement.EditStart`、拒绝/确认/取消交互及测试。不得借机修改专注首页、
  Apple Domain 或发布记录。
- 此次只更新 Agent 规则、项目历史和本地教学记录，没有修改 Windows 或 Swift 产品代码，
  不需要重复运行自动化测试，也没有修改 `HANDOFF.md`。

## 2026-07-29：完成 Swift Domain 生命周期模型

- 新增 `LifecycleVectorTests`，读取 `application-lifecycle.json` 中属于纯 Domain 的状态与
  日期案例；有效红灯为缺少 `LocalDate`、未来待办、记录状态和生命周期规则。
- Swift Domain 新增平台无关 `LocalDate`、`UnscheduledTodo`、`ScheduledTodoRecord`、
  `UnscheduledTodoRecord`，以及 scheduled 的 active/completed/deleted 和 future 的
  active/planned/deleted 状态。
- `TodoLifecycle` 实现自然完成、确认删除、planned 幂等状态、到期未来待办稳定排序、
  相对日期换算和整组活动计划替换时保留非活动历史。
- 测试初始编译同时指出 Swift Testing 宏比较式右侧不能直接放 `try`，将预期值提前解析后
  保持断言清晰。最终 `swift test` 五个 suite、五项测试通过，遍历当前事项、自动顺延、
  管理、提醒和生命周期共享案例。
- 纯 Swift Domain 的职责至此完成。规划时两个数据库的先后写入与失败恢复、Repository/Clock
  协议、通知去重和夏令时本地时间解析属于 Apple Application，尚未实现。
- 本轮只修改 Swift 与项目文档，按用户确定的验证边界没有重复运行 C# 测试，也没有修改
  `HANDOFF.md`。

## 2026-07-29：按用户授权重写 Apple Application 阶段交接

- 用户明确要求更新 `HANDOFF.md`，因此本次依照权限规则整体替换交接快照。
- 新交接不堆叠历史，只记录 Windows 已发布基线、共享合同完成、Swift Domain 完成、最近验证
  数字、最新休息/开始时间规则和 Apple Application 唯一下一任务。
- Apple Application 任务明确拆为协议、固定 Clock/fake、规划用例、跨库幂等恢复、提醒去重和
  夏令时解析；完成条件禁止提前创建 SQLite、SwiftUI 或 Apple App。
- 另列用户计划交给其他 Codex 的 Windows PD-053 同步任务，避免 Apple Agent 顺手修改
  Windows UI。
- 交接同时写明新的验证边界：纯 Swift 改动只跑 Swift；只有共享合同、跨平台规则或 C# 代码
  变化时才运行相应 C# 测试。
- 本次只修改交接、项目历史和本地教学记录，没有修改产品代码，因此未运行自动化测试。

## 2026-07-30：完成 Apple Application 第一纵向切片

- 用户确认后续默认直接按仓库规则推进，不自动调用 Superpowers、brainstorming 或其配套
  形式流程；该偏好已写入 `AGENTS.md`，同时保留测试、架构、验证和教学记录质量门槛。
- Swift Package 新增 `AttentionGuardianApplication` library 与 test target；Domain 不反向
  依赖 Application。
- 新增可替换 `Clock` 和 `ScheduledTodoRepository` 协议。仓库端口读取完整生命周期记录并
  接受一次整组替换，不暴露 SQLite、表或事务实现。
- 先加入六项 Application 行为测试并观察到缺少用例、请求、端口和测试替身的有效编译红灯，
  随后实现 `AddScheduledTodoUseCase`。
- 新增用例使用固定 Clock 的当前时刻完成到期记录，只从活动计划计算下一
  `currentSelectionPriority`，让未指定开始时间使用 Clock，调用 Swift Domain
  `ScheduleTrial.insert`，以 `TodoLifecycle.replaceActiveSchedule` 保留 completed/deleted
  历史，并在试算成功后执行一次整组写入。
- 测试覆盖默认开始时间与结束时间推导、到期完成、历史保留、显式开始时间、活动优先级、
  不可移动冲突、无效持续时间零读写、重复 UUID 试算失败零写入和跨日标志。
- 最小 `swift test --filter AddScheduledTodoTests` 为 6 项通过；随后完整 `swift test` 为
  6 个 suite、11 项测试通过，0 项失败，构建无警告。
- 同步更新 `ARCHITECTURE.md` 的 Apple 当前状态。本轮没有创建 SQLite、SwiftUI、系统通知
  适配器、Xcode App 或可运行客户端，没有修改 C# 和 `HANDOFF.md`，也没有提交或发布。

## 2026-07-30：完成 Apple Application 新增未来待办切片

- 先新增三项 Application 测试并观察有效编译红灯：缺少未来待办 Repository、请求和用例。
- `Clock` 增加可替换 `TimeZone`，让 Application 能在固定时刻下确定用户的本地“今天”，
  测试不读取 `TimeZone.current`。
- 新增 `FutureTodoRepository` 最小端口，只接受 `UnscheduledTodoRecord` 保存；没有读取或
  写入当日待办仓库，也没有暴露数据库实现。
- 新增 `FutureTodoDateSelection`，以 `.exact(LocalDate)` 和 `.daysFromToday(Int)` 在类型层面
  保证日期输入二选一；相对日期调用 Domain `TodoLifecycle.relativeDate` 后保存确定日期。
- 测试覆盖直接日期、上海时区跨 UTC 日期边界的“两天后”换算、不可移动属性保存，以及零天
  相对输入失败零写入。
- 最小未来待办测试 3 项通过；完整 `swift test` 为 7 个 suite、14 项测试通过，0 项失败，
  构建无警告。
- 本轮没有创建 SQLite、SwiftUI、系统通知、Xcode App 或可运行客户端，没有修改 C#、
  `HANDOFF.md` 或共享规则，也没有提交或发布。

## 2026-07-30：完成 Apple Application 打开状态切片

- 新增纯 Domain `MandatoryConflictDetector`，从尚未结束的不可移动事项稳定重算所有重叠对；
  首尾相接不算冲突，普通事项不进入结果。
- 新增 `LoadOpeningStateUseCase`：完成 `end <= now` 的活动记录，仅在状态变化时整组保存，
  从活动计划选择当前事项、恢复不可移动冲突，并查询一次活动未来待办后筛选本地今天及逾期项。
- `FutureTodoRepository` 增加 `loadAllActive`；Clock 的本地日期换算抽为 Application 共用边界。
- 首次最小测试有效红灯为缺少冲突检测器；实现后测试夹具曾把第三项同时与第二项重叠，检测器
  正确返回两组。修正夹具为真正首尾相接后，最小 2 项测试通过。
- 完整 `swift test` 为 9 个 suite、16 项测试通过，0 项失败，构建无警告。
- 未创建 SQLite、SwiftUI、通知或 App，未修改 C#、共享合同和 `HANDOFF.md`。

## 2026-07-30：完成 Apple Application 管理编排切片

- 新增 `ScheduleManagementUseCase`，统一在可替换 Clock 下完成到期事项、提取活动计划并调用
  已有 Domain 管理规则。
- Application 现可编排管理加载、普通/不可移动重排、编辑、确认删除、休息插入和开始时间
  修改；成功结果只执行一次整组保存。
- 开始时间试算返回确认需求或不可移动拒绝时不写仓库；用户删除先标记 `deleted`，再用重排后
  活动计划替换，避免把删除误作物理消失或自然完成。
- 测试覆盖重排整组写入、开始时间拒绝零写入，以及加载、编辑、休息插入、删除保留历史。
- 最小管理测试 3 项通过；完整 `swift test` 为 10 个 suite、19 项测试通过，0 项失败，无警告。
- 未创建 SQLite、SwiftUI、通知或 App，未修改 C#、共享合同和 `HANDOFF.md`。

## 2026-07-30：完成 Apple Application 规划幂等恢复切片

- `FutureTodoRepository` 增加按 UUID 读取 active 源事项和幂等标记 `planned` 的端口。
- 新增 `PlanFutureTodoUseCase`：沿用源 UUID、标题和不可移动属性，先完成 Domain 试算并整组
  写入当日记录，成功后才标记未来待办。
- 当 planned 标记首次失败时，当日计划保留；重试检测到任意生命周期的同 UUID 当日记录后，
  不再插入、不再顺延、不再整组写入，只补 planned 标记。
- 测试证明首次标记失败后两次调用仅发生一次当日整组替换；另证明当日写入失败时不会调用
  planned 标记，源记录继续 active。
- 最小规划测试 2 项通过；完整 Swift 测试 11 个 suite、21 项通过，0 项失败，无警告。
- 未创建 SQLite、SwiftUI、通知或 App，未修改 C#、共享合同和 `HANDOFF.md`。

## 2026-07-30：完成 Apple Application 提醒进程内去重切片

- 新增 `HandoffReminderUseCase`，从当日仓库读取 active 计划，以固定 Clock 调用既有 Domain
  `HandoffReminderPolicy`，符合资格时返回当前/下一事项和提醒时刻。
- 新增 actor `ReminderDeduplicationState`，以当前事项 UUID 原子 claim；同一进程中同一事项
  最多产生一次待发送提醒。
- 不符合资格的评估不会占用去重状态，因此同一事项稍后进入五分钟窗口仍可提醒。
- 最小提醒 Application 测试 2 项通过；完整 Swift 测试 12 个 suite、23 项通过，0 项失败，
  构建无警告。
- 本轮没有系统通知发送、权限请求或后台任务，也未创建 SQLite、SwiftUI 或 App，未修改
  `HANDOFF.md`。

## 2026-07-30：完成 Apple Application 本地日期时间解析切片

- 新增 Swift Application 共享向量测试，直接读取
  `test-vectors/v1/resolve-local-date-time.json`；有效红灯为缺少解析器。
- 新增 `LocalDateTimeResolver`，严格接收 `yyyy-MM-dd'T'HH:mm:ss` 与 IANA 时区标识。
- 解析器把当地钟表组件视为待解析值，收集目标时区附近可能偏移，生成候选确定时刻，并再次按
  目标时区格式化回验：零候选为 `invalid`，一个为 `resolved`，多个为 `ambiguous`。
- 共享案例验证纽约正常冬令时返回 `-05:00`、春季跳跃 `02:30` 被拒绝、秋季回拨 `01:30`
  被判为重复，Foundation 不会静默移动或选择偏移。
- 最小共享向量测试 1 项通过；完整 Swift 测试 13 个 suite、24 项通过，0 项失败，无警告。
- 未创建 SQLite、SwiftUI、通知或 App，未修改 C#、共享合同和 `HANDOFF.md`。

## 2026-07-30：补齐 Apple Application 未来待办确认删除

- 为进入 Application 生命周期共享向量门槛，先补齐此前缺少的未来待办删除编排。
- `FutureTodoRepository` 增加 `markDeleted`；`DeleteFutureTodoUseCase` 只有收到明确确认才调用
  写入，取消时保持 active。
- 参数化测试同时覆盖确认与取消；最小测试通过，完整 Swift 为 14 个 suite、25 项通过，
  0 项失败，无警告。
- `application-lifecycle.json` 的完整 Application 读取测试仍是下一项，未误写为本轮完成。

## 2026-07-30：Apple Application 通过完整共享生命周期合同

- 新增 `ApplicationLifecycleVectorTests`，直接读取 `application-lifecycle.json` 全部 8 个案例，
  分别驱动规划重试、未来待办删除、管理加载、打开状态、当日删除、历史保留替换和相对日期用例。
- 共享向量测试通过；完整 Swift 为 15 个 suite、26 项通过，0 项失败，无警告。
- Apple Application 协议、Clock、编排、恢复、去重与本地时间共享门槛完成；下一阶段为
  Apple Persistence/Infrastructure，仍未创建 SQLite、SwiftUI 或 App。

## 2026-07-30：按用户授权重写 Persistence 阶段交接

- 用户明确要求更新 `HANDOFF.md`，因此整体替换当前快照。
- 新交接记录 Swift Domain/Application 已完成、15 个 suite 与 26 项测试证据、未完成边界，
  并把 Apple 双 SQLite Persistence/Infrastructure 设为下一位 Agent 的唯一主线。
- 交接列出 schema、迁移、事务、两个 Repository、失败恢复、关闭重开和完成条件，明确禁止
  重复 Application 或提前进入 SwiftUI/App。
- 本次只更新文档，没有修改产品代码，因此没有重复运行测试。

## 2026-07-30：实现 Apple 双 SQLite Persistence 第一完整切片

- Swift Package 新增 `AttentionGuardianPersistence` library、系统 `CSQLite` 边界和独立集成
  test target；Persistence 依赖 Application/Domain，后两层不反向依赖 SQLite。
- 当日与未来待办由两个 Repository actor 和两个独立数据库文件实现，连接不跨 actor 并发共享。
- 两库各自使用 `PRAGMA user_version` v2：v0 逐级创建、v1 事务迁移、迁移失败回滚、高版本
  明确拒绝。测试同时检查错误数据库中不会出现另一类表。
- 当日整组替换在 `BEGIN IMMEDIATE` 事务内完成。缺失的旧 active 记录标记 deleted，传入记录
  按 UUID upsert；completed/deleted 历史不物理删除。UUID、标题、epoch 小数秒、原始偏移、
  不可移动属性、录入优先级、状态和完成时刻可关闭重开后往返。
- 未来仓库实现 active 列表、按 UUID 查找、保存、幂等 planned 和 deleted 状态更新；
  非 active 记录不会重新出现在活动查询。
- 真实仓库集成测试覆盖 v0、v1→v2、未来版本拒绝、失败回滚、双库隔离、历史保留、关闭重开、
  生命周期隐藏，以及 Application 规划第二步首次失败后重试不重复写入当日计划。
- 最小 `SQLitePersistenceTests` 实际为 1 个 suite、7 项通过。完整 Swift Package 验证在本文
  追加时尚待执行，因此不提前记录为通过。
- 未创建 SwiftUI、系统通知适配器或 Apple App；未修改 `HANDOFF.md`，也未提交或发布。

## 2026-07-30：Apple Persistence 完整 Swift 回归验证

- 在最小 Persistence 7 项通过后，实际运行完整
  `swift test --package-path apple/AttentionGuardianDomain`。
- 结果为 16 个 suite、33 项测试全部通过，0 项失败；其中原有 Domain/Application 26 项与新增
  Persistence 7 项共同通过。
- 本轮验证没有改动 C#、Windows 发布基线或 `HANDOFF.md`。下一主线可进入 Apple 平台
  Infrastructure 的通知/应用数据目录组合边界，之后才是 SwiftUI Design Token 与页面。

## 2026-07-30：补齐 Apple 应用数据目录与迁移前备份

- 新增 `ApplePersistenceContainer`，作为未来 macOS/iOS App 的单一持久化组合入口。默认路径
  为用户 `Application Support/AttentionGuardian`，固定创建
  `scheduled-todos.sqlite3` 与 `future-todos.sqlite3`；测试可显式传入隔离目录。
- v1 数据库升级 v2 前使用 SQLite backup API 创建
  `.pre-migration-v1.sqlite3` 快照，不直接复制可能带 WAL 的数据库文件。迁移失败时事务保持
  原 v1，备份仍可读取；不完整备份会被删除。
- 新增测试覆盖组合入口建目录与双库、成功迁移前快照内容，以及失败迁移同时保留原库和可读备份。
- 首次编译发现 `URL.appending(path:)` 需要 macOS 13+；改用更早系统支持的
  `appendingPathComponent`，没有无意抬高 Package 最低版本。
- Persistence 最小测试实际为 1 个 suite、9 项通过；完整 Swift Package 为 16 个 suite、
  35 项通过，0 项失败。
- iOS 文件保护属性依赖未来 Xcode App 的平台与 entitlement 组合，本轮不在纯 Swift Package
  中猜测；系统通知和 SwiftUI 仍未实现。未修改 `HANDOFF.md`，没有提交或发布。

## 2026-07-30：实现 Apple 五分钟前系统通知适配

- Swift Package 新增 `AttentionGuardianInfrastructure` product/target，依赖
  Application/Domain，但后两层不反向依赖 UserNotifications。
- `AppleNotificationCenter` 把权限状态、明确授权请求和通知入队抽成可替换端口；生产
  `UserNotificationCenterAdapter` 使用 `UNUserNotificationCenter`。
- `AppleHandoffNotificationSender` 只消费现有 Application 已判定并按当前 UUID 去重的
  `PendingHandoffReminder`。通知固定标题为“即将交接”，正文同时包含当前事项和下一事项名称。
- 未决定权限时返回 `authorizationRequired`，拒绝时返回 `denied`，两者都不入队；后台提醒
  评估不会偷偷请求权限，未来 UI 必须在用户明确操作时调用授权入口。
- 为跨 target 构造已判定提醒，给 `PendingHandoffReminder` 增加公开初始化器，没有改变提醒
  资格、时间窗口或去重规则。
- 首次编译发现 Swift continuation 最低需要 macOS 10.15，并发现 Void continuation 类型无法
  推断；适配器明确标注 macOS 10.15/iOS 13 可用范围并写明 continuation 类型。随后通知最小
  测试 4 项通过，完整 Swift 为 17 个 suite、39 项通过，0 项失败。
- 当前尚未创建 Xcode App，因此没有弹出真实权限窗口，也未实机验证前台/最小化通知展示；
  这些属于 App 组合与实机验收。未修改 `HANDOFF.md`，没有提交或发布。

## 2026-07-30：开始 iPhone 优先的共享 SwiftUI 表现层

- 完整阅读 `DESIGN.md` 后新增 `AttentionGuardianPresentation` product/target，供 macOS 与
  iOS 共享，不访问 SQLite、UserNotifications 或平台窗口 API。
- 第一批实现 Design Token、Mesh/静态降级 ambient 背景、Material Dashboard、Capsule Button、
  连续 `GlassNumberStepper`、双击/辅助功能展开的 `AlertGlass`、底部隐藏抽屉与 Floating
  Capsule Tab Bar，以及专注首页 empty/ready/focused 三种骨架。
- ready 状态只显示“开始”，不泄露任务名；focused 状态只显示当前事项与托管确认，不包含
  开始/结束时间、倒计时、进度条或下一事项详情。
- 所有页面使用 `VStack`/`HStack`、`Spacer`、最大可读宽度、size class、Dynamic Type 和安全区
  组织。额外执行源码检查，Presentation 中没有 `.position`、`.offset`、`GeometryReader`、
  `UIScreen`、`NSScreen` 或 `px` 定位。
- Package 明确 Apple 基线为 macOS 14/iOS 17；macOS 15/iOS 18 使用 Mesh Gradient，较早系统
  自动使用同品牌静态渐变。首次因未声明平台导致 SwiftUI API 可用性编译失败，明确基线后通过。
- 自适应 Token 测试 2 项通过；完整 Swift Package 为 18 个 suite、41 项通过，0 项失败。
- 当前仍是可编译共享组件，尚未创建 Xcode App 或进行真实 macOS/iPhone 视觉、Material、
  VoiceOver 和触控实机验收。未修改 `HANDOFF.md`，没有提交或发布。

## 2026-07-30：建立原生 macOS SwiftUI App 壳

- 新增 `AttentionGuardianMacApp` executable product/target，入口使用系统 SwiftUI
  `WindowGroup`；没有隐藏标题栏、覆盖窗口边框或创建自绘关闭/最小化/全屏按钮。
- 新增 `SystemClock` 并在 `MacAppModel` 启动时组合真实
  `ApplePersistenceContainer`、两个 Repository 与 `LoadOpeningStateUseCase`。当前事项存在时
  首页先进入 ready，点击开始后才切换 focused 并显示名称；无当前事项显示平静空状态。
- 本地数据打开失败时只显示用户可理解的应用内错误，不把 SQLite 错误或路径暴露到 UI。
- 添加/管理导航目前只提供明确的开发中承载面，尚未伪装成已完成表单或列表；下一阶段接入
  真实页面后替换。
- `swift build --product AttentionGuardianMacApp` 实际成功。为真实窗口检查，在 `/private/tmp`
  组装临时自签名 `.app` 并启动；辅助功能树确认窗口含系统“关闭按钮”“全屏幕按钮”“最小化
  按钮”和系统标题 `Attention Guardian`，画面未出现自绘交通灯。
- 完整 Swift Package 仍为 18 个 suite、41 项通过，0 项失败。临时验证窗口已关闭；未修改
  `HANDOFF.md`，没有提交或发布。

## 2026-07-30：透明原生标题栏与持续 ambient 流动

- Mac Scene 使用 `hiddenTitleBar`，`NativeWindowChrome` 配置透明标题栏、
  `fullSizeContentView`、透明窗口底色和背景拖动。没有创建自定义关闭、最小化或全屏按钮。
- 真实临时 `.app` 复查确认白色标题栏和标题文字已经消失，系统三颗交通灯直接位于蓝青 ambient
  背景上；辅助功能树仍识别原生关闭、最小化和全屏按钮。
- `AmbientBackground` 改由 `TimelineView` 驱动：未开始状态较快，稳定专注状态降低速度与
  饱和度，状态切换增加一次 1.6 秒衰减脉冲；Reduce Motion 时暂停时间轴。
- macOS 15/iOS 18 低频移动 Mesh 内部控制点，边界点保持稳定；较旧目标系统移动归一化渐变
  起止点作为同色降级。两者都不读取屏幕尺寸或使用绝对元素位置。
- 连续截取两帧并排除标题栏区域后比较：2.5 秒内背景区域约 63.1% 色彩通道发生变化，
  平均通道变化约 1.06，证明是克制的持续流动而非静态图片。
- 源码绝对定位检查无结果；完整 Swift 仍为 18 个 suite、41 项通过，0 项失败。未修改
  `HANDOFF.md`，没有提交或发布。

## 2026-07-30：完成添加事件的真实 UI 到数据库闭环

- 新增共享 `AddEventFlow`，第一屏严格只选择“今天要做什么？”或“这些事情之后做”，没有混合
  两类字段。
- 当日表单实现名称、当天开始小时/分钟、持续小时/分钟和不可移动开关；结束时间不由用户输入。
  保存时 Mac 组合层用 `LocalDateTimeResolver` 严格解析系统时区，拒绝夏令时无效/重复时刻，
  再调用 `AddScheduledTodoUseCase` 写入真实当日数据库。
- 未来表单实现名称、“一天后/两天后”、年/月/日和不可移动开关。快捷日期同步具体日期；
  手动调整日期会取消快捷状态，月份/年份变化自动收紧合法日。保存调用
  `AddFutureTodoUseCase` 写入独立未来数据库。
- 新增可替换 `GlassPicker`、文本输入、表单动作和 draft 模型。Presentation 不持有 Repository，
  Mac App 只负责 draft→Request 与用例组合，方便后续修改视觉或字段排列。
- 保存成功后返回专注首页并重新读取 OpeningState；失败留在原表单并显示普通用户错误，不暴露
  SQLite 或堆栈。
- 完整 Swift 为 18 个 suite、41 项通过，0 项失败；绝对定位检查无结果。
- 真实临时 App 冒烟检查确认类型选择页和当日表单完整出现在辅助功能树，名称空白时保存禁用；
  未输入或保存测试数据。排查并终止了此前遗留的旧 SwiftPM 测试进程，避免旧承载面遮蔽新版。
- 事件管理仍是开发中承载面。未修改 `HANDOFF.md`，没有提交或发布。

## 2026-07-30：按用户授权重写 Apple UI 阶段交接

- 用户明确要求更新 `HANDOFF.md`，因此整体替换当前快照，没有把旧交接继续堆成时间线。
- 新交接记录 Apple Domain/Application/Persistence/Infrastructure、共享 Presentation、
  原生透明 Mac 壳、动态 ambient 和真实添加写库闭环的当前状态与 18 suite/41 项验证证据。
- 明确下一位 Agent 主线为继续 UI 功能内容，最小建议先做事件管理的加载、展示、开始时间开关、
  删除与未来待办主动展开，再逐步增加拖拽、编辑、冲突组和休息模板。
- 明确当前 UI 不是用户确认终稿；用户之后会继续提出颜色、比例、层级、间距与交互修改。实现
  必须保持 Token、组件、draft、App 组合和业务层分离，Mac 继续服务 iPhone 适配且禁止绝对定位。
- 本次只更新交接与历史/教学文档，没有修改产品代码，因此没有重复运行测试。没有提交或发布。

## 2026-07-30：完成 Apple 事件管理加载、展示与删除最小闭环

- 新增 `FutureTodoManagementUseCase`。未来待办只有用户主动展开管理区后才读取，结果按日期与
  UUID 稳定排序；删除只有明确确认后才标记 `deleted`，取消保持零写入。
- 新增共享 SwiftUI `ManagementSurface` 及纯展示模型。管理页加载活动当日待办，默认隐藏开始
  时间，页面级按钮可切换“显示开始时间 / 隐藏开始时间”；气泡只在开启后显示开始时间，不显示
  结束时间、倒计时或时间轴。
- 当日与未来待办删除都先显示包含事项名称的确认对话框；确认后调用既有 Application 用例并
  刷新当前列表，当日删除后同时刷新首页状态。
- 未来待办区域默认折叠且不访问 Repository；第一次展开才读取。每次重新进入管理页都会清空
  旧的未来待办展示缓存，保证仍由本次主动展开触发最新读取。
- 管理页使用弹性容器、`ViewThatFits`、Dynamic Type 与共享 Token，没有加入绝对坐标；当前
  切片未实现拖拽、编辑、冲突组、开始时间修改确认或休息模板。
- 新增 Application/Presentation 测试。相关 2 个 suite、8 项通过；完整 Swift Package 为
  18 个 suite、44 项测试全部通过，0 项失败。Mac App product 成功编译，绝对定位源码检查无
  结果。
- 当前目录仍缺少 `.git` 元数据，因此不能报告分支、工作树差异或执行提交；本轮没有修改
  `HANDOFF.md`，没有提交、推送或发布。
