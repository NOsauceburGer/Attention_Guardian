# Attention Guardian 用户使用指南

Attention Guardian（托管时间）帮助你把未来事项交给软件看守。它不会把首页变成
时间表，而是在你主动开始后只显示当前应该处理的一件事。

## 1. 第一次打开

打开应用后会看到以下两种状态之一：

- 当前没有需要执行的事件：显示平静空状态。
- 当前时间正处于某个当日待办的开始与截止范围内：只显示“开始”按钮。

点击“开始”后，应用才显示当前事件名称和“未来的事项已为你托管”。专注页面不会
显示开始时间、结束时间、倒计时或进度条。

窗口顶部中央有一条短横线。用鼠标从短横线向下拖动，可以展开顶部抽屉。正式版包含：

- 添加事件
- 事件管理

点击任一入口或抽屉外部后，抽屉会自动收回。

## 2. 添加事件

进入“添加事件”后，先选择事件类型。

### 当日待办

当日待办是有明确开始时间和持续时间、需要参加排序与冲突处理的事件。这里的“当日”
是产品分类名称，事件可以跨过午夜进入第二天。

需要填写：

1. 名称；
2. 开始时间；
3. 持续时间；
4. 是否为“不可移动事件”。

结束时间由应用自动计算，不需要手工填写。如果没有调整开始时间，应用使用保存操作
当下的时间。

“不可移动事件”适合会议、预约、车次等不能随意延后的事项。普通事件遇到不可移动
事件时会自动移动到它结束之后，并保持原来的持续时间。

### 未来待办

未来待办只有日期，没有具体时间，不参加当日排程和冲突检测。

可以选择：

- 一天后；
- 两天后；
- 或直接选择具体年月日。

日期到达后，下次打开应用时，首页顶部会显示“有当日未设置时间待办”。每一项可以：

- 点击“规划”：名称会自动带入当日待办表单；只有成功保存后，原记录才标记为
  `planned`；
- 点击“删除”：确认后只处理选中的这一项。

如果进入规划页面后没有保存就返回，原未来待办不会改变。

## 3. 自动顺延与冲突

新增当日待办与已有安排重叠时：

- 没有不可移动事件：受影响的普通事件依次向后顺延；
- 遇到一个或多个不可移动事件：普通事件整体越过它们，再继续排列；
- 两个不可移动事件互相重叠：应用仍会保存，并在首页显示“有不可移动事件冲突”，
  由你进入事件管理决定顺序；
- 顺延进入第二天：显示“有事件被顺延至明日”；
- 发生任意顺延：首页短暂显示“有事件被顺延”。

自动顺延只移动时间，不改变事件原有持续时间。

## 4. 专注、提醒与任务切换

当前事件结束前五分钟，只有同时满足以下条件才发送 Windows 系统提醒：

- 下一事件与当前事件首尾相接，没有时间空档；
- 下一事件不是“休息”；
- 当前事件持续时间不少于五分钟；
- 应用仍在运行，窗口可以处于打开或最小化状态。

通知会同时显示当前事件和下一事件名称。应用完全退出后不会发送提醒。

当前事件结束并进入下一事件时间范围后，专注页会自动切换到新事件，并使用克制的
淡入过渡。开启 Windows“减少动态效果”后会立即切换。

## 5. 管理当日待办

进入“事件管理”后，默认只显示当日待办，排列顺序就是执行优先级。

### 调整顺序

- 普通事件：直接拖拽到目标位置；
- 键盘操作：聚焦事件后使用 `Alt + ↑` 或 `Alt + ↓`；
- 如果普通事件无法放入不可移动事件占用的位置，应用会把它放到最后一个阻挡它的
  不可移动事件之后；
- 互相冲突或首尾相接的不可移动事件会组成一组，组内可以调整顺序。

重排会保持每个事件的持续时间，并从原计划最早开始时间重新计算队列。

### 编辑

双击事件气泡展开编辑区域，可以修改：

- 名称；
- 持续时间；
- 是否不可移动。

“休息”事件不能改名，但可以设置为不可移动，也可以再改回普通事件。再次双击收起时统一保存和重排。
如果带着尚未收起的修改离开，应用会询问是否保存。

### 删除

点击“删除”后会出现包含事件名称的确认框。确认后该事件在活动列表中消失，后续事件
向前补位。数据库将记录标记为 `deleted`，而不是直接抹除。

## 6. 添加休息

管理页右侧有固定名称为“休息”的模板：

- 默认持续 20 分钟；
- 可以修改模板时长；
- 可以重复拖入主列表；
- 也可以点击“加入队尾”。

拖入后的休息按普通事件保存，可以排序、修改持续时间和删除。

## 7. 管理未来待办

管理页默认隐藏未来待办。点击“展开未来待办”后才会读取并显示，按日期从近到远排列。

未来待办不能拖拽。双击气泡后可以修改名称和日期，或删除；再次双击收起时保存。

## 8. 完成、删除与安排状态

应用不会因为事件结束就直接删除数据库记录：

- `active`：当前仍参与排程或提醒；
- `completed`：时间自然结束，用户界面默认隐藏，但保留历史记录；
- `deleted`：用户明确确认删除；
- `planned`：未来待办已经成功转为当日待办。

这些历史状态为以后由用户主动授权的分析和规划能力保留基础数据。

## 9. 常见问题

### 点击程序后没有窗口

不要运行 `tests` 目录中的同名测试程序。源码 Release 的正确路径是：

```text
src/AttentionGuardian.Desktop/bin/Release/
net10.0-windows10.0.19041.0/win-x64/AttentionGuardian.Desktop.exe
```

## 安装与启动

1. 双击 `AttentionGuardian-0.1.0-win-x64-setup.exe`。
2. 按安装向导完成安装；如有需要，可以勾选创建桌面快捷方式。
3. 从开始菜单中的 “Attention Guardian” 启动应用。
4. 可在 Windows“已安装的应用”中卸载。

安装包包含 .NET 运行组件并自动部署 Windows App Runtime。由于 v0.1.0 安装包目前
没有项目方代码签名，Windows 可能显示来源保护提示；请只使用项目正式发布页提供的
安装包，并核对发布页列出的 SHA-256。

### 没有看到 Windows 通知

- 检查 Windows“系统 → 通知”以及勿扰模式；
- 确认当前事件与下一非休息事件首尾相接；
- 确认当前事件持续时间不少于五分钟；
- 应用完全退出：按当前设计不会提醒。

开发者可以设置 `ATTENTION_GUARDIAN_ENABLE_ACCEPTANCE=1` 后启动 Release，打开内部
验收页面检查 Windows Runtime 错误；普通用户的正式界面不会显示这一板块。

### 为什么首页没有显示事件名称

这是产品设计。即使当前已有事件，用户点击“开始”前也不会显示名称，避免应用主动
抢占注意力。

### 为什么普通列表没有时间

普通管理和专注页面刻意隐藏开始时间与结束时间。添加当日待办时仍可设置开始时间，
排程计算保持确定且可验证。

## 10. 数据位置与隐私

数据仅保存在本机：

```text
%LOCALAPPDATA%\AttentionGuardian
```

当日待办和未来待办分别使用独立 SQLite 数据库。v0.1.0 不包含账号、云同步、AI、
第三方日历或社交功能。
