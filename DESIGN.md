# Attention Guardian Design System

## Platform Scope

本文件同时保存两套明确分离的界面语言：

1. **Apple SwiftUI Design Language**：未来 macOS 与 iOS 客户端的当前设计规范。使用
   SwiftUI 原生 Material、Mesh Gradient、Spring Animation 与平台可访问性能力。
2. **Windows Avalonia Design Language**：已发布 Windows v0.1.0 的设计基线。它继续描述
   Avalonia、GPU shader、Windows 窗口和顶部抽屉，不得被误当成 Apple 客户端实现要求。

两套客户端共享产品目标、信息架构、语义颜色和 calm ambient 品牌气质，但不共享 View、
控件实现或平台交互方式。以下 Apple 规范优先约束新的 macOS/iOS 工作；文档后半部分的
Windows 规范继续约束现有 Avalonia 客户端。

# Apple SwiftUI Design Language

## Apple Design Read

面向 macOS 与 iOS 的原生专注产品。界面应像一组处于同一 ambient 空间中的安静玻璃实体：
背景建立情绪，Dashboard 建立空间，Component 建立操作层级；静止时自然融入，交互时才
显露质量、折射和触感。

- `DESIGN_VARIANCE: 5`
- `MOTION_INTENSITY: 5`
- `VISUAL_DENSITY: 2`
- 技术语言：SwiftUI、原生 Material System、Mesh Gradient、Spring Animation。
- 总体美学：calm ambient design + restrained Liquid Glass。
- “Dashboard”在本规范中只表示悬浮于背景上的主要内容承载面，不表示信息密集的效率
  仪表盘、统计面板或传统 Todo Dashboard。

## Apple Core Style

- 克制、大面积留白、低信息密度，并保留可触摸的实体感。
- 当前任务仍是专注界面唯一主视觉；不增加倒计时、进度条、时间轴或密集计划信息。
- 禁止科幻 HUD、锐利霓虹、粒子爆发、扫描线、彩虹描边、强烈发光边框和炫技式形变。
- 禁止用大面积高饱和色、突兀语义色或静止高亮迫使用户聚焦。静止状态必须 calm；
  更明显的高光、折射和形变只在用户正在交互时短暂出现。
- 红、黄、青绿等语义色继续受到严格限制：只用于小型状态信号、必要文字或局部材质
  着色，不把整个 Dashboard 或提醒染成实心语义色。
- 任何材质、动效或深度效果都不能压过当前任务、损害文字清晰度或制造持续刺激。

## Apple Ambient Background

- Apple 客户端不复用 Windows 的 GPU runtime shader；背景改用 SwiftUI Mesh Gradient。
- 色调保持现有品牌不变：蓝青、冰蓝、深钴，并只加入克制的淡紫雾。
- Mesh 使用大尺度、低频、连续色场；边界柔化，不出现光束、硬边、科技纹理或鼠标追踪线。
- 未开始时可以保持略高对比度和可感知但缓慢的运动；开始专注、任务切换或结束时只产生
  一次短促形态/流速变化，随后回到低对比、低流速的稳定状态。
- Mesh Gradient 是整个页面连续的环境层；页面、Dashboard 或 Tab 切换时不得更换成另一张
  不连续背景，也不得出现黑场或明显色温跳变。
- Reduce Motion 开启时冻结 Mesh 或降为近静态状态；平台无法稳定渲染时使用同色调静态
  渐变降级，不能因此改变信息层级。

## Apple Material and Liquid Glass

- Apple 客户端优先使用 SwiftUI 原生 Material System。第一版从 `thinMaterial` 开始，
  真实设备效果不合适时再统一调整材质等级。
- Liquid Glass 不是给普通形状降低 `opacity`。玻璃必须由系统 Material、背景采样/模糊、
  边缘 specular highlight、克制的折射感与软阴影共同形成。
- 材质层和内容层保持分离：玻璃可以通透，文字、图标、焦点与命中区域必须清晰。
- 静止玻璃只保留低对比边缘、柔和高光和足以辨认的厚度；高强度折射、高光移动与形变只在
  Hover、Press、Focus、Drag、Tab 切换等交互过程中出现。
- 禁止廉价 glassmorphism：单纯透明填充、厚黑阴影、全屏模糊、白色描边加透明背景、
  多层彩虹高光都不构成合格的 Liquid Glass。
- 参考视频中的结构、透镜放大和液态形变可以采用，但强烈绿蓝光斑和彩色像差不得照搬。
  折射色限制在现有冰蓝、蓝青和深钴体系；动画结束后不保留彩色边缘。
- Reduce Transparency 开启时，Material 降级为具有明确厚度、边缘和对比度的冷色实体表面，
  内容和层级不得消失。

## Apple Depth System

Apple 界面使用四级稳定深度，不依靠夸张透视或厚重阴影：

1. **Depth 0 — Ambient**：全屏 Mesh Gradient，负责环境与状态气氛。
2. **Depth 1 — Dashboard**：主要内容面悬浮在 Ambient 上，使用大尺度 Material、
   极软短阴影和克制外缘高光建立空间。
3. **Depth 2 — Components**：Button、Stepper、输入、任务气泡和 Tab Bar 悬浮于
   Dashboard 上；尺寸更紧凑，高光更清楚，阴影距离仍保持很短。
4. **Depth 3 — Active Interaction**：Hover、Focus、Press、Drag 和移动中的 Tab
   Selection Lens。只在交互期间短暂提高高度、折射或高光，完成后回到原层级。

- Soft shadow 只表达相邻层之间的距离，不制造漂浮很高的卡片。
- Specular highlight 必须服从同一假想光源与圆角裁切；不能在每个组件上使用互相矛盾的
  高光方向。
- Dashboard 与 Component 必须能被一眼分辨为不同深度，但静止颜色差异应足够克制，
  不能用明显明暗断层强迫注意。
- 普通文字与静态说明不需要独立玻璃容器；只有真正构成操作或空间层级的元素才进入
  Dashboard/Component 材质。

## Apple Component Language

- Apple 客户端现有 Button 与 Stepper 统一重构为 Capsule；这一条只适用于 Apple
  SwiftUI 设计，取代旧 Windows 规范中“不得全部胶囊化”的限制。
- Button Capsule 使用系统 Material、稳定内容对齐、完整命中区域和明确按下反馈。
- Stepper 使用一个连续 Capsule 承载“减号—可直接编辑数值—加号”，不得拆成三个视觉块，
  也不得出现系统默认上下箭头或突兀深色输入内层。
- Capsule 的玻璃厚度通过内外边缘、短软阴影和 specular highlight 表达，不使用粗描边。
- 输入控件在静止时融入 Component 层；聚焦后以轻微上浮、高光移动、边缘清晰度和克制
  morphing 表示“正在编辑”，不得突然切换成黑色或高饱和色块。
- 任务气泡、提醒和 Dashboard 仍根据内容使用圆角矩形或适当容器；“Button 与 Stepper
  Capsule 化”不等于把所有页面内容无差别塞进胶囊。

## Apple Micro Interactions

所有可交互 Button、Stepper、输入、Tab、任务气泡和拖拽表面都必须具有与其行为相符的
微交互：

- **Hover（macOS/指针环境）**：轻微上浮，soft shadow 稍扩散，specular highlight
  平滑移动；不得突然变亮或改变主色。
- **Press**：短暂压缩或下沉，玻璃厚度收紧，释放后一次平稳回到静止状态。
- **Focus/Input**：边缘高光和材质清晰度连续增强；内容、光标和错误状态保持可读。
- **Drag**：提起、真实占位、移动和落下连续发生；列表不能通过频繁重建而跳动。
- **Morphing**：只连接同一组件的相关状态，如选中透镜、展开控件或 Press 形态；禁止为了
  展示技术而让无关元素持续变形。

Micro interaction 的目标是让玻璃“有质量”，不是让界面显得活泼。静止后必须完全回到
calm 状态，不保留强高光、上浮或颜色脉冲。

## Apple Bottom Drawer and Floating Tab Bar

- Apple 客户端不使用 Windows 顶部抽屉。抽屉位于底部，默认隐藏，只保留一个低存在感
  短横，用户向上滑动或使用等价可访问操作唤出。
- 展开后的底栏为 Floating Capsule Tab Bar，悬浮在 Dashboard/ambient 空间之上；不能贴成
  普通矩形系统工具栏，也不能遮挡主要任务内容。
- Base Glass Capsule 保持稳定；每个 Tab 的图标和文字是独立内容层；上方另有一个可移动的
  Liquid Selection Lens，作为当前选中项的最高交互层。
- 选择 Lens 必须有可感知的玻璃厚度、柔和边缘高光、有限的放大/折射和短软阴影。静止时
  折射克制，不保留参考视频中强烈的绿色或彩虹色像差。
- Tab 切换使用 `matchedGeometryEffect` 保持同一块选择玻璃的身份连续，但不能只做刚性
  位置平移。Spring 过程中必须表现：
  1. 起步时沿目标方向轻微拉伸；
  2. 前缘先鼓向目标，后缘稍后跟进；
  3. 移动中短暂跨过两个 Tab 之间的空间，并产生有限透镜放大/内容偏移；
  4. 到达后只有一次很小的 overshoot；
  5. 最终恢复对称 Capsule 和低对比静止高光。
- 选中图标/文字向品牌冰蓝过渡，离开项回到白色或中性内容色；颜色变化与 Lens 移动同步，
  不在动画结束时突然跳色。
- Spring 必须快速响应、阻尼充分、最多一次轻微回落，不允许连续弹跳。具体参数在真实
  macOS/iOS 原型与目标设备上校准，参考视频只锁定运动结构，不锁死数值。
- Reduce Motion 开启时取消拉伸、overshoot 与液态拖尾，改为短淡入或直接移动到目标；
  Tab 状态仍必须清楚。

## Apple Motion Language

- Apple 页面和组件状态使用 SwiftUI Spring Animation 作为主要运动语言，取代 Windows
  逐元素模糊上浮作为默认页面转场。
- Spring 表达玻璃的质量、跟随与落位；文字本身不独立弹跳，不对页面所有元素同时播放
  弹簧，也不使用 elastic/bouncy 的连续回弹。
- Dashboard 进入、底部抽屉、Tab Lens、组件展开和拖拽落位可以使用不同响应时间，但必须
  共享“快速响应、阻尼充分、一次落位”的节奏。
- Ambient Mesh 的运动时间尺度应明显慢于 Component；只有用户动作发生时 Component 才
  产生短促反馈，避免所有层同时运动。
- 页面切换保持背景连续，并优先保持共享 Dashboard、Tab 与选择 Lens 的空间连续性。
- Reduce Motion 开启时冻结背景、取消形态拉伸和布局弹簧，使用短淡入或立即完成状态变化。

## Apple Platform Adaptation

- macOS 与 iOS 共享品牌、Material、Depth、Component 与 Motion 规则，但输入方式必须原生：
  macOS 补齐 Hover、键盘、焦点和指针反馈；iOS 补齐触控命中区、长按拖拽与必要触觉反馈。
- macOS 使用系统原生窗口壳、标题栏和交通灯按钮。不得像 Windows 客户端那样重新绘制最小化、
  全屏/缩放和关闭控件，也不得用自定义无标题栏窗口替代系统窗口行为。
- 标题栏视觉使用透明全尺寸内容：隐藏标题文字与白色标题栏材质，让 ambient 背景延伸到窗口
  顶部，系统红黄绿交通灯直接位于背景上。这里隐藏的是标题栏外观，不是系统窗口按钮或行为。
- 底部抽屉和 Floating Tab Bar 在两端保持相同概念，但尺寸、安全区、窗口宽度与导航职责
  分别适配，不以固定 Windows 像素值复制。
- 共享 SwiftUI 页面禁止使用绝对屏幕坐标、`.position`、按单张截图计算的 `.offset` 或
  `UIScreen`/`NSScreen` 尺寸摆放元素。布局依赖安全区域、弹性容器、size class、
  Dynamic Type 和内容自身尺寸；常量只用于可复用间距、圆角与最小命中区，不用于锁死位置。
- 使用 SwiftUI 系统字体、动态文字和语义颜色；不得把 Windows Segoe UI 字号机械映射为
  Apple 固定字号。
- VoiceOver、Full Keyboard Access、Dynamic Type、Increase Contrast、Reduce Motion 与
  Reduce Transparency 都必须有可验证降级。

## Apple Spatial Drag and Drop

Apple 事件管理页的普通当日待办使用“时间实体”空间拖拽。该交互的优先级是对象连续性、
材质可信度和舒适的 Motion Design，不追求在同一阶段覆盖更多事件类型。第一阶段只允许普通
事件拖拽；未来待办、孤立不可移动事件和不可移动连续组不启用本手势。

### 物理隐喻与材质

- 用户不是移动列表行，而是从时间空间中拿起一个时间单元，再把它放回新的位置。
- Press 阶段先让原 Capsule 轻微缩小、下沉。压力很浅，只以连续材质中的克制
  inner shadow 和偏心 specular highlight 表达；禁止实色凹坑、硬边内圈、独立圆盘或
  非 Liquid Glass 阴影。
- Lift 后 Capsule 收拢为无文字、无图标、无时长的圆形 `Lens Core` Bubble。iPhone 上
  Bubble 会被手指遮挡，因此它不承担内容识别，只表达“对象已被拿起”的空间状态。
- Bubble 中心不是第二个物体。压力只留下一个很浅、无边界的流体折射密度变化；Lift 后痕迹
  迅速减弱，只保留足以说明玻璃厚度的 inner shadow 与 specular highlight。
- Bubble 外阴影必须克制，只说明它已离开列表平面。禁止厚黑影、发光圆环、霓虹边缘、
  实色核心或把压力痕迹画成明显内凹球。
- Lens Core 在 macOS/iOS 26 及以上必须使用系统 Clear Liquid Glass，不得把 ambient 背景色
  取样后重新染成不透明圆球。它经过文字、图标和卡片边缘时应由系统玻璃产生可辨认的局部折射、
  放大与光影偏移；旧系统仅以 `.ultraThinMaterial` 降级，并保留细薄 specular 边缘。
- Release 时内部扰动随 Bubble 展宽自然摊平，不单独播放“凹陷消失”动画；最终恢复平静
  Capsule 材质。

### 休息时间实体

- 管理页不再使用包含标题、时长说明和独立设置区的“休息模板卡片”。休息入口本身就是一个
  可重复使用的时间实体：静止时为圆形 Liquid Glass 按钮，中心只显示
  `cup.and.saucer.fill`，不在圆形内部堆叠名称或分钟数。
- 休息入口在 macOS/iOS 26 及以上使用 SwiftUI 原生
  `glassEffect(.clear.interactive())`，由系统直接完成环境内容取样、边缘折射与交互高光；
  较旧系统降级为 `.ultraThinMaterial`，不得使用容易形成乳白遮挡的 `.thickMaterial`。
  高通透感不得通过整体降低 `opacity`、透明色块或白色渐变球伪造。
- 圆形入口处于 Components 之上的浮动交互层。静止时以短距离、宽扩散、低对比 soft shadow
  表达悬浮；阴影不得变成黑色硬边或脱离玻璃的发光圆环。Reduce Transparency 开启时改为
  具有明确厚度与对比度的冷色实体表面。
- 双击圆形入口后，同一个玻璃对象横向 Morph 为连续 Capsule Stepper。展开开始时杯子先渐隐，
  Morph 完成后 Stepper 的减号、分钟数与加号才整体渐显；展开态不保留杯子。收起时 Stepper
  快速渐隐，玻璃形状收拢后杯子才渐显。展开不增加第二块卡片、不显示独立标题栏，也不移动成
  与原对象无关的新控件。
- 双击产生一次克制 micro interaction：接触点向外扩散一圈很浅的液态波纹；specular
  highlight 沿同一假想光源短暂移动；整体先微小放大后以高阻尼 Spring 落位。波纹不得成为
  实色圆环，动画结束后必须完全消失。Reduce Motion 开启时取消波纹扩散与回弹，只保留短促的
  材质状态过渡。
- 圆形与 Capsule 状态都能发起拖拽。拖拽沿用普通事件的
  `pressing → lifting → dragging → magnetized → settling → committing` 状态机、按压点
  锚定、占位与目标磁吸反馈；不得退回 macOS 原生 `.draggable` 的独立白色快照预览。
- 拖起后休息入口仍保留原位，因为它是可重复使用的模板；被拿起的时间实体收拢为与普通事件
  相同的 Lens Core Bubble。释放到事件前或队尾后插入一条固定名称“休息”的普通事件，模板
  本体以 Spring 回到拖前的圆形或展开状态，可立即再次使用。
- iPhone 使用相同组件与状态机；圆形最小命中区遵守 44 point。触控端通过长按进入 Lift，
  指针端通过拖动阈值进入 Lift。Bubble 不承担文字识别，避免被手指遮挡时丢失必要信息。
- 休息实体固定悬浮在管理界面安全区域内的底部正中，不属于宽屏右栏，也不随事件列表滚动。
  它使用独立于 Dashboard、列表和队尾按钮的最高稳定 Depth；列表底部必须预留滚动余量，使
  最后一项能够滚出控件遮挡区。macOS 与 iPhone 保持同一底部中央空间关系，只调整安全区间距。

### 拖拽状态机

拖拽必须使用明确且可测试的单向状态机：

```text
idle
→ pressing
→ lifting
→ dragging
→ magnetized
→ settling
→ committing
→ idle
```

持久化或目标解析失败时从当前活动状态进入 `returning`，Bubble 回到原占位并展开为 Capsule，
随后回到 `idle`。任何时刻只有一个普通事件可以进入活动拖拽状态；编辑展开状态与拖拽状态互斥。

- `pressing`：确认浅压力，不改变排程或列表顺序。
- `lifting`：原位置保留与原 Capsule 完全相同的占位空间；活动对象进入列表上方 Overlay，
  通过共享几何身份从 Capsule 收拢为 Bubble。
- `dragging`：手势提供原始位置，视觉 Bubble 使用短距离 Spring 滞后形成 Calm Magnet
  弹性跟随，不机械贴合指针，也不积累影响可控性的长尾延迟。
- `magnetized`：进入合法目标带后锁定稳定目标；小范围指针抖动不得让目标在相邻位置间闪烁。
- `settling`：释放后 Bubble 先吸附实际落点，再从中心向两侧展开。
- `committing`：Application 完成整组排程写入；视觉对象保持落位身份，不提前生成第二张卡片。
- `returning`：失败或取消时回到原占位，原时间表保持不变。

### 布局、占位与坐标

- 使用独立 Overlay 承载活动 Bubble，原列表只保留占位。Bubble 不受 `ScrollView` 或单行
  Capsule 裁切，列表不会因活动对象离开而塌陷。
- 使用 SwiftUI Anchor/Preference 采样当前内容自身的行区域和插入区域，并在命名局部坐标空间
  中解析。不得读取 `UIScreen`、`NSScreen`，不得使用截图坐标、固定屏幕位置或
  `GeometryReader` 手算整页布局。
- `matchedGeometryEffect` 或等价原生共享几何机制维持同一对象身份。开始和落位不得通过
  删除旧 View、在目标处突然新建 View 来伪造 Morph。
- 原占位保持真实高度、间距和圆角，不以整表频繁重建制造跳动。

### 弹性跟随与目标反馈

- 跟随采用已确认的 `Calm Magnet` 强度：Bubble 与手势之间保留短距离、充分阻尼的弹性滞后。
  它必须比直接贴手更有实体感，但明显弱于 macOS Dock 的拉伸和拖尾。
- 目标反馈介于 Calm Magnet 与 Dock 强度之间。目标 Capsule 或插入占位建议放大约
  `3%–3.5%`，最终数值以真实 macOS/iPhone 观感校准，不作为不可调整常量。
- 目标位置使用低对比边缘清晰度、有限 specular highlight、轻微磁吸和邻近空间让位表达。
  不使用强烈颜色变化、实心高亮、发光描边或持续脉冲。
- 目标解析必须使用迟滞区。进入目标后，只有跨过相邻目标的稳定阈值才切换，避免指针或手指
  微小抖动造成闪烁。

### 真实落点与业务规则

- Presentation 不计算排程、不可移动挡板或实际落点。
- Lift 开始后，Mac/iOS App 组合层调用 Application；Application 使用既有 Domain
  `ScheduleManagement.reorder` 对所有候选索引生成无写入预览。
- 每个候选预览包含请求索引、Domain 实际索引和是否使用回落位置。Bubble 从拖拽过程中就磁吸
  Domain 的真实可落位置，避免释放后才突然越过不可移动事件。
- Drop 后只调用一次正式重排与整组持久化。若实际位置与请求位置不同，落位到真实位置并显示
  “这个事件没办法放在这里”。
- 预览、取消和失败均不得写数据库；正式成功仍保持持续时间、稳定标识、录入优先级和历史状态。

### 平台输入与可访问性

- macOS 使用指针拖拽、Hover 和键盘移动；iOS 使用短 Press→Lift 后的触控拖拽，并避免与
  `ScrollView` 垂直滚动发生不可控竞争。
- 普通事件必须提供与拖拽等价的“上移 / 下移”键盘和 VoiceOver 操作；这些路径调用同一
  Application 重排，不维护第二套规则。
- 不可拖拽事件应有明确可访问状态，不能只通过手势无响应或颜色表达限制。
- Reduce Motion 开启时取消弹性尾随、形态拉伸、overshoot 和邻近让位；仍保留原位占位、
  目标轮廓、确定落位与完整键盘路径。
- Reduce Transparency 开启时，Lens Core 和 Capsule 降级为具有明确厚度、边缘、内层次和
  对比度的冷色实体表面，不使用透明度降低整个内容层。

### 动画完成与错误恢复

- 手势结束不等于立即重建列表。Bubble 必须先完成目标吸附和 Bubble→Capsule 展开，再由最终
  数据确认稳定列表。
- Spring 快速响应、阻尼充分，最多一次很小回落。不得连续弹跳、旋转或出现弹性橡皮筋拖尾。
- 正式重排失败时，Bubble 沿同一共享几何身份返回原占位；错误使用普通用户文案，不能把
  Repository、SQLite 或堆栈信息暴露到 UI。
- 应用进入后台、页面离开或活动事项在拖拽中失效时，取消拖拽并恢复原位，不提交半完成计划。

### 验证门槛

- 纯 Presentation 测试覆盖状态机合法转换、单活动对象、目标迟滞、取消和失败返回。
- Application 测试覆盖候选预览零写入、预览实际索引与正式重排一致、正式写入一次和回落标识。
- 交互测试或可替换协调器测试覆盖键盘上移/下移与拖拽调用同一重排入口。
- 完整 Swift 回归之外，必须在真实 macOS 窗口检查指针跟随、ScrollView 裁切、目标稳定和
  Bubble→Capsule 连续性；正式 iOS target 建立后补触控遮挡、滚动竞争、Reduce Motion、
  Reduce Transparency、VoiceOver 和不同 Dynamic Type 的真机矩阵。

## Apple Design Review Gate

每个 SwiftUI 页面或组件在进入实现前必须回答：

- 静止状态是否 calm，还是存在迫使用户注意的颜色、高光或运动？
- 当前任务是否仍是唯一主视觉？
- Mesh Gradient 是否保持品牌色调、连续且无硬边/科幻纹理？
- Glass 是否来自系统 Material 与真实层级，而不是只降低 opacity？
- Dashboard、Component 和 Active Interaction 的深度是否清楚但不过度？
- Button/Stepper Capsule 是否保留清晰内容、完整命中区和可访问状态？
- Micro interaction 是否解释 Hover、Press、Focus、Drag 或 Morph，而不是装饰？
- Tab Lens 是否表现同一块玻璃的液态迁移，而非普通背景块平移？
- Spring 是否阻尼充分、最多一次轻微回落，并在 Reduce Motion 下正确降级？
- Reduce Transparency、Increase Contrast、Dynamic Type、VoiceOver 和键盘/触控路径是否完整？
- 是否在真实 macOS/iOS 设备或窗口中检查 Material、Mesh、折射、阴影和动画，而不是只看
  SwiftUI Preview？

# Windows Avalonia Design Language（v0.1.0 已发布基线）

以下内容描述现有 Windows/Avalonia 客户端。除非用户明确要求修改 Windows 版本，否则
不得用 Apple SwiftUI 的 Mesh Gradient、底部抽屉、全 Capsule 组件或 Spring Tab Lens
自动改写该基线。

## Design Read

面向个人专注场景的 Windows 原生产品界面。用户在普通桌面光线下把注意力交给软件，界面需要像一块安静、有重量的冷色玻璃，操作时有触感，静止时尽量消失。

- `DESIGN_VARIANCE: 5`
- `MOTION_INTENSITY: 5`
- `VISUAL_DENSITY: 2`
- 模式：重设计，保留既定信息架构，替换旧单会话视觉与交互。
- 美学：Apple 风磨砂玻璃近似实现，不宣称使用 Apple 官方 Liquid Glass。

## Design Direction

采用“统一明亮 ambient 画布 + 安静的工具表面”。专注、添加、管理和确认页面
共享同一张蓝青到深钴的连续动态背景，不在导航时切换深浅画布。添加、管理和浮层继续沿用
Super Productivity 式平面层级、清晰分隔与克制短动效；专注主页则使用参考
Amaterasu 的蓝青到深钴 ambient mesh，成为整个低信息密度画面的主要材质。
色场不播放视频，不使用射线或鼠标追踪线条。未开始时已经存在，但运动缓慢；
用户主动开始后，冰蓝、青绿和淡紫色雾加速交融，表达状态已经切换。
玻璃只用来表达可触碰、可展开、可拖动或暂时悬浮的界面材料。

界面不使用传统 Todo List、时间轴、仪表盘、倒计时或进度条。当前任务是专注界面唯一主要文字。

## Core Design Principles

### 1. Ambient First，组件退后

ambient 背景负责建立空间、情绪与专注状态；组件只负责承载操作，不应成为画面主角。
用户首先感知到的应是安静、连续的环境，其次才是可操作表面。除当前任务和当前主操作外，
任何组件都不得依靠大面积实色、高饱和颜色、厚重阴影或过度模糊争夺注意力。

判断一个组件是否合格时，应先把注意力从它移开：如果它仍持续吸引视线，说明层级过高；
如果刻意寻找时无法辨认边界、状态或操作方式，说明克制过度。目标不是“看不见”，而是
“自然融入，需要时清楚”。

### 2. Frosted Glass 是层级材料，不是装饰效果

磨砂玻璃只用于可触碰、可编辑、可展开、可拖动、暂时悬浮或需要温和提示的表面。普通
文字和静态内容直接位于 ambient 画布上，禁止为了“统一风格”把所有内容套入玻璃卡片。

玻璃由两个独立层组成：

1. **材质层**：低不透明度、柔和背景模糊、冷灰或语义色轻微着色；
2. **内容层**：清晰文字、焦点、边缘高光和交互命中区域。

不得通过降低整个组件的 `Opacity` 制造通透感，因为这会同时损害文字、焦点和可访问性。
材质层可以很轻，但内容层必须保持足够对比度。Windows Acrylic 自动补偿亮度时，应调整
独立材质层，而不是用更深的实心底色抵消。

### 3. 克制必须保留实体感

克制不等于全透明。每个交互表面至少保留以下三个信号中的两个：

- 低对比边缘或融入表面底色的 ambient glow；
- 与背景略有差异的半透明填充；
- 短距离、低模糊、低不透明度阴影。

阴影只说明“轻微浮起”，不得制造悬浮很高的卡片。禁止强反射、彩虹描边、发光边框、
多层高光、厚重黑影和炫技式 glassmorphism。

### 4. 精确对齐优先于装饰

同一语义组必须共享可验证的网格、基线和控件高度。标签、数值、冒号、单位及相邻控件
不能依赖目测 Margin 拼接。时间与日期控件使用明确的行列结构，数值在触发器内视觉居中，
冒号与两侧数值垂直居中。

相关元素应形成连续组；不相关元素才通过更大的留白分层。任何留白都必须回答“它在连接
什么，或分开什么”。如果空白让小时与分钟、标题与控件、快捷日期与具体日期看起来属于
不同区域，即使数值整齐，也属于失败的布局。

### 5. 留白提供呼吸，不制造割裂

留白用于降低认知负担、保护当前任务和建立页面节奏，不用于补救不稳定布局。空间层级遵循：

- 组件内部间距最小；
- 同一字段组内部间距次之；
- 不同字段组之间更大；
- 提醒与中央专注内容之间保留最大的可忽略空间。

气泡展开、窗口缩放或文本换行后，这一关系仍需成立。禁止通过随机的 13px、17px 等一次性
数值修补单个截图；应优先复用间距尺度和网格规则。

### 6. Progressive Disclosure 保护专注

非当前信息默认折叠，只暴露“发生了什么”和一个低层级入口。详细名称、冲突关系和处理
操作只在用户主动展开后出现。提醒不得一次同时展示说明、多个按钮和完整数据。

专注首页的视觉层级固定为：

1. 当前任务或“开始”入口；
2. 托管确认；
3. 必要但可忽略的折叠提醒；
4. 顶部抽屉等导航工具。

提醒不能因语义重要而自动获得与当前任务相同的面积或对比度。语义严重性通过小型颜色
标识、文字和可访问名称表达，不通过整块红色或黄色实心背景表达。

### 7. 连续组件表达连续操作

同一操作的多个部分应共享一个表面。Number Stepper 使用单个胶囊承载
“减号—可直接编辑数值—加号”，不得重新拆成输入框、上下箭头块或多个相邻按钮。
小时—冒号—分钟以及年—月—日虽然可分别操作，但视觉上必须保持紧凑、均衡和连续。

组件形状由行为决定：圆形用于单一入口，胶囊用于连续的短操作或状态，圆角矩形用于表单、
列表和一般按钮。不得仅为“更像玻璃”而把所有组件改成胶囊。

### 8. Motion 解释变化，不表演变化

动效只解释内容从哪里出现、哪个表面被提起、哪个区域改变了高度。常规动效短促、安静，
不使用弹跳、过冲、旋转或夸张缩放。

布局变化必须连续：气泡展开时编辑区淡入并逐步增加真实高度，下方气泡同步移动且间距
不变；收起时执行相反过程。不得先显示内容再跳高、重建整个列表、造成重叠或改变滚动
位置。Windows“减少动态效果”开启时，布局直接到达最终状态。

### 9. 可访问性是材质与动效的下限

透明度、模糊和低层级不能牺牲文字对比度、键盘焦点或命中区域。颜色不是唯一状态信号；
所有图形按钮必须有可读名称；直接编辑、按钮步进和拖拽必须提供键盘路径。减少透明度和
减少动态效果均应有明确降级，而不是简单隐藏内容。

## Design Review Gate

新增或修改界面在合入前必须回答：

- 它是否保护了当前任务作为唯一主层级？
- 玻璃是否只用于有层级意义的表面？
- 材质层与内容层是否独立，文字是否仍清楚？
- 同组标签、控件和数值是否共享网格与基线？
- 留白是在连接/分组，还是造成了视觉割裂？
- 展开前是否只暴露必要信息？
- 动画是否解释真实布局变化，并支持减少动态效果？
- 键盘、焦点、对比度和非颜色状态信号是否完整？
- 在真实 Windows 窗口而非只看 XAML 或截图时，组件是否仍安静、清楚、稳定？

## Color

源设计值使用 OKLCH，Avalonia 中使用经过对比度校验的 sRGB 近似值。

- Background: `#111418`，近黑冷灰
- Background deep: `#0D1014`，静态渐变暗部
- Focus ambient top: `#3084A6`，明亮蓝青
- Focus ambient middle: `#29638F`，承托白色文字的中蓝
- Focus ambient deep: `#1C3A6F`，深钴下沉色
- Focus mist: `#7EDCE1`、`#4DB9D6`、`#6F7BD3`
- Glass light: `oklch(0.99 0.006 245 / 0.62)`
- Glass dark: `oklch(0.28 0.018 245 / 0.56)`
- Ink: `oklch(0.23 0.018 250)`
- Muted ink: `oklch(0.43 0.018 250)`
- Primary: `oklch(0.58 0.15 245)`，冰蓝
- Primary pressed: `oklch(0.51 0.15 245)`
- Mandatory: `oklch(0.58 0.18 25)`，不可移动事件语义红
- Warning: `oklch(0.76 0.13 82)`，跨日提示
- Rest: `oklch(0.76 0.08 175)`，低饱和青绿
- Error: `oklch(0.52 0.19 25)`

颜色策略为 restrained。冰蓝只用于主操作、焦点和选中状态；红色只用于不可移动事件与冲突；黄色只用于跨日；青绿只用于休息模板。

## Material

- 可交互玻璃表面使用独立低透明材质层、轻量模糊、融入底色的 ambient glow 和
  1px 低对比边缘；光感必须由圆角表面自身裁切，不得作为独立亮条越过边界；
  文字、焦点和命中区域位于独立内容层。
- 普通内容不使用嵌套玻璃卡片，依靠留白和字体层级组织。
- 卡片和气泡圆角为 9–14px；输入框为 9–10px；连续短操作、状态标签和“开始”
  可以使用胶囊。组件形状必须由行为决定，不得无差别胶囊化。
- 浮层阴影使用冷色、短距离、低模糊，不使用纯黑大面积阴影。
- 浮层与触发器之间保留 6–8px 的自然间隔，像从 ambient 背景轻微浮起，而不是盖上
  一块新面板。
- 可编辑文字区域在静止、悬停和输入状态下都不得出现纯黑或近黑的内层填充。静止时
  只保留低对比边缘并让 ambient 自然透出；用户主动聚焦后，使用约 180ms 的轻微上浮、
  克制缩放、冰蓝边缘内高光和短距离冷色阴影确认交互，失焦后平静回落。焦点反馈说明
  “你正在修改这里”，不得用持续高压背景暗示“你现在应该看这里”。
- 连续控件中的文字编辑（例如 `GlassNumberStepper`）由整个连续表面响应焦点，不得
  只在中间数值后突然出现独立黑块。
- 系统不支持背景模糊或用户要求减少透明度时，玻璃降级为不透明冷银表面，并保留边缘和对比度。

## Typography

- 使用 Windows/Avalonia 可用的统一系统无衬线字体栈，优先 Segoe UI Variable，回退 Segoe UI。
- 排版使用语义角色统一维护：页面标题 30px、表单标题 28px、区块标题 20px、
  正文 16px、次要文字 14px、说明与状态文字 13px。相同职责不得在不同页面随意改字号。
- 深色背景正文使用约 1.5 倍行高；标题使用更紧凑的 1.2–1.25 倍行高。
- 当前任务名称 36–44px、SemiBold，最多三行。
- 页面标题 26–30px、SemiBold。
- 正文 15–17px。
- 控件标签 13–14px。
- 不使用装饰性大写眉题、渐变文字、超紧字距或混合字体。
- 所有时间只允许在添加表单和管理气泡的持续时间编辑器中出现；专注界面完全不显示时间。

## Launch Motion

- 应用窗口第一次进入前台时播放一次约 7.2 秒的品牌开场；数据初始化与动画并行，
  不把开场伪装成加载进度。
- 开场从纯黑开始，中央先闪出极弱冰蓝光点，并独立停留约 1 秒；在此之前不得出现
  彩色流体。随后蓝青、深钴和少量紫色流体才从中心以柔化、有机边缘扩散。
- 流体显现后直接读取只读终稿 `docs/design/final.svg`。一个克制的冰蓝光点沿该 SVG
  已有封闭 path 的真实轮廓移动，并留下带 ambient glow 的 Stroke Outline；不得重新
  生成、简化、平滑、拆分或写回任何路径坐标，也不得使用水平裁切或整词透明度代替
  轮廓描绘。艺术字只用于这次品牌开场，不进入按钮、标签、表单或正文排版。
- `Guardian` 字标固定采用大幅倾斜的 `G`、向下回旋的长尾、紧凑连写的小写字母、
  明显的 `d` 高杆和 `n` 后向右延伸的收笔；线条纤细、圆润，不得退回圆体手写字体
  或平均高度的普通 cursive 字形。
- `final.svg` 是字形和笔画几何的唯一终稿。实现只能读取其单一填充 path 及 path
  内已有 contour；画布上的缩放、居中、描边和发光属于渲染参数，不得改变源文件。
- 光点不改写终稿 path，而是把原轮廓在运行时映射为八个确定阶段。`G` 必须先从
  右上方起笔，沿 C 形双侧轮廓连续完成；C 完成前，交叉连接与下方回环不得抢先出现。
  随后才依次完成 `G` 的连接、下方回环与小写字母。全部轮廓完成以前保持
  空心；轮廓完全显出后，实体填充才以短促、平滑的材质转换加入，同时保留边缘
  ambient glow。该转换不是重新书写，也不得提前让实体字形透出。
- 字迹完成后先完成白色实体填充，再做一次清楚但仍低于主轮廓亮度的柔和呼吸微闪，并保留
  足够的静止阅读时间后逐渐退场；开场层末段与底下常驻 ambient shader 交叉淡化，
  不得出现黑场、背景跳切或单独的科技扫描线。
- 开场期间右上角最小化、最大化和关闭按钮不可见且不可点击；进入专注界面后才以
  低速度淡入。字标使用其真实可见几何边界在窗口中水平、垂直居中，不以 SVG
  viewBox 中的透明留白计算对齐。
- 开场不使用粒子爆发、HUD、锐利霓虹、闪烁或机械音效。窗口最小化时暂停时间轴，
  恢复可见后继续。
- Windows 开启减少动态效果时不播放开场，窗口直接显示已经初始化的主界面。

## Focus Surface

### 未开始

- 即使当前时刻存在待办，也不显示名称。
- 页面中央显示八个低对比圆环围绕中央圆形“开始”入口，形成安静而明确的进入仪式。
- 中央入口使用微发光实体边缘；外围圆环必须更分散、更低透明，不得与入口争夺层级。
- 蓝青 ambient 色场已经存在，色彩对比相对鲜明且流动速度高于稳定专注态。
- 顶部保留低存在感下拉把手、折叠提醒和跨日提示。

### 已开始

- 点击“开始”产生明确按下反馈。
- 当前事件名称从上至下淡入，纵向位移 12–18px，时长约 420ms。
- 圆环入口退场时，背景流体先短促加速一次，再自然降低色彩对比和流速，进入安静
  专注态；加速只确认“专注已经开始”，不能成为持续刺激。减速尾段保持约
  1–1.5 秒，使用户能看见状态切换，同时避免突然静止。
- 当前任务切换或结束时使用同类短促流动脉冲作委婉提醒，随后回到稳定专注态。
- 色场以 GPU runtime shader 生成连续域扭曲流体；没有射线、硬边或鼠标跟随，
  任务文字始终位于其上。平台无法取得 shader 绘制能力时保留静态渐变降级。
- 主内容只保留当前事件名称与“未来的事项已为你托管”两句话。
- 不显示开始、结束、安全截止、剩余时间、倒计时或进度条。
- 没有当前事件时显示平静空状态，不自动暴露未来事件。

### 五分钟前提醒

- 只有当前事件与下一非休息事件首尾相接，且当前事件持续不少于五分钟时发送。
- Windows 通知同时显示当前事件与下一事件名称。
- 下一事件是休息、有空档、没有下一事件、应用完全退出时不提醒。
- 休息结束后若紧接普通或不可移动事件，则正常提醒下一事件。
- 冲突未解决时采用新输入优先，同时在通知中显示两个冲突名称。

## Top Drawer

- 窗口使用完全连续的无标题栏画布，不在顶部绘制贯穿全宽的任务栏、标题文字或
  分隔线。
- 右上角悬浮三个 36px 圆形控件，依次承担最小化、最大化/还原和关闭；关闭按钮
  只在悬停时出现克制的暗红反馈。
- 顶部中央把手区域负责下拉抽屉；除此之外的顶部空白区域可以拖动窗口。正常窗口
  保留 14px 圆角，最大化时圆角归零；边缘 6px 区域负责八方向缩放。
- 窗口顶部常驻一条细短玻璃把手，不显示文字。
- 用户从顶部向下拖动后，抽屉随指针移动并有阻尼。
- 展开后显示一条顶部中央的细长玻璃胶囊，内部横向排列“添加事件”和“事件管理”。
- 抽屉从上方 24px 位置在 280ms 内向下落位并渐显；收起时反向播放，结束后从
  可访问树中移除。
- 两个入口严格等分整个抽屉宽度，文字水平和垂直居中，以细分隔线区分；悬停或
  键盘聚焦时才出现轻微高光，不显示大型卡片按钮。
- 点击任一按钮或抽屉外部时收回；不提供额外收起按钮。
- 抽屉内容需要完整键盘访问，并提供与拖拽等价的可聚焦操作。

## Alerts

### 到期未来待办

- 默认折叠，只显示“有当日未设置时间待办”和右侧展开按钮。
- 展开后显示事项名称，不直接放置每条操作按钮。
- 展开区只提供一个“进入管理”按钮。

### 不可移动事件冲突

- 使用 320–430px 的居中窄幅低层级玻璃提醒，不使用贯穿页面的大面积粉红或红色底。
- 折叠时只显示小型语义红点、简短标题和展开入口。
- 展开后显示冲突组名称，只提供一个“进入管理”按钮。
- 提醒与中央专注内容保留充分垂直空间，使用户不处理时可以舒服地忽略它。
- 进入管理后自动滚动到第一组冲突。

### 顺延反馈

- 任意顺延成功后，从顶部缓慢下拉“有事件被顺延”，三秒后自动缩回。
- 跨日另显示“有事件被顺延至明日”，并提供关闭按钮。
- 两种提示可以同时出现。
- 四类首页提醒必须复用同一 `AlertGlass` 规格：430px、相同低透明冷灰材质、边缘、
  圆角、短阴影、内边距和文字层级。黄色、红色等语义只允许用于 6–8px 状态点，
  不得把整块提醒染成实心语义色。

## Add Flow

类型选择页只显示两个入口：

- 当日待办：`今天要做什么？`
- 未来待办：`这些事情之后做`

当日待办表单包含名称、开始时间、持续时间小时/分钟输入和“不可移动事件”选择。
结束时间由系统计算，不显示可编辑字段。当日待办不显示日期选择；日期固定使用
用户保存操作发生的本地当天。开始时间的小时、冒号和分钟组成紧凑控件组，持续时间
使用整数小时与分钟输入。开始时间和持续时间必须共享字段标题行、单位标签行和
44px 控件行的水平基线。

持续时间使用统一的 `GlassNumberStepper`：

- 单个连续胶囊内依次为减号、可直接编辑数值和加号；
- 中间数值支持鼠标定位、键盘输入、回车和失焦提交；
- 不使用上下箭头、深色分割块或突兀分隔线；
- 添加页和管理页不得为同一行为创建不同外观。

未来待办表单包含名称、具体日期和相对日期快捷选项。第一版直接显示等宽的
“一天后”和“两天后”按钮，当前选项使用冰蓝选中态；点击快捷按钮后同步更新下方
年/月/日，用户手动修改任一日期选择器后取消快捷选中态。保存时两种日期表达只能
提交一种。

### 展开选择器

- 当日开始小时/分钟、未来待办年/月/日、未来待办管理日期和休息模板时长统一使用
  `GlassPicker`，不得混入系统默认 ComboBox 或三列滚轮 DatePicker。
- 触发器是 44px 高的深色圆角按钮；当前值在控件中真正视觉居中，箭头位于右侧独立
  对称列，不得用不均匀 Padding 假装居中。
- 浮层使用 14px 大圆角 `ExperimentalAcrylicMaterial` 独立材质层，采用低不透明冷灰
  着色、真实背景模糊、1px 内边缘和极弱短阴影；平台不支持时退化为深色半透明表面。
- 选项纵向排列，单行至少 42px；悬停和选中只增加半透明灰白高光，不使用整行
  高饱和蓝色。
- 未来待办日期明确拆成“年 / 月 / 日”三个独立选择器，月份或年份变化后自动修正
  不存在的日期，例如二月没有 31 日。

从到期事项进入规划时直接打开当日待办表单，名称预填且允许修改。未保存返回时原事项保持活动。

## Management Surface

- 管理页高密度堆叠的折叠当日事件、休息事件、队尾入口与底部休息 Stepper 统一使用 64 point
  Capsule 高度和高度一半的连续圆角。只有双击展开的编辑态允许随内容自然增高，但仍保留同一
  圆角语言；不得让相邻静止 Capsule 因文字、图标或按钮内容不同而产生高度跳变。
- 管理页只展示结束时刻晚于当前时刻的活动当日待办。结束时刻等于或早于当前时刻的
  任务标记为已完成并从活动查询隐藏，不继续占用列表、排序或冲突组；数据库历史记录
  必须保留，且 v0.1.0 不提供已完成历史入口。
- 当日待办为纵向长条气泡，顺序就是优先级和执行顺序。
- 管理页默认不显示开始时间。页头提供低存在感的时间显示切换按钮；开启后，每个气泡仅在
  末尾显示开始时间，不增加结束时间、倒计时或时间轴。该选择只改变显示，不改变排程。
- 普通气泡为冷银玻璃；不可移动事件为语义红玻璃；休息气泡为青绿玻璃且名称固定。
- 普通气泡可拖拽排序。提起时轻微放大、边缘高光增强，列表留出真实占位；落下时使用快速无弹跳缓动。
- 普通事件无法放入不可移动事件前时，自动落在最后一个阻挡事件后，并显示“这个事件没办法放在这里”。
- 冲突或首尾相接的不可移动事件形成可拖拽连续组；其他不可移动事件不可拖动。
- 冲突组同一行等分显示，进入页面自动定位第一组。
- 双击气泡后在约 240ms 内以 ease-in-out 平滑展开名称、持续时间、删除和不可移动
  复选框，并增加可修改的开始时间；再次双击先柔和收起，再统一试算、确认、保存和重排。
- 修改开始时间与普通事件相撞时，使用确认弹窗让用户选择“原事件移到此事件之后”或
  “原事件在此时间结束”。若撞到不可移动事件则直接说明无法修改，不显示无效选择。
- 展开与收起通过真实高度和透明度连续变化，下方气泡同步移动并保持原间距；不得重建
  整个列表、产生重叠或跳动。保存失败时恢复展开状态。
- 离开时若存在未收起修改，询问是否保存。
- 所有删除都显示包含名称的确认弹窗，默认焦点在取消。
- 底部正中悬浮休息实体默认 20 分钟，可展开调整时长并重复拖入；宽屏和窄屏均不建立右侧模板栏。
- 底部休息实体的拖拽手势不得抢占 Stepper 子按钮。减号与加号均使用完整 44 point 命中区，
  连续点击必须对称响应；拖拽仅在移动超过空间拖拽阈值后接管。
- 未来待办默认折叠，主动展开后按日期从近到远显示。气泡不可拖拽，双击编辑名称、日期或删除。

## Motion and Tactility

- 常规状态切换 180–240ms，使用 ease-out-quart 或 ease-out-quint，不使用 bounce/elastic。
- 涉及展开和收起的双向布局变化使用 220–260ms ease-in-out；单向进入使用 ease-out。
- 按钮按下缩放到 0.97–0.98，并有 1px 下沉感。
- 拖拽使用指针捕获和变换动画，不通过频繁重建完整视觉树制造卡顿。
- 展开/收起通过透明度、裁剪和真实高度变化表达状态，不使用闪烁；相邻内容必须随布局
  连续移动，间距在整个动画过程中保持一致。
- 主页面之间切换采用逐元素 fluid reveal，不再对整页做缩放、淡化，也不绘制覆盖
  画布的闪光或气泡遮罩。共享 ambient 背景在导航前后始终连续。
- 新页面的标题、主面板和次级区域按视觉顺序错峰出现：每项从下方 18px、
  0 透明度和 4px 模糊开始，使用 ease-out-quart 在 360ms 内上浮并变清晰，
  相邻元素间隔 58ms。
- 表单页面先让承载面板柔和浮现，再依次显示标题和字段；管理页面依次显示页头、
  主列表和休息面板。动画被连续导航打断时必须恢复上一组元素的原始视觉属性。
- 专注色雾是唯一允许持续运动的背景层。未开始时对比更高、流速更快；稳定专注态
  对比更低、流速更慢。开始、任务切换和任务结束只产生一次短促加速脉冲；边界完全
  虚化，中心保留足够深度承托白色文字。
- Windows 窗口隐藏、最小化、脱离视觉树或关闭时停止刷新 shader；恢复可见后再
  继续，避免不可见时消耗 GPU。
- 减少动态效果时冻结色雾位置，取消逐元素上浮、气泡高度动画和拖拽惯性；页面内容
  直接显示最终状态。

## Accessibility

- 正文和控件满足 WCAG AA；大字至少 3:1，正文至少 4.5:1。
- 焦点环清晰，不只依赖颜色。
- 拖拽必须提供键盘等价操作：选中后上移、下移或移动到目标组。
- 红色不可移动气泡同时使用可访问名称说明其状态。
- 展开按钮、顶部把手、删除按钮和关闭按钮均有可读名称。
- 系统通知不作为唯一状态来源，应用内仍保留冲突和到期提醒。

## Final Verification Interface

最终验收与诊断界面仍在主要功能完成后实现。它必须使用样例数据、简单规则说明、原计划与结果对比，并明确显示已应用或冲突仍待管理；不得默认展示用户真实待办。
## Homepage Reminder Consistency

- 首页常驻提醒与短时顺延反馈统一使用 430px 居中窄幅玻璃表面；它们共享尺寸和环境材质，但继续使用各自必要的语义色与关闭行为。
- 到期未来待办与不可移动事件冲突默认折叠，不显示向下箭头。用户双击整块提醒表面展开，再次双击收起，与管理页气泡的交互语言保持一致。
- 到期未来待办展开后按事项显示“规划”。规划进入当日待办表单并预填原名称；提醒本身不再把用户送往不承担添加职责的管理页。
- 双击只是用户主动索取详情的手势，不通过持续高亮、强对比或静止遮罩迫使注意力停留。
- 大字号任务名称必须保留完整字体行盒和下行部空间；`g`、`y`、`p`、`q`、`j` 等字母不得被视觉容器裁切。
